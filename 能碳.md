# 能碳监测平台 · 产品文档

> **面向算力工业园区的低碳算力能碳分析平台**
>
> 本文件有两个作用：
> 1. **给你看**——这是产品说明书，讲清楚这个软件要做成什么样。
> 2. **给 Claude Code 看**——每次打开 Claude Code 会通过根目录 `CLAUDE.md` 的 `@能碳.md` 导入自动读到本文件。以后你不用重复解释需求，直接说「按 能碳.md 做」。
>
> 文档版本：**v5.0** ｜ 状态：产品定义已定稿，尚未开始编码

---

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-21 | 初版。通用工厂能碳分析工具 |
| v2.0 | 2026-09-01 | 改为智算中心算力能碳管理；引入 Agent；新增碳资产 |
| v3.0 | 2026-09-02 | 确立「算力工业园区」框架、百万 Token 碳足迹、三性分析方法论 |
| v4.0 | 2026-09-02 | 以三个核心问题重构 Agent 框架；新增空置率与空载能耗、储能有效性评估、使用强度驱动的寿命折损与回收再利用；文档结构压缩 |
| **v5.0** | **2026-09-06** | **Agent 架构改为「主 Agent + 三个 SubAgent」**；新增能碳预测、电网级风光趋势、跨数据库因子对齐三项能力；文档更名为 `能碳.md` |

### v5.0 相对 v4.0 的四处关键变化

1. **Agent 架构重构**：由单个 Agent 改为**主 Agent 编排 + 三个专职 SubAgent**（能碳预测 / 低碳识别 / 因子对齐与核算），工具集在代码层按 Agent 分开挂载，见[第 8 节](#8-agent-架构主-agent--三个-subagent)
2. **新增预测能力** ⭐：v4.0 只做事后测算，v5.0 的 SubAgent 1 可**预测**待执行任务的能耗与碳排，并强制给出 P25–P75 区间与样本数
3. **新增电网级风光趋势** ⭐：v4.0 只有园区自建风光（决定"你用的电有多绿"），v5.0 补上**当地电网风光出力趋势**（决定"你买的电有多绿"），二者数据源与作用机制不同，**必须分开**
4. **新增跨数据库因子对齐** ⭐：v4.0 只有单张因子表，v5.0 的 SubAgent 3 可在多个排放因子库间**检索、对齐、呈现分歧并做换库敏感性分析**，且**绝不自动选定一个因子隐藏分歧**

### v4.0 相对 v3.0 的四处关键变化

1. **Agent 框架改为问题驱动**：不再罗列职责，而是围绕[三个核心问题](#12-三个核心问题agent-要回答的)组织，每个问题明确「要什么数据 → 调什么工具 → 产出什么结论」
2. **新增空置率与空载能耗**：卡闲着也在耗待机电，这是纯浪费。原有的「利用率」盖不住这件事
3. **新增储能**：v3.0 只有风电光伏。储能有充放电和往返效率损失，**可能不减碳反而增碳**，必须单独测算净效果
4. **隐含碳摊销改为按使用强度**：v3.0 按日历时间摊销，无法回答「使用频率如何影响生命周期」。v4.0 引入等效运行小时、寿命折损、残值与回收再利用

### 文档结构（v4.0 压缩后）

| 部分 | 小节数 | 内容 |
|---|---|---|
| 第一部分 · 产品定义 | **2** | 产品概述与框架、名词解释 |
| 第二部分 · 功能规格 | 9 | 五个模块 + Agent 架构（主+三子）+ 基础表 + 数据模型 + 界面 |
| 第三部分 · 技术与工程 | **3** | 技术方案与栈、目录与路线图、已知限制 |
| 第四部分 · 协作约定 | 2 | 常用命令、工作约定 |

---

## 目录

- [第一部分 · 产品定义](#第一部分--产品定义)
  - [1. 产品概述与算力工业园区框架](#1-产品概述与算力工业园区框架)
  - [2. 名词解释（零基础必读）](#2-名词解释零基础必读)
- [第二部分 · 功能规格](#第二部分--功能规格)
  - [3. 模块一 · 数据接入与三性分析](#3-模块一--数据接入与三性分析)
  - [4. 模块二 · 高能耗时段、空置率与风光储有效性](#4-模块二--高能耗时段空置率与风光储有效性)
  - [5. 模块三 · 百万 Token 碳足迹与服务器生命周期](#5-模块三--百万-token-碳足迹与服务器生命周期)
  - [6. 模块四 · 能源成本与碳资产](#6-模块四--能源成本与碳资产)
  - [7. 模块五 · 低碳算力工作流](#7-模块五--低碳算力工作流)
  - [8. Agent 架构（主 Agent + 三个 SubAgent）](#8-agent-架构主-agent--三个-subagent)
  - [9. 基础表管理](#9-基础表管理)
  - [10. 数据模型](#10-数据模型)
  - [11. 界面结构](#11-界面结构)
- [第三部分 · 技术与工程](#第三部分--技术与工程)
  - [12. 技术方案与技术栈](#12-技术方案与技术栈)
  - [13. 目录结构与开发路线图](#13-目录结构与开发路线图)
  - [14. 已知限制与不做清单](#14-已知限制与不做清单)
- [第四部分 · 协作约定](#第四部分--协作约定)
  - [15. 常用命令](#15-常用命令)
  - [16. 给 Claude Code 的工作约定](#16-给-claude-code-的工作约定)
- [附录 A · 基础参数初始值](#附录-a--基础参数初始值)
- [附录 B · 核心公式与单位换算](#附录-b--核心公式与单位换算)

---

# 第一部分 · 产品定义

## 1. 产品概述与算力工业园区框架

### 1.1 一句话定位

**能碳监测平台**是一个跑在你自己电脑上的算力能碳分析工具：把算力园区当工厂看，接入用电、风光储、服务器和任务 Token 数据，由一个 **Agent** 分析这些数据的**差异性、时序性和不稳定性**，回答三个核心问题，最终形成一套可复用的**低碳算力工作流**。

### 1.2 三个核心问题（Agent 要回答的）

**整个平台是围绕这三个问题设计的。** 功能模块、数据模型、Agent 工具集，全部服务于它们。

#### 🔍 问题一 · 谁在什么时候费电，机器是不是闲着

- 服务区在**什么时段、运行哪些模型**时能耗最高？
- **空置率**是什么情况——有多少卡装了却没跑任务，闲着还在耗待机电？
- **哪些时段负荷突然增高**，是什么触发的？

#### 🔍 问题二 · 风光储到底有没有发挥作用

- **风力发电、光伏发电、储能**是否真正发挥了作用，还是装了看着好看？
- 能耗高频出现的原因是什么？

> ⚠️ 储能尤其要小心：它有**往返效率损失**，如果充电时段的碳强度不够低、放电时段不够高，**储能会净增碳而不是减碳**。平台必须算出这个净值，而不是默认它一定有用。

#### 🔍 问题三 · 不同时段的碳足迹，以及使用频率对服务器寿命和回收的影响

- 不同时段的**碳足迹**差多少？同样产 100 万 Token，谷段和峰段排碳差几倍？
- **不同使用频率**如何影响服务器的**生命周期成本**？
- 不同生命周期内的不同使用频率，如何影响服务器的**回收和重复利用**？

> 这一问推翻了 v3.0 的一个做法：隐含碳原来按**日历时间**摊销（用了多少天），但你问的是**使用频率**的影响。v4.0 改为按**等效运行小时**摊销——跑得狠的机器老得快，摊销也该快。见 [5.4](#54-使用强度与寿命折损v40-新增)。

#### 三个问题与三个 SubAgent 的对应

v5.0 把 Agent 拆成主 Agent + 三个 SubAgent，与三个核心问题**不是简单一一对应**，而是按**时态**重新划分：

| SubAgent | 时态 | 主要服务 | 新增能力 |
|---|---|---|---|
| **1 · 任务需求与能碳预测** | **未来** | 问题一 | ⭐ **预测**（v4.0 只做事后测算） |
| **2 · 风光趋势匹配与低碳识别** | **未来 × 时段** | 问题二 | ⭐ **电网级**风光趋势（v4.0 只有园区自建） |
| **3 · 因子对齐与碳核算报告** | **过去** | 问题三 + 数据库构建 | ⭐ **跨数据库**因子对齐（v4.0 只有单张表） |

详见[第 8 节](#8-agent-架构主-agent--三个-subagent)。

### 1.3 算力工业园区框架

整份文档建立在这个类比上。工业碳核算几十年的方法论可以直接搬过来。

| 传统工业园区 | **算力工业园区** | 平台里对应 |
|---|---|---|
| 园区 / 服务区 | 算力服务区 | `park_id`（首个实例：纳溪服务区） |
| 车间 | 机房 / 集群 | `room_id` |
| 生产线 | **服务器机型（异构）** | `server_model` |
| 设备 | 单台节点 / 单张卡 | `device_id` |
| 原料 | **电力**（市电 + 风 + 光 + 储） | `energy_records` 等 |
| 工序 | **计算任务类型**（训练/微调/推理） | `job_type` |
| **产品** | **Token** | `token_records` |
| 单位产品能耗 | 每百万 Token 电耗（kWh/MTok） | 核心指标 |
| **单位产品碳排** | **每百万 Token 碳足迹**（gCO₂e/MTok） | **最核心指标** |
| 设备能效 | Tokens/kWh | 差异性分析主指标 |
| **产能闲置** | **空置率 / 空载能耗** | 问题一的核心 |
| 设备折旧与报废 | **等效寿命消耗 / 残值 / 回收** | 问题三的核心 |
| 边角料 / 损耗 | PUE 损耗（制冷、供配电） | PUE − 1 部分 |

> **平台支持多服务区**（`park_id`）。**纳溪服务区**为首个分析对象，其装机规模、PUE、电价等参数在[园区结构表](#9-基础表管理)里按实际填写，平台不内置任何假设值。

### 1.4 核心指标体系

| 层级 | 指标 | 单位 | 服务于 |
|---|---|---|---|
| **顶层** | **每百万 Token 碳足迹** | gCO₂e/MTok | 对外交代 · 问题三 |
| 顶层 | 每百万 Token 电耗 | kWh/MTok | 纯能效 |
| 顶层 | 绿电利用比例 | % | 问题二 |
| **中层** | **空置率** | % | **问题一** |
| **中层** | **空载能耗占比** | % | **问题一** |
| 中层 | 使用强度 | % | 问题三 |
| **中层** | **等效寿命消耗率** | % | **问题三** |
| 中层 | 储能净减碳量 | tCO₂e | 问题二 |
| 中层 | 能效 | Tokens/kWh | 机型对比 |
| 中层 | PUE | — | 机房效率 |
| 中层 | 度电碳强度 / 度电成本 | gCO₂e/kWh · 元/kWh | 随时段变化 |
| 底层 | 卡时、功率、负载率、利用率 | 卡·h / kW / % | 原始活动数据 |

### 1.5 它不解决什么问题（先说清楚）

- ❌ **不是单纯的能碳数据平台**——存数、画图、出报表只是手段，**分析数据的三性、回答三个核心问题才是目的**
- ❌ **不做碳交易**——碳配额、绿证只做台账和测算，不接交易所、不做定价
- ❌ **不做实时调度**——平台给建议，不直接控制算力调度、储能充放电或制冷系统
- ❌ **不做带约束的成本-碳联合优化求解**——做测算、识别、归因、建议，不做优化求解器
- ❌ **不做设备可靠性预测**——寿命折损是**工程简化模型**，不是失效物理模型，不预测哪台机器什么时候坏
- ❌ **不出具认证级报告**——仅供内部分析，不能用于 ISO 14064 / 14067 第三方核证
- ❌ **不做多人协作**——单机个人使用，无账号、权限、共享数据库
- ❌ **不替你判断数据对错**——平台负责测算和统计标记，数据质量由你把关

### 1.6 核心设计原则

1. **分析优先于展示**：每张图必须回答一个具体问题。做不出结论的图不做
2. **数据边界优先**
   - 原始明细（逐条功耗、设备序列号、任务名、机房位置）**只存本地 SQLite**
   - 发给大模型 API 的**只有聚合统计量**
   - 每次调 API 前，界面**必须展示将要发送的完整内容**，你确认后才发
   - 提供**纯本地模式开关**：关掉 Agent 后测算、图表、导出全部照常
3. **不静默采信**：解析和采集的数据先进「待确认」区，你确认后才入库
4. **可追溯**：每条数据记来源和时间；每次测算记因子版本和口径；每条 Agent 结论记基于哪些数据
5. **Agent 的话不是结论**：Agent 输出标注为「AI 分析建议」，与确定性计算结果**视觉区分**
6. **跨平台**：同一套代码，Windows 和 macOS 都能跑

### 1.7 目标用户

算力园区 / 服务区 / 智算中心里负责能源管理、双碳、运维或成本分析的个人。**不需要懂编程**，会用 Excel 就行。

---

## 2. 名词解释（零基础必读）

后面章节会反复用这些词，先讲明白。

### 2.1 算力与园区

| 名词 | 大白话解释 |
|---|---|
| **算力工业园区** | 把算力中心当工厂看：电是原料，Token 是产品，服务器是生产线。见 [1.3](#13-算力工业园区框架)。 |
| **Token** | 大模型处理文字的最小单位，约等于半个汉字。**它是算力园区的「产品」。** |
| **百万 Token（MTok）** | 100 万个 Token。单个 Token 能耗极小，行业习惯按百万个衡量。 |
| **异构** | 园区里有多种型号的服务器/加速卡，性能和能效各不相同。**异构是差异性分析的前提。** |
| **IT 负载 / IT 电耗** | 服务器、GPU、存储、网络设备本身用掉的电。**不含**制冷和供配电损耗。 |
| **PUE** | 机房总电耗 ÷ IT 电耗。PUE=1.3 表示 IT 每用 1 度，机房总共用 1.3 度。**越接近 1 越好。** |
| **卡时** | 一张 GPU 卡运行一小时 = 1 卡时。 |
| **负载率** | 设备实际功率 ÷ 额定功率。 |
| **能效** | 每度电产出多少 Token（Tokens/kWh）。**越大越好。** |

### 2.2 空置与使用强度（问题一、问题三）

| 名词 | 大白话解释 |
|---|---|
| **空置率** | 装了机器但没跑任务的比例。分两种：<br>**物理空置**——机位空着没装设备；<br>**逻辑空置**——装了设备但没有任务占用。 |
| **空载能耗** | 空置设备待机时仍然消耗的电。**这是纯浪费**——花了电，一个 Token 也没产出。 |
| **利用率 vs 空置率** | 利用率看的是「在跑的机器跑得满不满」；空置率看的是「有多少机器根本没在跑」。**两回事，不能互相替代。** |
| **使用强度** | 一段时间内，实际运行卡时 ÷ 额定可运行卡时。反映机器被用得多狠。 |
| **等效运行小时** | 按负载率加权后的运行小时。**满载跑 1 小时比空转 1 小时更耗寿命**，所以要加权。 |
| **等效寿命消耗率** | 累计等效运行小时 ÷ 设计寿命小时。**这台机器的命用掉了百分之几。** |

### 2.3 三性（方法论核心）

| 名词 | 大白话解释 | 回答什么 |
|---|---|---|
| **差异性** | 同样一件事在不同条件下差多少。本平台指：**不同任务/模型 × 不同服务器机型**的能耗差异。 | 「为什么这批任务比那批费电？」 |
| **时序性** | 数据随时间变化的规律：日内周期、周周期、趋势，以及**不同曲线之间的时间错配**。 | 「什么时候费电？绿电来的时候我在用吗？」 |
| **不稳定性** | 数据的波动、突变和异常程度。 | 「负荷突然增高是正常的还是出事了？」 |
| **变异系数（CV）** | 标准差 ÷ 均值。**无量纲**，可跨不同量级数据比较。 | 「哪个指标抖得最厉害？」 |
| **相位差** | 两条周期曲线高峰之间的时间差。例：负载高峰 20:00，光伏高峰 12:00，相位差 8 小时。 | 「负载和绿电对得上吗？」 |
| **突变点（变点）** | 数据统计特性明显改变的时刻。 | 「什么时候开始变的？」 |

### 2.4 能与碳

| 名词 | 大白话解释 |
|---|---|
| **活动数据** | 「消耗了多少」的原始数字（度电、卡时、Token），所有计算的起点。 |
| **排放因子** | 换算系数。例：1 度电排 570 克 CO₂ → 570 就是电力排放因子。 |
| **碳足迹** | 一件事或一个产品从头到尾排放了多少温室气体。 |
| **gCO₂e / tCO₂e** | 克/吨二氧化碳当量。1 tCO₂e = 1,000,000 gCO₂e |
| **功能单位** | 碳足迹「按什么算」的基准。**本平台是「百万 Token」。** |
| **运营碳排** | 机房运行时用电产生的碳排放。占大头。 |
| **隐含碳** | 服务器、GPU **在生产制造时**就已排放的碳。按使用强度摊到每个周期。 |
| **Scope 1/2/3** | 范围一：自己烧的（柴油机）；范围二：**外购电力，绝对大头**；范围三：硬件隐含碳等。 |
| **location-based（地域法）** | 用电网平均排放因子算。反映物理现实。 |
| **market-based（市场法）** | 扣掉买的绿电/绿证后再算。反映采购选择。<br>⚠️ **两种必须同时给出并注明**。 |

### 2.5 风光储与成本（问题二）

| 名词 | 大白话解释 |
|---|---|
| **风电 / 光伏** | 自建或采购的零碳电力。**出力看天吃饭，供给天然不稳。** |
| **储能** | 电池等储电装置。低谷充电、高峰放电，起「搬运电力」的作用。 |
| **往返效率（η）** | 放电量 ÷ 充电量。典型 85–92%，**中间损失的那部分电白烧了**。 |
| **储能净减碳** | 放电时段避免的碳排 − 充电时段造成的碳排。**可能为负**——那说明储能在增碳。 |
| **绿电利用比例** | 绿电用量 ÷ 总用电量。**分时段算才有意义**，月度总量会掩盖时间错配。 |
| **弃电（弃风弃光）** | 绿电发出来了但负载没跟上，白白浪费的部分。 |
| **间歇性** | 光伏晚上不发电、风电看天，供给天然断续。**不稳定性分析的重点之一。** |
| **分时电价** | 电价按时段分尖峰/高峰/平段/低谷，谷段可能只有峰段的三分之一。 |
| **需量电费** | 按**最高功率**收的固定费用，与用电量无关。**功率冲高一次，整月都按那个数交钱。** |

### 2.6 生命周期与回收（问题三）

| 名词 | 大白话解释 |
|---|---|
| **生命周期成本（LCC）** | 采购成本 + 全生命周期电费 + 运维成本 − 残值。**买得便宜不等于用得便宜。** |
| **残值** | 设备退役时还值多少钱。用得越狠，残值掉得越快。 |
| **梯次利用** | 从高要求场景退下来的设备，转到低要求场景继续用（如训练卡转推理、转测试环境）。 |
| **回收再利用** | 拆解回收金属和元器件。可**抵扣一部分已计入的隐含碳**。 |
| **可再利用潜力** | 剩余等效寿命的比例。剩得越多，越值得梯次利用而不是直接报废。 |

### 2.7 Agent 相关

| 名词 | 大白话解释 |
|---|---|
| **Agent（智能体）** | 能自己看数据、自己判断、自己给结论的 AI 助手。它会主动调用平台功能查数、算数，再综合出结论。 |
| **工具（Tool）** | Agent 能调用的功能，如「查某时段能耗」「评估储能有效性」。工具**全部在你本地运行**，Agent 只拿返回结果。 |
| **数据边界** | 规定哪些数据可以发给云端模型、哪些绝对不行的一条线。见 [1.6](#16-核心设计原则)。 |

---

# 第二部分 · 功能规格

## 3. 模块一 · 数据接入与三性分析

> **v4.0 把原来的「数据接入」和「三性分析」合并成一个模块。** 理由：数据进来的目的就是被分析，分开讲会让人误以为「先建个数据库再说」——那恰恰是本平台反对的。

### 3.1 五类数据

| # | 数据类型 | 干什么用 | 服务于 | 没有它会怎样 |
|---|---|---|---|---|
| A | **能耗数据** | 电耗、PUE、碳排 | 全部 | 平台没法用 |
| B | **Token 计量** | 每百万 Token 碳足迹 | 问题三 | **核心指标算不出来** |
| C | **风光储数据**（园区自建 + **电网级** ⭐） | 电源有效性、分时绿电比例、**低碳窗口识别** | **问题二** | 问题二无法回答；缺电网级数据则低碳窗口识别不可用 |
| D | **任务与机型台账** | 任务×机型归因、空置率 | **问题一** | 做不了异构归因和空置分析 |
| E | **设备生命周期台账** | 寿命折损、LCC、回收 | **问题三** | 只能按日历时间摊隐含碳 |

#### A · 能耗数据

| 途径 | 方式 | 拿到什么 |
|---|---|---|
| 文件导入 | Excel / CSV / PDF / Word | 用电台账、分项电表报表、电费账单 |
| 实时采集 · GPU | NVML（`pynvml`） | 每张卡功率(W)、利用率、显存、温度 |
| 实时采集 · 整机 | IPMI / Redfish | 整机功率、进出风温度 |
| 实时采集 · 机房 | 电表 / 动环系统 | 总电耗、制冷电耗、UPS 电耗 → 算 PUE |

**采样策略**：默认 60 秒一次。原始明细保留 30 天，之后按 5 分钟均值降采样归档。

#### B · Token 计量

> **假设**：来自推理服务或网关的统计日志（vLLM / TGI metrics、API 网关日志、自研调度系统导出）。

关键字段：统计区间、**模型标识**（问题一要按模型看能耗）、任务类型、`tokens_in`、`tokens_out`、关联机型、关联节点。

**训练任务**：不产出推理 Token，改用**训练消耗 Token 数**作为产量口径，与推理**分开展示、不混算**。

> ⚠️ **口径必须说清楚**：用 `tokens_in + tokens_out` 还是只用 `tokens_out`，行业无统一标准。平台**默认用总和**，可切换，且**结果上必须标注**。

#### C · 风光储数据（v4.0 扩展）

**发电侧**（风电、光伏分别记录）

| 字段 | 示例 |
|---|---|
| 时间区间 | 2026-07-03 12:00 ~ 13:00 |
| 电源类型 | 光伏 / 风电 / 其他 |
| 理论可发量 | 200 kWh |
| 实际上网/供给量 | 180 kWh |
| 实际消纳量 | 145 kWh |
| 合同编号 | PPA-2026-007 |

**储能侧**（v4.0 新增，问题二的关键）

| 字段 | 示例 | 说明 |
|---|---|---|
| 时间区间 | 2026-07-03 13:00 ~ 14:00 | |
| 充电量 | 120 kWh | 该时段充进去多少 |
| 放电量 | 0 kWh | 该时段放出来多少 |
| 充电来源 | 光伏 / 市电 / 混合 | **决定充电碳强度，极关键** |
| SOC 起止 | 32% → 68% | 荷电状态 |
| 额定容量 / 功率 | 500 kWh / 250 kW | |

> **绿证（GEC）是例外**：不带时间属性，只在月度/年度总量上抵扣。单列在碳资产台账，**不参与分时计算**。

#### D · 任务与机型台账

**任务日志**：任务 ID、名称（可脱敏）、类型、起止时间、占用节点/卡数、归属对象。

**服务器机型台账**：机型标识、加速卡型号/数量、单卡额定功率、整机额定功率、**待机功率**（算空载能耗用）、隐含碳、设计寿命、所属机房。

> **`待机功率` 是 v4.0 新增的必填字段**——没有它算不出空载能耗，问题一答不完整。

#### E · 设备生命周期台账（v4.0 新增）

| 字段 | 示例 | 用途 |
|---|---|---|
| 设备 ID / 机型 | node-12 / H100-8卡 | |
| 投产日期 | 2024-03-15 | 算日历役龄 |
| 采购成本 | 480,000 元 | LCC |
| 累计运行小时 | 12,400 h | |
| **累计等效运行小时** | 14,100 h | 按负载率加权，见 [5.4](#54-使用强度与寿命折损v40-新增) |
| 当前状态 | 在役 / 待退役 / 已梯次利用 / 已回收 | |
| 预计残值 | 96,000 元 | |

### 3.2 统一接入流程

```
① 数据进来（上传文件 / 采集器写入 / 导入日志）
      ↓
② 平台解析，抽出候选数据
      ↓
③ 【待确认预览区】 ← 关键环节，绝不跳过
   表格展示解析结果，可逐行改 / 删错行 / 补缺失 / 做列名映射
      ↓
④ 点击「确认入库」
      ↓
⑤ 写入本地数据库，记录来源 + 时间
```

> 实时采集数据量大，改为**批次确认**：每批展示统计摘要（条数、时间范围、功率区间、异常值个数），确认后整批入库。

**文件格式可靠性（说实话）**

| 格式 | 可靠性 | 注意 |
|---|---|---|
| Excel / CSV | 🟢 高 | 首选；自动识别 UTF-8 / GBK |
| Word (.docx) | 🟡 中 | 格式自由时易漏，**必须人工核对** |
| PDF（文本型） | 🟠 中低 | 跨页表格易错行，**必须人工核对** |
| PDF（扫描件） | 🔴 不支持 | 需 OCR，见[第 14 节](#14-已知限制与不做清单) |

---

### 3.3 三性分析总览

**这是平台区别于普通「能碳数据平台」的地方。** 三性各自回答一类问题，用不同方法，产出不同的图。

```
       差异性                时序性               不稳定性
    「谁跟谁不一样」      「什么时候变」        「变得稳不稳」
          │                    │                     │
          └────────┬───────────┴──────────┬──────────┘
                   ↓                      ↓
            找到问题组合            找到问题时段
                   └──────────┬───────────┘
                              ↓
                    Agent 综合归因 + 减碳建议
```

**举例**：差异性发现「机型B 跑长文本效率低」→ 时序性发现「这类任务集中在夜间高碳时段」→ 不稳定性发现「功率尖峰频繁，推高需量电费」→ 三条线索指向同一建议：**把长文本任务从机型B 迁到机型A，并错峰到光伏充足时段**。

### 3.4 差异性 —— 「为什么这批任务比那批费电」

**核心问题**：同样产出 100 万 Token，不同的「模型/任务 × 服务器机型」组合，能耗差多少？

#### 分析矩阵（主产出，直接回答问题一）

```
              机型A(A100)   机型B(H100)   机型C(国产卡)
模型甲·短文本    2,150         3,400          1,780     ← 单位：Tokens/kWh
模型甲·长文本    1,240         2,980            890
模型乙·批量推理  2,600         4,100          2,050
模型丙·微调        —             —              —
```

每格填**能效（Tokens/kWh）**，可切换填充：单位 Token 电耗、平均功率、卡利用率、每百万 Token 碳足迹。

#### 计算方法

| 指标 | 公式 | 说明 |
|---|---|---|
| 能效 | `Token数 ÷ IT电耗_kWh` | 越大越好 |
| 单位 Token 电耗 | `IT电耗_kWh ÷ (Token数 ÷ 1e6)` | kWh/MTok |
| 机型能效比 | `机型A能效 ÷ 机型B能效` | >1 说明 A 更优 |
| 组内变异系数 | `标准差 ÷ 均值` | 组合内部离散程度 |
| 离群标记 | 超出 `均值 ± 2×标准差` | 标出反常任务实例 |

> **能耗归因用 IT 电耗，不用总电耗。** PUE 是机房层面的属性，摊进机型对比会污染结论——那反映的是机房好坏，不是机型好坏。机房差异单独在 PUE 对比里看。

#### 输出

- 🔲 **模型/任务 × 机型 能效矩阵**（热力图）
- 📊 机型能效排名 + 相对差距条形图
- 📈 同任务在不同机型上的能效分布箱线图
- ⚠️ 低效组合清单
- 🤖 Agent 归因：为什么这个组合低效

### 3.5 时序性 —— 「什么时候费电，绿电来的时候我在用吗」

#### ① 周期识别

用滚动均值做简易分解（不引入重依赖）：

```
原始曲线 = 长期趋势 + 周期成分 + 残差
           ↑           ↑          ↑
      整体在往上走  日内/周内规律  随机波动
```

- **日内周期**：24 小时 × 日期 热力图，一眼看出每天什么时段最忙
- **周内周期**：工作日 vs 周末
- **长期趋势**：滚动 7 日 / 30 日均值

#### ② 自相关

计算不同滞后步长的自相关系数，验证周期是否真实存在：滞后 24 小时显著 → 有日周期；滞后 168 小时显著 → 有周周期。

#### ③ 相位差与错配分析（本节重点，直接服务问题二）

| 曲线对 | 看什么 | 错配意味着 |
|---|---|---|
| **负载 vs 光伏出力** | 相位差 | 光伏来了没人用 → 弃光；负载高峰只能用市电 → 碳排高 |
| **负载 vs 风电出力** | 相位差 | 同上 |
| **储能充放电 vs 碳强度** | 时段对齐 | 充在高碳时段、放在低碳时段 → **储能反而增碳** |
| **负载 vs 电价** | 相位差 | 高负载撞尖峰电价 → 电费高 |
| **IT 负载 vs 制冷电耗** | 滞后关系 | 制冷响应滞后多久，冷机策略是否合理 |

**相位差算法**：对两条归一化曲线做互相关，取相关系数最大处的滞后量。

#### 输出

- 🔥 24小时 × 日期 功率热力图
- 📈 时序分解三联图（原始 / 趋势 / 周期 / 残差）
- 📊 自相关图（ACF），标出显著周期
- 🔀 **多曲线叠加图**：负载 + 光伏 + 风电 + 储能SOC + 电价 + 碳强度，同一时间轴
- 🎯 **错配矩阵**：各曲线对的相位差和相关系数

### 3.6 不稳定性 —— 「负荷突然增高是正常的还是出事了」

直接回答问题一的第三小问。

| 类别 | 指标 | 算法 |
|---|---|---|
| **波动幅度** | 滚动标准差 | 窗口内标准差，默认 24 点 |
| | 变异系数 CV | `标准差 ÷ 均值`，无量纲可跨指标比较 |
| | 极差比 | `(最大值−最小值) ÷ 均值` |
| **突变** | 变点检测 | 滑动窗口比较前后段均值，差异超阈值则标记 |
| **尖峰** | 尖峰频次 | 超过 P95 分位数的次数 |
| | 尖峰持续时长 | 短尖峰和长高位性质不同 |
| **间歇性** | 风光间歇指数 | 出力为零的时段占比 |
| | 供给波动率 | 出力的 CV |

**稳定性评分**：给每个监测对象（机房/机型/服务）算综合评分，把上面指标归一化后加权。**权重可调，界面显示当前权重**，不做隐藏魔法数字。

> ⚠️ **平台只标记「统计上反常」，不判断「业务上是否有问题」。** 计划内压测也会被标为突变点。**是不是问题由你判断。**

#### 输出

- 📈 滚动波动率曲线（叠加原始曲线对照）
- 📍 **负荷突增事件清单**（时刻、幅度、持续时长、同期启动的任务 → 归因线索）
- 📊 各监测对象稳定性评分排名
- ⚡ 尖峰事件清单（是否触发新的月度最高需量）
- 🌤 风光出力间歇性图（出力曲线 + 零出力时段阴影）

---

## 4. 模块二 · 高能耗时段、空置率与风光储有效性

本模块产出**问题一和问题二的具体结论清单**。

### 4.1 高能耗时段识别

**什么叫高能耗时段**：一段**持续的、显著高于基线**的区间。三要素缺一不可——持续时长、超出幅度、所在环节。

**识别算法（本地确定性计算，不依赖 Agent）**

1. **算基线**：所选窗口内功率的中位数（或手工指定）
2. **标记**：连续超过 `基线 × 阈值系数`（默认 1.2）且持续超过最短时长（默认 30 分钟）的区间
3. **合并**：间隔小于 10 分钟的相邻区间合并为一段
4. **分级**：按超出幅度分为「关注 / 重要 / 严重」三档

> 所有阈值在界面上**可调且显示当前取值**，不做隐藏的魔法数字。

**五维归因**（识别出时段后，拆解「费在哪」）

| 维度 | 回答什么 | 输出 |
|---|---|---|
| **按环节** | IT 在跑，还是制冷在费？ | IT / 制冷 / 供配电 / 其他 的构成变化 |
| **按模型** ⭐ | **哪些模型在这段时间跑？** | 模型能耗排名 + Token 产出（直接答问题一） |
| **按任务** | 哪些任务撞在这段时间？ | 任务清单 + 卡时占比 |
| **按机型** | 哪些机型贡献最大？ | 机型能耗排名（关联[差异性分析](#34-差异性--为什么这批任务比那批费电)） |
| **按设备** | 哪些节点/卡功率反常？ | 设备功率排名、离群标记 |

### 4.2 空置率与空载能耗（v4.0 新增）

**为什么单独做**：利用率高不代表没浪费。一个机房可能「在跑的卡都跑满了，但一半的卡根本没分到任务」——这种浪费利用率指标看不出来，**空置率才看得出来**。

#### 两种空置

| 类型 | 定义 | 说明 |
|---|---|---|
| **物理空置率** | `(可容纳机位 − 已装机位) ÷ 可容纳机位` | 机房建了但没装满，属于资本闲置 |
| **逻辑空置率** | `(装机卡数 − 被任务占用卡数) ÷ 装机卡数` | **装了但没跑任务，这部分仍在耗待机电** |

#### 空载能耗（真正的浪费）

```
空载能耗(t) = 空置卡数(t) × 单卡待机功率 × 时长
空载能耗占比 = 空载能耗 ÷ IT电耗 × 100%
空载碳排     = 空载能耗 × PUE × 该时段碳强度
```

> **单卡待机功率**来自[服务器机型台账](#d--任务与机型台账)的 `idle_power_w` 字段，必须按实测填写。GPU 空闲功率通常是额定功率的 5–15%，但差异很大，**不要用经验值代替实测**。

#### 输出

- 📉 **空置率日内曲线**（24 小时 × 日期热力图，看什么时段最闲）
- 📊 空载能耗占比卡片 + 趋势
- 💸 **空载成本与碳排卡片**：这段时间白烧了多少电、多少钱、多少碳
- 📋 长期空置设备清单（连续 N 天空置率 > 阈值的节点）
- 🔀 **空置率 vs 能耗 双轴图**：如果空置率高但能耗不降，说明待机功耗偏高

### 4.3 负荷突增识别

复用[不稳定性分析](#36-不稳定性--负荷突然增高是正常的还是出事了)的变点检测和尖峰识别，但在这里**做因果线索关联**：

```
检测到负荷突增事件（时刻 t，幅度 +42%，持续 25 分钟）
        ↓
自动关联同一时间窗内：
  · 新提交/启动的任务（job_records）
  · 该时段活跃的模型（token_records）
  · 制冷电耗是否同步上升（区分 IT 驱动 vs 制冷驱动）
  · 是否触发月度最高需量（关联需量电费）
        ↓
输出「突增事件 + 疑似触发源」清单
```

> ⚠️ **平台只做时间对齐，不做因果推断。** 时间上撞在一起不等于因果关系。**是不是真凶由你判断。**

### 4.4 风光储有效性评估（v4.0 新增，问题二的核心）

**「是否真正发挥了作用」不能看装机量，要看三件事**：发出来了吗、用上了吗、真减碳了吗。

#### 三个层次的评估

**① 供给侧：发出来了吗**

| 指标 | 公式 |
|---|---|
| 出力系数 | `实际发电量 ÷ 理论可发量` |
| 等效利用小时 | `发电量 ÷ 装机容量` |
| 间歇指数 | `出力为零的时段数 ÷ 总时段数` |

**② 消纳侧：用上了吗**

| 指标 | 公式 |
|---|---|
| **消纳率** | `实际消纳量 ÷ 供给量` |
| **弃电量** | `供给量 − 消纳量` |
| 分时绿电比例 | `绿电消纳量(t) ÷ 总用电量(t)` |
| 总量绿电比例 | `绿电消纳量 ÷ 总用电量` |

**③ 减碳侧：真减碳了吗（反事实基线）**

```
减碳量 = 绿电消纳量 × (市电碳强度 − 绿电碳强度)
       （绿电碳强度通常取 0）
```

> **关键**：减碳量要用**同时段的市电碳强度**，不能用月均值。绿电如果都消纳在本来就低碳的时段，减碳效果会被高估。

#### 储能有效性（最容易被想当然的部分）

储能不发电，它只是**搬运电力**，而且搬运过程有损耗。所以它**可能减碳，也可能增碳**。

```
【往返效率】
  η = 放电量 ÷ 充电量        典型 0.85 ~ 0.92

【要放出 E_out 度电，必须先充进】
  E_in = E_out ÷ η           效率损失的那部分电，白烧了

【储能净减碳】
  净减碳 = E_out × ( EF_放电时段 − EF_充电时段 ÷ η )

  · 结果 > 0 → 储能真的在减碳 ✅
  · 结果 < 0 → 储能在增碳 ⚠️（充电时段不够干净，或效率损失吃掉了收益）
```

**一个关键分支**：如果充电用的是**本来会被弃掉的绿电**，那么 `EF_充电时段 = 0`，净减碳 = `E_out × EF_放电时段`，**全额为正**。所以：

> **储能的价值高度依赖「充电时用的是什么电」。** 平台必须记录 `充电来源`（光伏 / 市电 / 混合）字段，否则这个测算做不了——这就是[数据接入 C](#c--风光储数据v40-扩展)把它列为必填的原因。

#### 有效性结论（平台自动分级）

| 结论 | 判定条件 |
|---|---|
| ✅ **充分发挥** | 消纳率 > 90% 且减碳量为正且显著 |
| 🟡 **部分发挥** | 消纳率 60–90%，或存在明显时段错配 |
| ⚠️ **未有效发挥** | 消纳率 < 60%，或弃电量大，或储能净减碳 ≤ 0 |

> 阈值可在设置里调，界面显示当前取值。

#### 输出

- 🌤 **风/光/储 三源出力曲线 + 负载曲线**叠加图，弃电时段和高碳时段着色
- 📊 三种电源各自的：供给、消纳、弃电、消纳率、减碳量对照表
- 🔋 **储能充放电时段碳强度对比图**（充电时段 EF vs 放电时段 EF，一眼看出搬得对不对）
- 🎯 **储能净减碳卡片**（为负时标红并说明原因）
- 📊 分时绿电利用比例柱状图（24 小时分布）
- 📋 弃电时段清单 + 高碳时段清单
- 🎯 **理论可迁移量测算**：把 X 度可延迟负载从高碳时段迁到弃电时段，能减多少碳
- 🏷 三源有效性结论卡片（充分/部分/未有效发挥 + 原因）

> ⚠️ **平台只算「理论可迁移量」，不判断任务能不能迁。** 任务有没有时效要求、能不能中断，只有你知道。这符合「不做实时调度」的边界。

---

## 5. 模块三 · 百万 Token 碳足迹与服务器生命周期

本模块回答**问题三**：不同时段碳足迹差多少，以及使用频率如何影响服务器的寿命、成本和回收。

### 5.1 碳足迹计算链路

```
       Token 产出量                    电耗
      (tokens_in + tokens_out)      E_IT (IT 电耗)
              │                          │
              │                    E_total = E_IT × PUE
              │                          │
              │                   ┌──────┴──────┐
              │                   ↓             ↓
              │             运营碳排        隐含碳
              │            (地域法/市场法)  (按使用强度摊销 ⭐v4.0)
              │                   └──────┬──────┘
              │                          ↓
              │                    总碳足迹 C_total
              └──────────┬───────────────┘
                         ↓
        每百万 Token 碳足迹 = C_total ÷ (Token数 ÷ 1,000,000)
                         ↓
                   单位：gCO₂e / MTok
```

### 5.2 核心公式

**① 每百万 Token 碳足迹（顶层指标）**
```
gCO2e/MTok = C_total_gCO2e ÷ (Tokens ÷ 1,000,000)
```

**② 运营碳排 · 地域法**
```
C_location = E_total × EF_grid
```

**③ 运营碳排 · 市场法**
```
C_market = (E_total − E_绿电 − E_绿证抵扣) × EF_grid + E_绿电 × EF_green
```
> EF_green 通常取 0。**两种算法必须同时输出**，报告中注明依据。

**④ 隐含碳 —— 按使用强度摊销（v4.0 改进）**
```
C_embodied(本期) = 设备隐含碳_gCO2e × (本期等效运行小时 ÷ 设计寿命等效小时)
```
> v3.0 用的是「日历时长 ÷ 设计寿命」，无法体现使用频率的影响。v4.0 改为等效运行小时，详见 [5.4](#54-使用强度与寿命折损v40-新增)。

### 5.3 分时段碳足迹（问题三第一小问）

**同样产 100 万 Token，在不同时段排的碳可以差一倍以上。** 原因：电网碳强度随时段变化，绿电供给也随时段变化。

```
每百万Token碳足迹(t) = [ E_total(t) × EF(t) + C_embodied(t) ] ÷ ( Tokens(t) ÷ 1e6 )
```

**输出**：24 小时分时段的 gCO₂e/MTok 曲线，标出峰谷值和**峰谷差倍数**。

> **这条曲线是「任务错峰」建议的量化依据**——如果谷段是 31 gCO₂e/MTok、峰段是 48，那把可延迟任务迁到谷段，每百万 Token 就能少排 17 克。

### 5.4 使用强度与寿命折损（v4.0 新增）

**核心命题**：跑得狠的机器老得快。同一台服务器，满载跑一年和空转跑一年，对寿命的消耗不一样，隐含碳的摊销速度也不该一样。

#### 使用强度

```
使用强度(t) = 实际运行卡时(t) ÷ 额定可运行卡时(t)
额定可运行卡时(t) = 装机卡数 × 时段小时数

空置率(t) = 1 − 使用强度(t)
```

#### 等效运行小时（寿命折损模型）

```
等效运行小时 = Σ ( 实际运行小时 × 强度系数 k )

  k 按负载率分档（默认值，可在设置里调）：
    负载率 < 30%      k = 0.6    （轻载，老化慢）
    负载率 30% ~ 70%  k = 1.0    （常规，基准）
    负载率 > 70%      k = 1.3    （重载，老化快）

设计寿命等效小时 = 设计寿命年数 × 365 × 24
等效寿命消耗率   = 累计等效运行小时 ÷ 设计寿命等效小时
```

> 🔴 **这是一个工程简化模型，不是可靠性物理模型。**
> 真实老化受温度循环、功率循环、器件批次、散热条件影响，远比负载率复杂。
> 平台**不预测设备何时失效**，只提供一个**可横向比较的相对指标**——哪台机器被用得更狠、寿命消耗更快。
> **分档系数必须在界面显示且可调**，不做隐藏假设。这符合[第 1.5 节](#15-它不解决什么问题先说清楚)「不做设备可靠性预测」的边界。

#### 使用频率如何影响隐含碳（关键洞察）

| 使用模式 | 等效寿命消耗 | 隐含碳摊销速度 | 每 Token 分摊的隐含碳 |
|---|---|---|---|
| **高频满载** | 快 | 快 | **低**（产出多，摊得薄） |
| **中频常规** | 中 | 中 | 中 |
| **低频空转** | 慢 | 慢 | **高**（产出少，摊得厚） |
| **完全空置** | 极慢 | 极慢 | **∞**（没有产出，全是浪费） |

> **这里有个反直觉的结论**：从**单位产品碳足迹**角度看，把机器用满反而更划算——因为隐含碳是沉没的，产出越多摊得越薄。
> 但从**设备寿命和更换频率**角度看，用得狠意味着更早更换，更早产生**新一批隐含碳**。
> **两者的平衡点，正是本模块要帮你算清楚的东西。**

### 5.5 生命周期成本与回收再利用（v4.0 新增）

#### 生命周期成本（LCC）

```
LCC = 采购成本 + Σ运行电费 + Σ运维成本 − 残值

残值 ≈ 采购成本 × (1 − 等效寿命消耗率) × 折价系数 γ
      γ 默认 0.5，可调（二手算力设备折价率差异很大）

每百万Token的LCC = 本期LCC分摊 ÷ (本期Tokens ÷ 1e6)     单位：元/MTok
```

> 这个指标和每百万 Token 碳足迹用**同一个功能单位**，可以并排看——**哪台机器又贵又脏，一目了然**。

#### 回收与重复利用

```
【可再利用潜力】
  = 1 − 等效寿命消耗率
  剩余越多，越值得梯次利用而非直接报废

【回收可抵扣的隐含碳】
  = 设备隐含碳 × 回收再利用率 r × 材料抵扣系数 m
  r 和 m 必须按你实际回收商的数据填写，平台不内置
```

#### 退役决策支持（三选一对照）

设备到了寿命后期，平台给出三条路径的**碳与成本对照**，供你决策：

| 路径 | 碳影响 | 成本影响 | 适用条件 |
|---|---|---|---|
| **继续在当前场景用** | 无新增隐含碳，但运营碳排可能因能效落后而偏高 | 无采购支出，电费可能偏高 | 能效仍可接受、剩余寿命充足 |
| **梯次利用**（转低要求场景） | 无新增隐含碳，延长隐含碳摊销周期 | 无采购支出 | 可再利用潜力 > 阈值（默认 30%） |
| **退役回收 + 换新** | 抵扣部分旧设备隐含碳，但产生新设备隐含碳 | 采购支出，但电费下降 | 新旧机型能效差距 > 阈值 |

> ⚠️ **平台只提供对照数据，不替你做决策。** 业务连续性、备件供应、机房改造成本这些因素平台不知道。

#### 输出

- 📈 **分时段每百万 Token 碳足迹曲线**（峰谷差倍数）
- 🎯 **每百万 Token 碳足迹主卡片**（地域法 / 市场法双数值 + 完整口径标注）
- 📊 各机型：使用强度 · 等效寿命消耗率 · 隐含碳摊销速率 对照表
- 💰 **每百万 Token 碳足迹 vs 每百万 Token 成本 双维散点图**（按机型着色）
- 🔧 设备生命周期看板（役龄、等效消耗率、剩余潜力、预计残值、状态）
- 🔁 退役决策三路径对照表
- 🥧 构成图：按 Scope / 按环节 / 运营碳 vs 隐含碳
- 🔍 **明细可下钻**：每条结果展开看「哪些活动数据 × 哪个因子（哪版）= 多少」
- 💾 导出 Excel + 图表 PNG

### 5.6 口径标注（硬性要求）

每百万 Token 碳足迹这个数，**必须连同下列口径一起展示**，否则数字没有意义、无法比较：

| 必须标注 | 可选值 |
|---|---|
| Token 统计口径 | 输入+输出 / 仅输出 |
| 碳算法 | 地域法 / 市场法（**两个都给**） |
| 是否含隐含碳 | 含 / 不含 |
| **隐含碳摊销方式** ⭐ | 按等效运行小时 / 按日历时长 |
| 电耗边界 | 仅 IT / 含 PUE（默认含 PUE） |
| 因子版本 | 如 v1.2 |
| 统计周期 | 如 2026-07 |

> **例**：`42.3 gCO₂e/MTok（地域法，输入+输出，含PUE，含隐含碳按等效运行小时摊销，因子v1.2，2026年7月）`
>
> 没有这一串后缀的数字，不要拿出去用。

### 5.7 Scope 归集

| Scope | 算力园区包含什么 |
|---|---|
| Scope 1 | 柴油发电机、备用锅炉（通常很小） |
| Scope 2 | **外购电力**（绝对大头） |
| Scope 3 | 硬件隐含碳、上游燃料、外购蒸汽/冷、**设备回收处置** |

---

## 6. 模块四 · 能源成本与碳资产

### 6.1 能源成本

```
总电费 = 电量电费 + 需量电费 + 其他费用
        ↑              ↑
  Σ(各时段电量 × 该时段电价)   按月度最高需量计收
```

**分时电价表**（可编辑，见[第 9 节](#9-基础表管理)）

| 时段类型 | 典型时间 | 特点 |
|---|---|---|
| 尖峰 | 夏季午后、晚高峰 | 最贵 |
| 高峰 | 白天 | 贵 |
| 平段 | 上午、傍晚 | 中 |
| 低谷 | 夜间 | 最便宜，**通常碳强度也更低** |

**成本-碳双维分析**：把同一时间轴上的「度电成本」和「度电碳强度」并排看，找出**又贵又脏**的时段。

| 时段 | 电价 | 碳强度 | 结论 |
|---|---|---|---|
| 尖峰 | 高 | 高 | ⚠️ 优先转移 |
| 低谷 | 低 | 低 | ✅ 优先安排 |
| 光伏高峰 | 中 | 极低 | ✅ 最优先安排 |

**折算到产品口径**：`每百万 Token 电费 = 总电费 ÷ (Tokens ÷ 1e6)`，与碳足迹同一功能单位，方便并排看。

### 6.2 碳资产台账

**只做台账和测算，不做交易**（见 [1.5](#15-它不解决什么问题先说清楚)）。

| 资产类型 | 记录什么 |
|---|---|
| 碳配额（CEA） | 免费分配量、有偿购买量、当前持仓、已履约量 |
| 绿电 | 采购电量、对应时段、合同编号 |
| 绿证（GEC） | 证书数量、对应电量、购买时间 |
| CCER | 持有量、可抵销比例上限 |

```
履约缺口 = 核算排放量 − 免费配额 − 已购配额 − 绿电抵扣 − CCER抵销
```

> - **> 0**：缺口，需补足 → 界面标红
> - **< 0**：盈余
> - CCER 抵销通常有比例上限（如不超过应履约量的 5%），平台按设置里填的上限自动截断并提示

### 6.3 输出

- 💰 电费总额卡片 + 分时段构成图
- ⚡ 需量曲线 + 月度最高需量标记（关联[尖峰事件](#36-不稳定性--负荷突然增高是正常的还是出事了)）
- 🔀 成本-碳双维散点图（横轴成本、纵轴碳强度，按时段着色）
- 🎯 每百万 Token 成本 + 每百万 Token 碳足迹 对照卡片
- 📒 碳资产台账表 + 🚨 履约缺口卡片（缺口标红 + 来源拆解）
- 💾 导出 Excel + 图表 PNG

---

## 7. 模块五 · 低碳算力工作流

前面模块是能力，这一节把它们串成**每月照着跑的固定动作**。这是平台的最终产出物。

### 7.1 工作流闭环

```
      ┌─────────────────────────────────────────────┐
      ↓                                             │
  ① 采集   → 能耗 / Token / 风光储 / 任务机型 / 生命周期
      ↓                                             │
  ② 分析   → 三性分析（差异性·时序性·不稳定性）        │
      ↓                                             │
  ③ 识别   → 高能耗时段 + 空置率 + 风光储有效性        │
      ↓                                             │
  ④ 测算   → 分时段Token碳足迹 + 成本 + 寿命 + 碳资产  │
      ↓                                             │
  ⑤ 建议   → Agent 给减碳措施 + 预期收益              │
      ↓                                             │
  ⑥ 执行   → 你和运维去落地（平台不自动执行）          │
      ↓                                             │
  ⑦ 复核   → 下周期同口径回测，措施到底有没有效 ───────┘
```

**⑦ 复核是闭环的关键**，也是最容易被略过的一步。平台把每条采纳的措施记录下来，下周期自动对比措施前后的**同口径**数据，给出「实际减碳量 vs 预期减碳量」。

> **同口径**指：相同的 Token 统计口径、相同的因子版本、相同的电耗边界、相同的隐含碳摊销方式。口径不一致的对比没有意义，平台会明确警告并拒绝对比。

### 7.2 固化形式

- **模板**：内置「月度能碳复盘」「季度履约盘点」「年度设备退役评估」三个模板
- **进度**：清单形式呈现，做完一步勾一步，中断了下次接着做
- **留痕**：每次跑完存成一个「工作流实例」，含数据快照、因子版本、口径、Agent 结论、你的批注
- **复用**：下月点「新建实例」，自动带上上次的参数配置

### 7.3 措施台账

| 字段 | 说明 |
|---|---|
| 措施描述 | 如「将长文本推理从机型B 迁至机型A，并错峰至光伏高峰时段」 |
| 关联发现 | 来自哪次分析的哪条结论（可追溯） |
| 关联问题 | 问题一 / 问题二 / 问题三 |
| 提出来源 | Agent 建议 / 人工提出 |
| 预期收益 | 预计减碳量、降本金额、每百万Token碳足迹改善 |
| 状态 | 待评估 / 已采纳 / 已执行 / 已复核 / 已否决 |
| 执行时间 | |
| 实际收益 | 复核时由平台按同口径回测填入 |

---

## 8. Agent 架构（主 Agent + 三个 SubAgent）

**先读完 [1.6 核心设计原则](#16-核心设计原则)的数据边界部分再看本节。**

> **v5.0 变化**：v4.0 是**单个 Agent + 15 个工具**，按三个核心问题组织调用。
> v5.0 改为**主 Agent 编排 + 三个专职 SubAgent**，并新增三项 v4.0 没有的能力：
> **① 预测**（原来只做事后测算）、**② 电网级风光趋势**（原来只有园区自建风光）、
> **③ 跨数据库因子对齐**（原来只有单张因子表）。

### 8.1 总体架构

```
                   ┌──────────────────────────────────┐
   你的问题  ────→ │            主 Agent               │ ────→ 结论与报告
                   │   分解 · 调度 · 汇总 · 冲突仲裁    │
                   └────────────────┬─────────────────┘
              ┌─────────────────────┼─────────────────────┐
              ↓                     ↓                     ↓
     ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
     │   SubAgent 1    │→ │   SubAgent 2    │→ │   SubAgent 3    │
     │  任务需求与      │  │  风光趋势匹配与  │  │  因子对齐与      │
     │  能碳预测        │  │  低碳识别        │  │  碳核算报告      │
     │                 │  │                 │  │                 │
     │ 要跑什么任务？   │  │ 什么时候跑最绿？ │  │ 到底排了多少碳？ │
     │ 要耗多少电和碳？ │  │ 风光对得上吗？   │  │ 用谁家的因子算？ │
     └────────┬────────┘  └────────┬────────┘  └────────┬────────┘
              └────────────────────┼────────────────────┘
              ┄┄┄┄┄┄┄┄┄┄┄┄ 数据边界 ┄┄┄┄┄┄┄┄┄┄┄┄  ← 云端 API 到此为止
                                   ↓
                    本地工具层（只读 · 只返聚合值 · 不写库）
                                   ↓
                          本地 SQLite (data/app.db)
```

**三个 SubAgent 的分工，用一句话区分**：

| SubAgent | 时态 | 回答 |
|---|---|---|
| **1 · 能碳预测** | **未来** | 要跑的任务将消耗多少电、排多少碳 |
| **2 · 低碳识别** | **未来 × 时段** | 这些任务放在什么时候跑最绿 |
| **3 · 碳核算** | **过去** | 已经发生的到底排了多少，依据是谁家的因子 |

> **1 和 2 面向未来（预测与决策），3 面向过去（核算与交代）。** 这个时态区分是三者不重叠的根本原因。

---

### 8.2 主 Agent（编排层）

> **贯穿全部四个 Agent 的一句话原则**：
> ### 数字由代码算，话由 Agent 说。
> 碳足迹、能耗、分摊结果一律由 `core/` 的确定性算法算死，同样输入永远同样输出；
> Agent 只负责解读、归因、建议和声明假设。**任何 Agent 都不得自己算数字。**

#### 职责

| 职责 | 说明 |
|---|---|
| **问题分解** | 把你的自然语言问题拆成子任务，判断需要哪几个 SubAgent |
| **调度** | 决定串行还是并行（见 [8.6](#86-调用时序)） |
| **结果汇总** | 把各 SubAgent 的结论组织成一份连贯的回答 |
| **冲突仲裁** ⭐ | SubAgent 之间、或 SubAgent 与确定性计算之间出现矛盾时的裁决 |
| **报告组织** | 生成结构化报告初稿 |

#### 主 Agent **不做**什么

- ❌ 不直接算数 —— 所有数字由本地测算层算，主 Agent 只转述
- ❌ 不直接读数据库 —— 只能通过 SubAgent
- ❌ 不改任何数据
- ❌ 不做调度执行 —— 只给建议

#### 冲突仲裁规则（硬性）

1. **确定性计算 > SubAgent 结论**。数字与 SubAgent 的话冲突时，以数字为准，并明确提示存在出入
2. **SubAgent 之间冲突 → 呈现分歧，不强行统一**。例如 SubAgent 2 说「该时段最绿」而 SubAgent 3 的历史核算显示该时段碳强度并不低，主 Agent 必须把两边都摆出来并说明可能原因（如电网因子版本不同），**不许挑一个说**
3. **数据不足 → 明确说「答不了」**，并指出缺哪类数据
4. **预测与实测冲突 → 以实测为准**，并把偏差作为下一轮预测的校准依据

---

### 8.3 SubAgent 1 · 任务需求与能碳预测

> ⭐ **v5.0 新增能力：预测。** v4.0 只做事后测算，回答「已经排了多少」；本 SubAgent 回答「将要排多少」。

#### 输入

| 数据 | 来源表 | 用途 |
|---|---|---|
| 待执行/在跑任务 | `job_records`（含待执行状态） | 识别任务需求 |
| 历史任务与能耗 | `job_records` + `energy_records` | 建立能效基线 |
| Token 计量 | `token_records` | 单位产出能耗基线 |
| 机型规格 | `server_specs` | 额定功率、待机功率 |
| 园区结构 | `park_structure` | 装机、PUE |

#### 能力

**① 任务需求检测**

识别当前在跑与待执行的算力任务：任务类型（训练/微调/推理）、规模（卡数×预计时长）、目标机型、可延迟性标记。

**② 能耗预测**

```
预测能耗_IT = Σ [ 任务预计卡时 × 该「任务类型 × 机型」的历史单位卡时能耗基线 ]

  基线 = 历史同类组合的【中位数】
        （用中位数不用均值——算力任务功率分布长尾严重，均值会被少数极端任务拉偏）

预测总能耗 = 预测能耗_IT × PUE
```

**③ 碳排放预测**

```
预测碳排 = 预测总能耗 × 预测时段碳强度 EF(t)
         （EF(t) 由 SubAgent 2 提供的电网风光趋势推算，见 8.4）
```

**④ 不确定性区间**

```
区间下界 = Σ [ 卡时 × 历史同类组合能耗的 P25 ]
区间上界 = Σ [ 卡时 × 历史同类组合能耗的 P75 ]
```

#### 硬性约束

1. **必须给区间，不许只给点估计**。单一数字会让人误以为预测是确定的
2. **必须标注基于多少历史样本**。样本数低于阈值（默认 10）时标注「样本不足，预测不可靠」
3. **不做黑箱模型**。基线外推可解释、可复现；不引入需要训练的预测模型（那会违反[依赖精简](#162-开发原则)且无法解释）
4. **预测结果必须存档**（`forecast_records`），下期与实测对比做偏差校准

#### 输出

- 📋 任务需求清单（类型、规模、机型、可延迟性）
- 📊 **能耗预测表**（点估计 + P25–P75 区间 + 样本数）
- 🌱 **碳排预测表**（同上，含所用碳强度来源）
- ⚠️ 低置信预测告警清单

---

### 8.4 SubAgent 2 · 风光趋势匹配与低碳识别

> ⭐ **v5.0 新增：电网级风光出力趋势。** v4.0 只有园区自建风光。

#### 两个层级必须分开（关键概念）

| 层级 | 数据内容 | 影响什么 | 对应算法 |
|---|---|---|---|
| **园区自建** | 自有光伏/风机的出力与消纳 | **你用的电有多绿** | 市场法（market-based）抵扣 |
| **电网级** ⭐ | 当地电网风电/光伏发电占比与趋势 | **你买的电有多绿** | 地域法（location-based）碳强度 |

> **为什么必须分开**：园区自建绿电是你自己发的，直接抵扣；电网风光占比是整个电网的电源结构，它决定**你从电网买的每一度电的碳强度**。
> 就算园区一块光伏板都没有，只要在电网风光大发的时段用电，碳排也更低——**这正是"低碳识别"的意义**。
> 两者数据源不同、作用机制不同、进的公式也不同，混在一起会算错。

#### 输入

| 数据 | 来源 |
|---|---|
| 能耗预测曲线 | **SubAgent 1 的输出** |
| 电网风电/光伏出力（实际与预测） | 电网公司发布数据 / 第三方数据服务 → `renewable_records`（`scope_level = grid`） |
| 园区自建风光出力 | 自有采集 → `renewable_records`（`scope_level = park`） |
| 分时碳强度 | 电网发布，或由风光占比近似推算 |
| 分时电价 | `tariff_table` |
| 储能充放电 | `storage_records` |

#### 能力

**① 曲线对齐与错配分析**

把预测能耗曲线与电网风光出力曲线放在同一时间轴，计算相位差与相关系数（方法见 [3.5](#35-时序性--什么时候费电绿电来的时候我在用吗)）。

**② 低碳窗口识别**

```
低碳窗口(t) 判据（两条同时满足）：
  电网风光出力占比(t) > 占比阈值（默认 P70 分位数）
  且 碳强度 EF(t) < 碳强度阈值（默认当日 P30 分位数）

高碳窗口(t) 判据：
  碳强度 EF(t) > 当日 P70 分位数
```

> 阈值全部可调，界面显示当前取值。

**③ 任务—窗口匹配建议**

把 SubAgent 1 标为「可延迟」的任务，匹配到低碳窗口，测算减碳潜力：

```
减碳潜力 = 可迁移能耗 × ( EF_原时段 − EF_目标低碳窗口 )
```

**④ 风光储有效性评估**

沿用 [4.4](#44-风光储有效性评估v40-新增问题二的核心) 的三层评估与储能净减碳判据（**净减碳可能为负**）。

#### 硬性约束

1. **只给建议，不执行调度**。任务能不能迁、有没有时效要求，平台不知道
2. **电网碳强度是近似值时必须标注**。理想情况用电网发布的分时碳强度；拿不到而用「风光占比 → 碳强度」近似推算时，**结果上必须标注「近似值」**
3. **不假设风光一定有用**。消纳率、弃电、时段错配都要算出来

#### 输出

- 🔀 **预测能耗 + 电网风光出力 + 园区自建出力 + 碳强度 + 电价** 五曲线叠加图
- 📋 **低碳窗口清单**（起止、风光占比、碳强度、可容纳负荷）
- 📋 高碳窗口清单
- 🎯 **任务—窗口匹配建议表**（任务、原时段、建议时段、预计减碳量）
- 📊 相位差与错配矩阵
- 🏷 风/光/储三源有效性分级（充分/部分/未有效发挥）

---

### 8.5 SubAgent 3 · 排放因子对齐与碳核算报告

> ⭐ **v5.0 新增：跨数据库因子对齐。** v4.0 只有单张因子表，无法回答「换个库结果差多少」。

#### 要解决的问题

同一个活动——比如「华东电网用电 1 kWh」——在不同数据库里的因子值**并不一样**：

| 数据库 | 特点 | 典型适用场景 |
|---|---|---|
| 中国生命周期基础数据库（CLCD 类） | 本土化程度高 | 国内产品碳足迹 |
| ecoinvent | 覆盖最广，欧洲视角 | 国际对标、出口合规 |
| IPCC 缺省值 | 权威但粗，全球平均 | 缺乏本地数据时的兜底 |
| 主管部门发布的电网基准线排放因子 | 政策合规首选 | 履约核算、政府报送 |
| 企业自建库 | 最贴合自身实际 | 内部管理与改进 |

> 🔴 **这些库的取值可能相差数十个百分点。** 用哪个库，直接决定你报出去的碳足迹数字。

#### 能力

**① 跨库检索与匹配**

活动描述（如「外购电力，华东，2026年」）→ 在各库中检索候选因子。

**② 对齐与分歧呈现** ⭐ 本 SubAgent 的核心

对每个活动，列出各库的：取值、单位、**系统边界**、数据年份、**地域适用性**、来源文献。

```
活动：外购电力（华东电网，2026年，1 kWh）
┌──────────────────┬─────────┬──────┬────────┬──────────┐
│ 数据库            │ 取值     │ 单位  │ 年份    │ 地域适用  │
├──────────────────┼─────────┼──────┼────────┼──────────┤
│ 主管部门基准线    │ [待填]  │gCO2e │ [待填] │ 华东      │
│ CLCD 类           │ [待填]  │gCO2e │ [待填] │ 中国      │
│ ecoinvent         │ [待填]  │gCO2e │ [待填] │ 中国(近似)│
│ IPCC 缺省值       │ [待填]  │gCO2e │ [待填] │ 全球平均  │
└──────────────────┴─────────┴──────┴────────┴──────────┘
分歧幅度：最大值 ÷ 最小值 = [平台自动计算]
```

**③ 敏感性分析**

```
换库敏感性 = ( 用库A的结果 − 用库B的结果 ) ÷ 用库B的结果 × 100%
```

回答「如果换一个数据库，我的每百万 Token 碳足迹会变多少」。

**④ 因子选择建议**

给出建议 + **理由**（地域匹配度、年份新旧、边界一致性、合规要求），但**不替你决定**。

**⑤ 碳足迹测算与报告生成**

调用[第 5 节](#5-模块三--百万-token-碳足迹与服务器生命周期)的确定性测算，生成含完整口径标注的结果与报告初稿。

#### 匹配可靠性分级（硬性要求）

| 等级 | 含义 | 平台行为 |
|---|---|---|
| 🟢 **精确匹配** | 活动描述、边界、地域、年份全对得上 | 直接可用 |
| 🟡 **近似匹配** | 有一项对不上（如年份差 2 年、地域用全国值代区域值） | **必须标注差异项，需你确认** |
| 🔴 **无匹配** | 各库都没有对应因子 | **必须人工填写，不许用相近项凑** |

#### 硬性约束（最重要的一条）

> 🔴 **绝不自动选定一个因子然后隐藏分歧。**
> 必须把各库取值和差异摆出来，由你拍板。
> 报告中**必须写明最终用的是哪个库、哪个版本、为什么选它**——这是碳核算可审计性的底线。

#### 输出

- 📊 **跨库因子对照表**（含分歧幅度）
- 📈 **换库敏感性分析**（结果随因子库变化的幅度）
- 🏷 匹配可靠性分级清单
- 🎯 碳足迹测算结果（含[第 5.6 节](#56-口径标注硬性要求)的完整口径标注）
- 📄 **碳核算报告初稿**（结果、依据、因子溯源、不确定性、局限）
- 💾 导出 Excel + 报告

---

### 8.6 调用时序

#### 默认流水线（回答「我该怎么安排任务更低碳」）

```
你提问
  ↓
主 Agent 分解
  ↓
SubAgent 1  →  预测能耗与碳排（含区间）
  ↓             （输出作为 SubAgent 2 的输入）
SubAgent 2  →  匹配电网风光趋势，找低碳窗口，给迁移建议
  ↓
SubAgent 3  →  按选定因子库核算，出报告
  ↓
主 Agent 汇总 + 冲突仲裁
  ↓
结论 + 报告初稿
```

#### 并行分支（回答「上个月到底排了多少碳」）

这类**纯回溯**问题不需要预测，主 Agent 直接调 SubAgent 3，跳过 1 和 2。

#### 依赖关系（硬性）

| 关系 | 说明 |
|---|---|
| SubAgent 2 **依赖** SubAgent 1 | 没有能耗预测，无法做窗口匹配 |
| SubAgent 3 **不依赖** 1 和 2 | 可独立对历史数据做核算 |
| 三者**都依赖**本地测算层 | 关掉全部 Agent 后，测算与图表照常可用 |

---

### 8.7 工具集分配

**所有工具在本地执行**，SubAgent 只收到返回值，且返回的一律是**聚合结果**，不含原始明细。

#### 主 Agent 的工具

| 工具 | 作用 |
|---|---|
| `dispatch_subagent` | 调度指定 SubAgent 并取回结构化结果 |
| `compare_periods` | 同口径对比两个时段（**口径不一致时报错，不静默对比**） |
| `get_factors` | 查基础表数值、版本、来源 |

#### SubAgent 1 的工具

| 工具 | 作用 |
|---|---|
| `list_jobs` | 任务清单、类型分布、卡时占比（**任务名默认脱敏**） |
| `query_energy_summary` | 时段能耗汇总（总量、分项、PUE） |
| `analyze_variability` | 「模型/任务 × 机型」能效矩阵——**预测基线的来源** |
| `query_idle_rate` | 空置率、空载能耗 |
| `forecast_task_energy` ⭐ | **能耗与碳排预测（含 P25–P75 区间与样本数）** |

#### SubAgent 2 的工具

| 工具 | 作用 |
|---|---|
| `query_grid_renewable` ⭐ | **电网级风光出力与占比趋势** |
| `query_park_renewable` | 园区自建风光出力与消纳 |
| `analyze_temporality` | 周期、自相关、各曲线对相位差 |
| `match_lowcarbon_window` ⭐ | **低碳/高碳窗口识别与任务匹配建议** |
| `evaluate_power_sources` | 风/光供给、消纳、弃电、减碳量、有效性分级 |
| `analyze_storage` | 储能充放电碳强度、往返效率、**净减碳量（可能为负）** |
| `query_cost` | 分时电费、需量费、每百万 Token 成本 |

#### SubAgent 3 的工具

| 工具 | 作用 |
|---|---|
| `align_emission_factors` ⭐ | **跨库因子检索、对齐、分歧呈现、匹配分级** |
| `factor_sensitivity` ⭐ | **换库敏感性分析** |
| `calc_token_footprint` | 每百万 Token 碳足迹（可下钻）+ 完整口径标注 |
| `analyze_lifecycle` | 使用强度、等效寿命消耗率、LCC、残值、回收潜力 |
| `query_carbon_asset` | 配额、绿电、绿证、履约缺口 |
| `detect_high_load` | 高能耗时段清单 + 五维归因 |
| `analyze_instability` | 滚动波动率、突变点、尖峰事件 |
| `generate_carbon_report` ⭐ | **碳核算报告初稿生成** |

> **不提供的工具**：任何 Agent 都没有「读原始记录」「读设备序列号」「执行调度命令」「控制储能充放电」「改数据库」这类工具。**全部只读、只拿聚合值。**

---

### 8.8 数据边界的具体执行

**允许出网**：聚合统计量、时间范围、指标名、因子数值与库名、机型和模型的**脱敏代号**、你主动输入的问题。

**禁止出网**：逐条功耗明细、设备序列号 / IP / 主机名、机房物理位置、原始任务名和模型名（默认脱敏为 `job-001` / `机型A` / `模型甲`）、任何文件原文、Token 明细日志、合同编号。

**多 Agent 下的执行要点**：

1. **边界过滤在工具层，不在 Agent 层**。所有 SubAgent 共用同一个 `agent/boundary.py` 出口，**不允许任何 SubAgent 自带过滤逻辑**——多套过滤规则必然出现漏洞
2. **SubAgent 之间传递的数据同样受约束**。SubAgent 1 传给 2 的必须是聚合后的预测曲线，不是逐条任务明细
3. 每次调 API 前，界面弹出 **「本次将发送的内容」** 完整预览（标明是哪个 SubAgent 发起），你点确认才发
4. 可勾选「本会话内不再询问」，但**换一个会话就恢复询问**
5. 所有 API 交互写入 `data/agent_audit.log`，**必须记录是哪个 Agent 发起的**
6. 设置页全局开关：**纯本地模式**（关闭全部 Agent，其余功能保留）

---

### 8.9 技术实现要点

| 项 | 选择 | 说明 |
|---|---|---|
| SDK | `anthropic`（Python 官方 SDK） | 见[第 12 节](#12-技术方案与技术栈) |
| 模型 | `claude-opus-5` | 除非你明确要求换 |
| 循环方式 | Tool Runner（`client.beta.messages.tool_runner`） | 不手写 agent 循环，工具用 `@beta_tool` 定义 |
| **多 Agent 实现** | **每个 SubAgent 是一次独立的 Tool Runner 会话**，各自只挂载自己那组工具 | 工具隔离比提示词约束可靠——SubAgent 1 根本拿不到 SubAgent 3 的工具 |
| 主 Agent 调度 | 主 Agent 的 `dispatch_subagent` 工具内部启动子会话，返回结构化结果 | |
| 思考模式 | `thinking={"type": "adaptive"}` | 归因与仲裁需要推理 |
| 流式 | 长回答用 `.stream()` + `.get_final_message()` | 避免请求超时 |
| 密钥 | 环境变量 `ANTHROPIC_API_KEY` | **绝不硬编码，绝不提交进 Git** |

> **为什么用工具隔离而不是提示词约束**：如果四个 Agent 共用同一套工具、靠提示词告诉它们"你只该用这几个"，模型偶尔会越界。**把工具集在代码层分开挂载，越界就成了不可能而不是不应该。**

---

### 8.10 出错处理（不静默失败）

| 情况 | 平台行为 |
|---|---|
| 没配 API Key | 明确提示「未配置密钥，Agent 不可用，其余功能正常」，不崩溃 |
| 网络不通 / API 报错 | 显示具体错误和建议，**测算结果照常展示** |
| 某个 SubAgent 失败 | **其余 SubAgent 结果照常返回**，主 Agent 明确说明哪一环缺失、因此哪些结论无法给出 |
| SubAgent 2 拿不到电网风光数据 | 明确说「缺电网数据，低碳窗口识别不可用」，**不用园区自建数据冒充电网数据** |
| SubAgent 3 因子无匹配 | 标为 🔴，**要求人工填写，不许用相近项凑数** |
| Agent 结论与测算数字矛盾 | 以**测算数字为准**，提示「AI 结论与计算结果存在出入，请以计算结果为准」 |
| 预测与实测偏差过大 | 提示偏差幅度，建议重新校准基线 |
| 数据不足以回答某个问题 | **明确说「缺哪类数据所以答不了」**，不许编造 |
| 超出 token 预算 | 提示已用量，询问是否继续 |

---

### 8.10-bis 待办：对标国际标准的七处修改 ⚠️

对照 ISO 14067 / ISO 14064 / IPMVP / 24-7 CFE 等成熟工作流做过一次审计，发现**七处偏差，其中三处是硬伤**：

| 级别 | 缺什么 | 后果 |
|---|---|---|
| 🔴 P0 | **分配规则（Allocation）** | 多任务共享 GPU 的能耗无法归集；**训练碳未摊到推理 Token，核心指标是残缺的** |
| 🔴 P0 | **M&V 常规/非常规调整** | 措施回测会把业务量变化误判为措施成效 |
| 🔴 P0 | 工作流顺序 | 边界与口径应前置到第一步，现在是先采集后定口径 |
| 🟠 P1 | 功能单位未按 ISO 14067 规范定义 | 缺"功能"和"质量"两要素，跨模型比较会误导 |
| 🟠 P1 | QA/QC 与数据质量评级 | 无系统的数据质量评价 |
| 🟠 P1 | 不确定性评估 | ISO 要求至少定性评估，现在缺统一表述 |
| 🟡 P2 | Agent Eval | Agent 说错话没有发现机制 |

📎 **完整审计与行动清单见 [docs/成熟工作流对标审计.md](./docs/成熟工作流对标审计.md)**

> ⚠️ **这七处尚未修改到本文档中。** 修改前请先读审计报告，其中 P0-1 分配规则涉及
> 新增两个口径字段与表 6 结构变更，影响面较大。

---

### 8.11 Skill 部署（方法论层）

工具解决「**能做什么动作**」，Skill 解决「**怎么做才不出错**」。三个 SubAgent 各自需要装载的领域方法论包——
共用的碳核算口径公约、不确定性表达、证据接地，以及各 SubAgent 专属的负载基线、
**24/7 无碳电力小时级匹配（CFE Score）**、跨库因子对齐等——详见：

📎 **[docs/Agent技能部署方案.md](./docs/Agent技能部署方案.md)**

> ⭐ 其中 **S2-1 CFE 小时级匹配**值得优先看：SubAgent 2 要做的事业界已有标准方法论，
> 采纳它可把自定义指标升级为可对标、可引用的方法。

### 8.12 界面视觉区分（硬性要求）

| 内容类型 | 呈现方式 |
|---|---|
| 平台计算结果 | 常规样式，直接展示 |
| Agent 输出 | **🤖 图标 + 浅色底框 + 「AI 分析建议，请自行核实」标注** |
| **预测值** ⭐ | **必须与实测值视觉区分**（虚线/浅色/加"预测"角标），并显示区间与样本数 |
| **近似碳强度** ⭐ | 用风光占比推算而非电网实发数据时，**标注「近似值」** |
| **因子分歧** ⭐ | 跨库取值差异超阈值时**标黄提示**，不允许只显示选中的那一个 |

## 9. 基础表管理

五张可编辑的基础表，是所有测算的依据。

| 表 | 内容 | 关键字段 |
|---|---|---|
| **排放因子表（多库）** ⭐ | 电力、燃料的碳换算系数，**同一活动可并存多个库的取值** | 因子名、**source_db**、**system_boundary**、**region_scope**、单位、数值、年份 |
| **分时电价表** | 各时段电价和需量单价 | 时段类型、生效月份、时间区间、电价 |
| **服务器机型台账** | 异构机型规格（差异性与空置分析基准） | 机型、卡型、额定功率、**待机功率**、隐含碳、寿命 |
| **园区结构表** | 服务区 → 机房 → 机型的层级与装机 | park_id、room_id、**装机卡数**、**可容纳机位**、PUE |
| **设备生命周期台账** | 单台设备的役龄与消耗 | 设备ID、投产日期、采购成本、累计等效运行小时、状态 |

**通用能力**：查看 / 编辑 / 新增 / 删除、导入 Excel 覆盖、导出备份。

**版本快照（硬性要求）**：每次修改自动存一版。历史测算结果记录当时用的版本，**基础表改动不影响已有结果**。

**多库因子的管理（v5.0 新增）** ⭐

排放因子表允许**同一活动并存多个数据库的取值**（通过 `source_db` 区分），这是 SubAgent 3 做跨库对齐的前提。管理上有三条要求：

1. **不设"默认库"的全局开关**。每次测算时显式选择用哪个库，选择结果随 `factor_version` 存进 `carbon_records`
2. **导入新库时不覆盖旧库**，而是并列新增一组 `source_db` 记录
3. **界面必须能一屏看到同一活动在各库的取值差异**，而不是切换下拉框逐个看

> 电价、PUE、隐含碳、待机功率随地区和设备差异极大，**必须按实际情况填写**，内置值仅为示例。初始值见[附录 A](#附录-a--基础参数初始值)。

---

## 10. 数据模型

本地 SQLite（`data/app.db`）。**十六张表**。

### 10.1 活动数据表（5 张）

**表 1 · `energy_records`（能耗记录）**

| 字段 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` / `ts_end` | 时间 | 是 | 2026-07-03 14:00 | 统计区间 |
| `park_id` / `room_id` | 文本 | 否 | naxi-01 / room-A | 园区层级 |
| `segment` | 文本 | 是 | IT | IT / 制冷 / 供配电 / 其他 / 总计 |
| `server_model` / `device_id` | 文本 | 否 | H100-8卡 / node-12 | 关联机型与设备 |
| `energy_kwh` | 小数 | 是 | 420.5 | 电耗 |
| `avg_power_kw` | 小数 | 否 | 420.5 | 平均功率 |
| `source_type` / `source_file` / `imported_at` | | 是 | 文件 | 来源与时间 |

**表 2 · `token_records`（Token 计量）**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` / `ts_start` / `ts_end` | | 是 | |
| `model_id` | 文本 | 否 | **模型标识（可脱敏）——问题一按模型看能耗靠它** |
| `service_id` | 文本 | 否 | 服务标识 |
| `job_type` | 文本 | 是 | 推理 / 训练 / 微调 |
| `tokens_in` / `tokens_out` | 整数 | 否 | 输入 / 输出 Token |
| `tokens_train` | 整数 | 否 | 训练消耗 Token（训练任务用） |
| `server_model` / `node_list` | 文本 | 否 | 跑在哪 |
| `source_file` / `imported_at` | | 是 | |

**表 3 · `renewable_records`（风光出力与消纳）**

| 字段 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` / `ts_start` / `ts_end` | | 是 | | |
| **`scope_level`** ⭐ | 文本 | 是 | grid | **park（园区自建）/ grid（当地电网级）**——v5.0 新增，**两者绝不可混算** |
| `source_type` | 文本 | 是 | 光伏 | **光伏 / 风电 / 其他**（分开记，才能分别评估） |
| `theoretical_kwh` | 小数 | 否 | 200.0 | 理论可发量（park 用） |
| `supply_kwh` | 小数 | 是 | 180.0 | 实际供给量（park）/ 实发电量（grid） |
| `consumed_kwh` | 小数 | 否 | 145.0 | 实际消纳量（**仅 park 有意义**，grid 填空） |
| **`grid_share_pct`** ⭐ | 小数 | 否 | 34.2 | **该时段风光占电网总发电的比例（仅 grid）——低碳窗口判据的输入** |
| `park_id` / `contract_no` | 文本 | 否 | | |
| `source_file` / `imported_at` | | 是 | | |

> ⭐ **`scope_level` 是 v5.0 最关键的新增字段。**
> `park` 记录的是**你自己发了多少绿电**（进市场法抵扣）；`grid` 记录的是**当地电网风光占比**（决定你买的电的碳强度，进地域法）。
> 二者数据源不同、作用机制不同、进的公式也不同。**混在一张表里不加区分，SubAgent 2 的低碳识别必然算错。**

**表 4 · `storage_records`（储能充放电）** ⭐ v4.0 新增

| 字段 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` / `ts_start` / `ts_end` | | 是 | | |
| `charge_kwh` | 小数 | 是 | 120.0 | 该时段充电量 |
| `discharge_kwh` | 小数 | 是 | 0.0 | 该时段放电量 |
| `charge_source` | 文本 | 是 | 光伏 | **光伏 / 风电 / 市电 / 混合**——决定充电碳强度，**净减碳测算的关键** |
| `soc_start` / `soc_end` | 小数 | 否 | 0.32 / 0.68 | 荷电状态 |
| `rated_capacity_kwh` / `rated_power_kw` | 小数 | 否 | 500 / 250 | 额定参数 |
| `park_id` / `source_file` / `imported_at` | | | | |

**表 5 · `job_records`（任务记录）**

`id`、`job_id`、`job_name`（可脱敏）、`job_type`、`ts_start`/`ts_end`、`gpu_hours`、`cards_used`、`server_model`、`node_list`、`owner`、`source_file`、`imported_at`

> `cards_used` 是 v4.0 新增必填字段——**算空置率要用它**（装机卡数 − 被占用卡数）。

### 10.2 结果表（3 张）

**表 6 · `carbon_records`（碳排放记录）**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` / `ts_start` / `ts_end` | | 是 | |
| `scope` / `category` | 文本 | 是 | Scope 1/2/3；运营碳排 / 隐含碳 |
| `emission_gco2e` | 小数 | 是 | **统一存 gCO₂e** |
| `method` | 文本 | 是 | location / market |
| `tokens_basis` | 整数 | 否 | 对应 Token 数 |
| `gco2e_per_mtok` | 小数 | 否 | **每百万Token碳足迹** |
| `token_scope` | 文本 | 否 | in+out / out_only |
| `include_embodied` | 布尔 | 是 | 是否含隐含碳 |
| `embodied_method` ⭐ | 文本 | 否 | **equiv_hours / calendar**（隐含碳摊销方式） |
| `energy_boundary` | 文本 | 是 | it_only / with_pue |
| `factor_id` / `factor_version` | | 是 | 用了哪版因子 |
| `job_id` / `model_id` / `server_model` | 文本 | 否 | 归属 |
| `calc_basis` | 文本 | 是 | 计算依据（下钻用） |
| `calculated_at` | 时间 | 是 | |

> `token_scope` / `include_embodied` / `embodied_method` / `energy_boundary` / `factor_version` 五个字段就是[第 5.6 节](#56-口径标注硬性要求)要求的口径标注，**必须随结果一起存**，否则数字无法比较。

**表 7 · `analysis_results`（三性与专项分析结果）**

| 字段 | 说明 |
|---|---|
| `id` | |
| `analysis_type` | variability / temporality / instability / idle / power_source / storage / lifecycle |
| `ts_start` / `ts_end` | 分析覆盖周期 |
| `dimension` | 分析维度，如 `model_id×server_model` |
| `metric_name` | 如 `tokens_per_kwh`、`idle_rate`、`storage_net_reduction` |
| `metric_value` | 数值 |
| `params` | **当时用的参数（JSON）——不记参数的结果没法复现** |
| `calculated_at` | |

**表 8 · `forecast_records`（能碳预测结果）** ⭐ v5.0 新增

| 字段 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `forecast_made_at` | 时间 | 是 | 2026-07-01 09:00 | **何时做的这次预测**（用于事后回溯） |
| `ts_start` / `ts_end` | 时间 | 是 | | 预测覆盖的时段 |
| `job_id` / `job_type` / `server_model` | 文本 | 否 | | 预测对象 |
| `pred_energy_kwh` | 小数 | 是 | 4820.0 | 能耗点估计 |
| **`pred_energy_p25` / `pred_energy_p75`** ⭐ | 小数 | 是 | 4120 / 5610 | **不确定性区间，必填** |
| `pred_carbon_gco2e` | 小数 | 是 | | 碳排点估计 |
| `pred_carbon_p25` / `pred_carbon_p75` | 小数 | 是 | | 碳排区间 |
| **`sample_size`** ⭐ | 整数 | 是 | 34 | **基线基于多少历史样本**——低于阈值须标注不可靠 |
| `ef_source` | 文本 | 是 | grid_approx | 所用碳强度来源：grid_actual / grid_approx（**近似须标注**） |
| `baseline_method` | 文本 | 是 | median_by_type_model | 基线算法，保证可复现 |
| `actual_energy_kwh` | 小数 | 否 | | **事后回填的实测值** |
| `deviation_pct` | 小数 | 否 | −8.4 | 预测偏差，用于校准 |

> ⭐ **为什么必须存 `sample_size` 和区间**：预测天然不确定。只给一个数字会让人误以为它是确定的。
> 存 `actual_energy_kwh` 和 `deviation_pct` 是为了**下期校准**——预测不回测就永远不知道准不准。

---

### 10.3 基础与资产表（6 张）

**表 9 · `emission_factors`（排放因子表，v5.0 扩为多库）**

| 字段 | 必填 | 示例 | 说明 |
|---|---|---|---|
| `factor_name` / `category` | 是 | 外购电力（华东） | |
| **`source_db`** ⭐ | 是 | 主管部门基准线 | **来自哪个数据库**——CLCD 类 / ecoinvent / IPCC / 主管部门基准线 / 企业自建。**跨库对齐的前提** |
| `input_unit` / `output_unit` | 是 | kWh / gCO2e | |
| `factor_value` | 是 | 按实际填 | |
| **`system_boundary`** ⭐ | 否 | 摇篮到大门 | 系统边界，跨库对比必须核对 |
| **`region_scope`** ⭐ | 否 | 华东 | 地域适用范围 |
| `applicable_year` | 否 | 2026 | |
| `data_source` | 是 | 文献/公告出处 | |
| `version` / `is_builtin` / `updated_at` | 是 | | |

> ⭐ `source_db`、`system_boundary`、`region_scope` 是 v5.0 新增字段。**没有它们，SubAgent 3 的跨库对齐和敏感性分析做不了**——同一活动在不同库的取值必须能区分开、能比较边界。

**表 10 · `tariff_table`**：`period_type`、`month_range`、`time_start`/`time_end`、`price_per_kwh`、`demand_charge`

**表 11 · `server_specs`（服务器机型台账）**

| 字段 | 必填 | 示例 | 说明 |
|---|---|---|---|
| `server_model` | 是 | H100-8卡 | 机型标识 |
| `accelerator` / `card_count` | 否 | H100 / 8 | 加速卡 |
| `rated_power_w_per_card` | 否 | 700 | 单卡额定功率 |
| **`idle_power_w_per_card`** ⭐ | 是 | 65 | **单卡待机功率——算空载能耗的必要条件** |
| `rated_power_kw` | 否 | 10.2 | 整机额定功率 |
| `embodied_kgco2e` | 是 | 3000 | 隐含碳 |
| `lifetime_years` | 是 | 5 | 设计寿命 |
| `room_id` / `version` / `updated_at` | | | |

**表 12 · `park_structure`（园区结构表）** ⭐ v4.0 新增

| 字段 | 必填 | 示例 | 说明 |
|---|---|---|---|
| `park_id` / `park_name` | 是 | naxi-01 / 纳溪服务区 | |
| `room_id` | 是 | room-A | |
| `server_model` | 是 | H100-8卡 | |
| `installed_cards` | 是 | 128 | **装机卡数——空置率分母** |
| `rack_capacity` | 否 | 40 | 可容纳机位（算物理空置率） |
| `racks_used` | 否 | 26 | 已装机位 |
| `pue` | 是 | 1.32 | **实测值，不内置** |
| `version` / `updated_at` | 是 | | |

**表 13 · `device_lifecycle`（设备生命周期台账）** ⭐ v4.0 新增

| 字段 | 必填 | 示例 | 说明 |
|---|---|---|---|
| `device_id` / `server_model` | 是 | node-12 / H100-8卡 | |
| `commissioned_at` | 是 | 2024-03-15 | 投产日期 |
| `purchase_cost` | 否 | 480000 | 采购成本（元），算 LCC |
| `cumulative_hours` | 是 | 12400 | 累计运行小时 |
| **`cumulative_equiv_hours`** ⭐ | 是 | 14100 | **累计等效运行小时（按负载率加权）** |
| `equiv_life_used` | 是 | 0.32 | 等效寿命消耗率 |
| `status` | 是 | 在役 | 在役 / 待退役 / 已梯次利用 / 已回收 |
| `residual_value` | 否 | 96000 | 预计残值 |
| `updated_at` | 是 | | |

**表 14 · `carbon_assets`**：`asset_type`、`year`、`amount`/`unit`、`direction`（获得/使用）、`contract_no`、`note`、`imported_at`

### 10.4 流程表（2 张）

**表 15 · `measures`（措施台账）** 字段见 [7.3](#73-措施台账)。

**表 16 · `workflow_runs`（工作流实例）**：`template`、`period`、`status`、`steps_done`（JSON）、`snapshot_params`（口径与参数快照 JSON）、`agent_conclusion`、`user_note`、`created_at`

---

## 11. 界面结构

Streamlit 侧边栏导航 + 主区域，共七页。

```
┌──────────────────┬──────────────────────────────────┐
│  能碳监测平台      │        （主内容区）                │
│  算力工业园区      │                                  │
│                  │   当前页的图表、表格、操作按钮      │
│  🏠 总览          │                                  │
│  📥 数据与三性分析 │  ← 模块一（合并）                 │
│  🔥 高能耗·空置·风光储 │  ← 模块二                    │
│  🌱 Token碳足迹与生命周期 │ ← 模块三                  │
│  💰 成本与碳资产  │  ← 模块四                         │
│  🔄 工作流        │  ← 模块五                         │
│  ⚙️ 设置与基础表  │                                  │
│                  │                                  │
│  ─────────────   │                                  │
│  🤖 Agent  [开/关]│  ← 全局开关，随时可关             │
│  服务区：纳溪 ▾   │  ← 多服务区切换                   │
└──────────────────┴──────────────────────────────────┘
```

| 页面 | 内容 |
|---|---|
| 🏠 **总览** | 三个核心问题的快照卡片 + 核心指标（Token碳足迹、空置率、绿电比例、储能净减碳、PUE、履约缺口）+ 最近导入 + 待办工作流 |
| 📥 **数据与三性分析** | 五类数据上传/采集 → 待确认预览区 → 确认入库；三个 Tab：差异性 / 时序性 / 不稳定性 |
| 🔥 **高能耗·空置·风光储** | 参数设置 → 高能耗时段清单 → 空置率与空载能耗 → 负荷突增事件 → 风光储三源有效性 → 五维归因 |
| 🌱 **Token碳足迹与生命周期** | 口径选择 → 主卡片（双算法+完整标注）→ 分时段曲线 → 使用强度与寿命消耗 → LCC 与退役决策 → 明细下钻 → 导出 |
| 💰 **成本与碳资产** | 电费构成 → 需量曲线 → 成本-碳双维图 → 碳资产台账 → 履约缺口 |
| 🔄 **工作流** | 模板选择 → 七步清单（勾选推进）→ 措施台账 → 历史实例 |
| ⚙️ **设置与基础表** | 五张基础表（可编辑+版本历史）+ API 密钥 + 数据边界设置 + 口径默认值 + 各类阈值参数 |

**统一的页面顺序**：`输入 → 确认 → 参数 → 看图 → 看明细 → 导出`。七个页面结构一致，学会一个就会用全部。

---

# 第三部分 · 技术与工程

## 12. 技术方案与技术栈

### 12.1 主方案：渐进式路线

**阶段一~四** → Python + Streamlit ｜ **阶段五** → PyInstaller 打包成 Win/Mac 双击即用的启动包

**决策日期**：2026-08-21 ｜ **决策人**：项目所有者

**为什么选它**：① 风险最低，先小成本做出能跑的东西；② 代码零浪费，打包只加一层配置；③ Streamlit 代码量最少，零基础也改得动；④ Python 在文件解析、统计分析、图表、LLM SDK 上生态最成熟。

**备选方案对比（留档）**

| 维度 | A · Streamlit | B · PySide6 原生 | C · Tauri/Electron + React |
|---|---|---|---|
| 最终形态 | 浏览器打开的本地网页 | 原生桌面窗口 | 现代化桌面客户端 |
| 界面美观度 | 一般，工具感 | 中等 | 最好 |
| 代码复杂度 | 最低 ⭐ | 中高（约 A 的 3–5 倍） | 最高 |
| 零基础可维护性 | 最好 ⭐ | 一般 | 最差 |
| **统计分析能力** | 最强（pandas/numpy）⭐ | 最强 ⭐ | 弱，要造轮子 |
| LLM SDK 生态 | 最成熟 ⭐ | 最成熟 ⭐ | 中等 |
| GPU 功耗采集 | `pynvml` 现成 ⭐ | 现成 ⭐ | 需绕道调 Python |
| 跨平台成本 | 零，代码完全一致 ⭐ | 需分别打包 | 需分别打包 |
| 开发周期 | 最短 ⭐ | 中等偏长 | 最长 |

**为什么没选 B**：界面代码复杂度对零基础不友好，且 macOS 打包必须有 Mac 实机。
**为什么没选 C**：需同时掌握前后端两套栈；而「界面漂亮」不是核心矛盾——核心矛盾是**把三性分析做对、把每百万 Token 碳足迹和寿命折损算准**，那是 Python 的主场。

### 12.2 关键决策记录

| 决策 | 选择 | 理由 | 代价 |
|---|---|---|---|
| **分析框架** | 算力工业园区（Token 即产品） | 可复用工业碳核算成熟方法论 | 需要 Token 计量数据 |
| **核心指标** | 每百万 Token 碳足迹 | 单位产品碳足迹是通用语言 | 口径敏感，必须强制标注 |
| **方法论主线** | 差异性 / 时序性 / 不稳定性 | 三性是能碳数据的固有特征 | 分析代码量显著增加 |
| **Agent 组织方式** ⭐ | 按三个核心问题组织，而非按职责 | 问题驱动比功能罗列更能落地 | 工具集需按问题设计 |
| **能耗归因边界** | 机型对比用 IT 电耗，不含 PUE | PUE 是机房属性，摊进去会污染结论 | 需要分项计量 |
| **Token 口径** | 默认 输入+输出，可切换，**强制标注** | 行业无统一标准，只能显式化 | 数据模型多带五个口径字段 |
| **绿电时间属性** | 风光按时序曲线，绿证按总量 | 绿证本不带时间属性 | 两套逻辑 |
| **储能建模** ⭐ | 记录充电来源，按往返效率算净减碳 | **不假设储能一定减碳**，它可能增碳 | 需要充电来源数据 |
| **寿命折损** ⭐ | 负载率分档加权的等效运行小时 | 能回答「使用频率的影响」，且足够简单 | 是简化模型，不是可靠性物理模型 |
| **隐含碳摊销** ⭐ | 按等效运行小时，而非日历时长 | 使用频率高的设备应摊得更快 | 需要持续跟踪运行强度 |
| **统计库** | 先用 pandas + numpy 自实现 | 滚动统计、CV、自相关、简易分解都能做 | 分解和变点检测不如专业库严谨 |
| **是否引入 statsmodels** | **暂不引入，列为待定** | 依赖精简优先 | 现阶段方法较朴素 |
| Agent 类型 | 大模型驱动的智能体 | 归因需处理非结构化情况、需解释原因 | 必须联网、有费用、有不确定性 |
| 数据边界 | 原始明细留本地，只发聚合摘要 | 在「能用 AI」和「数据安全」间取平衡 | 实现复杂度上升 |
| 数字 vs 结论分离 | 碳足迹由代码算死，Agent 只解读 | 测算必须确定可复现 | Agent 能力限制在解读层 |
| 数据存储 | 本地 SQLite | Python 自带，零安装，单文件好备份 | 不支持多人 |
| 解析结果确认 | 必须人工确认才入库 | 解析出错是常态 | 多一步操作 |
| 基础表版本快照 | 每次修改存版本 | 历史结果可追溯、不被静默改变 | 存储开销 |

### 12.3 技术栈清单

| 依赖 | 版本 | 干什么（大白话） | 阶段 |
|---|---|---|---|
| **Python** | ≥3.11 | 编程语言本身，软件的地基 | 全程 |
| **streamlit** | ≥1.30 | 把 Python 代码变成网页界面 | 一 |
| **pandas** | ≥2.0 | 处理表格数据，「代码版的 Excel」 | 一 |
| **numpy** | ≥1.26 | 数值计算，三性分析的统计指标靠它 | 一 |
| **openpyxl** | ≥3.1 | 读写 .xlsx | 一 |
| **xlrd** | ≥2.0 | 读老格式 .xls | 一 |
| **plotly** | ≥5.18 | 画交互式图表（热力图、箱线图、多曲线叠加） | 一 |
| **SQLite** | Python 自带 | 本地数据库，无需安装 | 一 |
| **pdfplumber** | ≥0.11 | 读 PDF 里的文字和表格 | 二 |
| **python-docx** | ≥1.1 | 读 Word 里的文字和表格 | 二 |
| **anthropic** | ≥1.0 | **Claude 官方 Python SDK，Agent 靠它调模型** | 三 |
| **pynvml** | ≥11.5 | 读 GPU 实时功耗（`nvidia-smi` 底层库） | 四 |
| **PyInstaller** | ≥6.0 | 打包成双击就能开的启动包 | 五 |

**待定依赖**：`statsmodels`（严谨的季节性分解和变点检测）。**默认不装**，阶段二若发现自实现不够用再评估并补记到 [12.2](#122-关键决策记录)。

> **`anthropic` 1.x 注意**：基于 `httpx2` 而非 `httpx`。若自己要用 HTTP 库，写 `import httpx2 as httpx`，用 `httpx` 包的对象会在请求时失败。

---

## 13. 目录结构与开发路线图

### 13.1 目录结构

```
LBY97/
├── 能碳.md                        # 本文档（产品说明书 + 项目宪法）
├── CLAUDE.md                      # 三行指针，@导入 能碳.md，保证自动加载
├── README.md                      # 怎么装、怎么启动
├── requirements.txt               # 依赖清单
├── .env.example                   # 环境变量模板（API 密钥填这里）
├── .gitignore                     # 不提交的文件（数据库、密钥、缓存）
│
├── app.py                         # 程序入口，运行它启动平台
│
├── pages/                         # 功能页面（Streamlit 自动读）
│   ├── 1_数据与三性分析.py
│   ├── 2_高能耗空置与风光储.py
│   ├── 3_Token碳足迹与生命周期.py
│   ├── 4_成本与碳资产.py
│   ├── 5_工作流.py
│   └── 6_设置与基础表.py
│
├── core/                          # 核心逻辑，界面代码调用它
│   ├── db.py                      # 数据库读写
│   ├── models.py                  # 十六张表的结构定义
│   ├── parsers/                   # excel / pdf / docx 解析
│   ├── collectors/                # gpu (pynvml) / ipmi 实时采集
│   ├── analysis/                  # ⭐ 三性分析（方法论内核）
│   │   ├── variability.py         # 差异性：模型×机型矩阵、CV、离群
│   │   ├── temporality.py         # 时序性：周期分解、自相关、相位差
│   │   └── instability.py         # 不稳定性：滚动波动、变点、尖峰
│   ├── idle.py                    # ⭐ 空置率与空载能耗（问题一）
│   ├── detector.py                # 高能耗时段识别
│   ├── power_source.py            # ⭐ 风光有效性评估（问题二）
│   ├── storage.py                 # ⭐ 储能净减碳测算（问题二）
│   ├── carbon.py                  # 碳足迹测算（含每百万Token、分时段）
│   ├── lifecycle.py               # ⭐ 寿命折损 / LCC / 回收（问题三）
│   ├── forecast.py                # ⭐ v5.0 能碳预测基线与区间（SubAgent1 的算法底座）
│   ├── grid_renewable.py          # ⭐ v5.0 电网级风光趋势与低碳窗口（SubAgent2 底座）
│   ├── factor_align.py            # ⭐ v5.0 跨库因子对齐与敏感性（SubAgent3 底座）
│   ├── cost.py                    # 电费与成本
│   ├── assets.py                  # 碳资产与履约缺口
│   ├── workflow.py                # 工作流引擎
│   ├── charts.py                  # 图表生成
│   └── exporter.py                # 导出 Excel / 图片 / 报告
│
├── agent/                         # Agent 层（可整体禁用）
│   ├── client.py                  # Claude SDK 封装
│   ├── boundary.py                # ⭐ 数据边界过滤 + 脱敏（唯一出口，不许绕过）
│   ├── audit.py                   # 调用审计日志（须记录发起方是哪个 Agent）
│   ├── orchestrator.py            # ⭐ 主 Agent：分解 · 调度 · 汇总 · 冲突仲裁
│   ├── prompts/                   # 各 Agent 的系统提示词
│   │   ├── orchestrator.md
│   │   ├── sub1_forecast.md
│   │   ├── sub2_lowcarbon.md
│   │   └── sub3_factor.md
│   └── subagents/                 # ⭐ 三个 SubAgent，工具集在代码层分开挂载
│       ├── sub1_forecast.py       # 任务需求与能碳预测
│       ├── sub2_lowcarbon.py      # 风光趋势匹配与低碳识别
│       ├── sub3_factor.py         # 因子对齐与碳核算报告
│       └── tools/                 # 按 Agent 分组的工具定义（@beta_tool）
│           ├── common.py
│           ├── sub1_tools.py
│           ├── sub2_tools.py
│           └── sub3_tools.py
│
├── data/
│   ├── factors.csv                # 内置排放因子表
│   ├── tariff.csv                 # 分时电价表
│   ├── server_specs.csv           # 服务器机型台账
│   ├── park.csv                   # 园区结构表（纳溪服务区等）
│   ├── device_lifecycle.csv       # 设备生命周期台账
│   ├── app.db                     # 本地数据库（自动生成，不进 Git）
│   ├── agent_audit.log            # Agent 调用审计（不进 Git）
│   └── versions/                  # 基础表历史版本快照
│
├── samples/                       # 示例文件，用来测试
│   ├── 示例_机房用电台账.xlsx
│   ├── 示例_Token计量.csv
│   ├── 示例_风光出力曲线.csv
│   ├── 示例_储能充放电.csv
│   └── 示例_任务日志.csv
│
└── tests/
    ├── test_carbon.py             # 碳测算公式（最重要）
    ├── test_boundary.py           # 数据边界过滤是否生效（最重要）
    ├── test_storage.py            # ⭐ 储能净减碳（含为负的情形）
    ├── test_lifecycle.py          # ⭐ 等效运行小时与摊销
    ├── test_forecast.py           # ⭐ v5.0 预测区间与样本量门槛
    ├── test_factor_align.py       # ⭐ v5.0 跨库对齐、分歧呈现、匹配分级
    ├── test_analysis.py           # 三性分析的统计指标
    └── test_parsers.py
```

**当前进度**：只有 `能碳.md`、`CLAUDE.md`（指针）和 `README.md`，其余是待建蓝图。

### 13.2 开发路线图

每阶段标注「做完之后你能干什么」，方便验收。

| 阶段 | 做什么 | 做完你能干什么 |
|---|---|---|
| **✅ 零** | 产品文档 | 明确要做什么，以后说「按 能碳.md 做」即可 |
| **🔨 一** | 地基 + 核心指标 | 见下 |
| **🔨 二** | 三性分析 + 问题一 | 见下 |
| **🔨 三** | 问题二 + 问题三 | 见下 |
| **🔨 四** | Agent + 成本碳资产 | 见下 |
| **🔨 五** | 实时采集 + 工作流闭环 | 见下 |
| **🔨 六** | 打包分发 | 双击即用，可发给同事 |

#### 🔨 阶段一 · 地基 + 核心指标

1. Streamlit 框架 + 七个页面骨架｜2. SQLite + 十六张表｜3. Excel/CSV 导入（能耗 + Token + 机型 + 园区结构）｜4. **每百万 Token 碳足迹测算**（双算法 + 完整口径标注）｜5. 五张基础表管理（含版本快照）｜6. 基础图表 + 导出

> **你能干什么**：把台账和 Token 统计导进去，得到 `每百万 Token 碳足迹 = XX gCO₂e（含完整口径标注）`，下钻查计算依据，导出汇总表。
> **验收**：走完「导入 → 确认 → 测算 → 下钻 → 导出」，数字与手算一致，口径标注完整。

#### 🔨 阶段二 · 三性分析 + 问题一

1. 差异性（模型/任务 × 机型能效矩阵）｜2. 时序性（周期分解、自相关、相位差）｜3. 不稳定性（滚动波动、变点、尖峰）｜4. **空置率与空载能耗**｜5. 高能耗时段识别 + 五维归因｜6. 负荷突增事件与线索关联｜7. PDF/Word 解析

> **你能干什么**：**完整回答问题一**——什么时段、哪些模型最费电，空置率多少、白烧了多少电，哪些时段负荷突增、可能是什么触发的。
> **验收**：三性各自能对真实数据产出可解释结论，参数可调、随结果存档可复现。

#### 🔨 阶段三 · 问题二 + 问题三

1. 风光出力与消纳导入 + 有效性评估｜2. **储能充放电导入 + 净减碳测算**｜3. 分时绿电比例 + 弃电/高碳时段｜4. **分时段 Token 碳足迹曲线**｜5. **使用强度与等效运行小时**｜6. **LCC、残值、回收再利用与退役决策**

> **你能干什么**：**完整回答问题二和问题三**——风光储各自到底有没有用（储能是不是在增碳），不同时段碳足迹差多少，哪台机器被用得太狠、该梯次利用还是换新。
> **验收**：储能净减碳能算出为负的情形并正确标红；寿命折损分档系数可调且随结果存档。

#### 🔨 阶段四 · Agent + 成本碳资产

1. 分时电价 + 需量 + 成本-碳双维｜2. 碳资产台账 + 履约缺口｜3. **数据边界过滤 + 脱敏（先做这个，再接模型）**｜4. `anthropic` SDK 封装 + 密钥管理

**再按下面的顺序上 Agent —— 先底座算法，后 Agent 外壳：**

5. `core/forecast.py` 预测基线与区间｜6. `core/grid_renewable.py` 电网风光与低碳窗口｜7. `core/factor_align.py` 跨库因子对齐
8. **SubAgent 3**（因子对齐与核算）——**先做它**，因为它不依赖 1 和 2，能独立验证
9. **SubAgent 1**（能碳预测）｜10. **SubAgent 2**（低碳识别，依赖 SubAgent 1）
11. **主 Agent**（编排与冲突仲裁）｜12. 发送前预览确认 + 审计日志 + 纯本地模式｜13. 报告初稿生成

> **为什么这个顺序**：三个 SubAgent 的底座算法要先在 `core/` 里跑通、能被测试验证，再往上套 Agent 外壳。
> 反过来做的话，Agent 说错话时你分不清是模型的问题还是底层算法的问题。
> SubAgent 3 排最前，因为它是唯一不依赖其他 SubAgent 的，可以独立闭环。

> **你能干什么**：点一下，主 Agent 自动分解问题、调度三个 SubAgent，给出预测、低碳窗口建议和碳核算报告初稿。
> **验收**：① 关掉全部 Agent 后其余功能全部正常（`core/` 不 import `agent/`）；② 审计日志确认发出内容**不含**任何原始明细和设备标识，且**记录了发起方是哪个 Agent**；③ SubAgent 3 能在跨库因子取值不同时**呈现分歧而非自动选一个**。

#### 🔨 阶段五 · 实时采集 + 工作流闭环

1. GPU 功耗采集（`pynvml`）+ 采样调度 + 降采样归档｜2. IPMI/Redfish 整机功耗｜3. 三个工作流模板 + 七步清单｜4. 措施台账 + 下周期同口径自动回测

#### 🔨 阶段六 · 打包分发

PyInstaller 配置 → Windows 启动包 → macOS 启动包 → 使用手册

#### 💭 远期可选

扫描件 OCR｜多服务区横向对标｜碳排预测｜引入 `statsmodels`｜自定义报告模板

---

## 14. 已知限制与不做清单

诚实列出来，避免误解。

### 14.1 数据缺失导致的能力降级

| 缺什么 | 后果 |
|---|---|
| **Token 计量** | ⚠️ 每百万 Token 碳足迹**算不出来**，只能退回按时段/任务口径 |
| **模型标识** | ⚠️ 问题一的「哪些模型最费电」答不了，只能到任务级 |
| **装机卡数 / 待机功率** | ⚠️ **空置率和空载能耗算不出来**，问题一答不完整 |
| **储能充电来源** | ⚠️ **储能净减碳算不准**，问题二的关键结论失效 |
| **电网级风光出力** ⭐ | ⚠️ **SubAgent 2 的低碳窗口识别不可用**；只能用园区自建数据，但那回答不了"买的电有多绿" |
| **待执行任务队列** ⭐ | ⚠️ **SubAgent 1 无法预测**，只能做事后测算 |
| **多个排放因子库** ⭐ | ⚠️ **SubAgent 3 的跨库对齐与敏感性分析做不了**，退化为单库核算 |
| **风光分开记录** | ⚠️ 只能给三源合计，无法分别评估「风电有用还是光伏有用」 |
| **设备累计运行强度** | ⚠️ 只能按日历时长摊隐含碳，问题三退化 |
| **分项计量粒度** | ⚠️ 只有园区总表时，机型级归因做不到 |

### 14.2 明确不做

| 项目 | 原因 |
|---|---|
| 扫描件 / 图片型 PDF | 需 OCR 引擎，依赖重、准确率不稳定 |
| 碳交易 / 配额撮合 / 定价 | 涉及资质和外部接口，超出单机工具范畴 |
| 自动执行调度、储能充放电或制冷控制 | 平台只给建议，执行由人决定；自动控制涉及生产安全 |
| 带约束的成本-碳联合优化求解 | 做测算、识别、归因、建议，不做优化求解器 |
| **设备可靠性 / 失效预测** | 寿命折损是工程简化模型，**不预测哪台机器什么时候坏** |
| 多人协作 / 共享数据库 | 明确单机使用 |
| 认证级碳核算报告 | 仅供内部分析，ISO 14064/14067 需第三方核证机构出具 |
| 自动判断数据正确性 | 平台只做测算和统计标记，数据质量由使用者把关 |
| **判断「统计异常」是否为「业务问题」** | 计划内压测也会被标为突变点，**是不是问题由你判断** |
| **对负荷突增做因果推断** | 平台只做时间对齐，**时间上撞在一起不等于因果** |
| Agent 完全离线运行 | 大模型 API 必须联网。**代价已知并接受**，用数据边界和纯本地模式控制风险 |
| **替你选定排放因子** ⭐ | SubAgent 3 呈现分歧和建议，**但不替你拍板**。选哪个库是有合规后果的决定 |
| **自动执行任务迁移** ⭐ | SubAgent 2 给低碳窗口匹配建议，**不下发调度指令**。任务能否迁移只有你知道 |

### 14.3 精度与适用性提醒

| 项目 | 说明 |
|---|---|
| Agent 结论的准确性 | ⚠️ 大模型有不确定性。**结论仅供参考，数字以平台计算为准** |
| **能碳预测的准确性** ⭐ | ⚠️ 预测基于历史基线外推，**遇到没跑过的任务类型或新机型会失准**。必须看区间和样本数，不要只看点估计 |
| **电网碳强度的近似** ⭐ | ⚠️ 拿不到电网实发分时碳强度而用"风光占比→碳强度"推算时，**结果是近似值**，平台会标注，但精度有限 |
| **跨库因子的可比性** ⭐ | ⚠️ 不同库的**系统边界可能不同**（摇篮到大门 vs 摇篮到坟墓）。边界不一致的因子**不能直接比大小**，平台会提示但无法自动对齐边界 |
| Token 口径的行业标准 | ⚠️ 输入+输出 vs 仅输出各家不同。平台强制标注，但**跨组织对比仍需谨慎** |
| **寿命折损分档系数** | ⚠️ 默认 0.6/1.0/1.3 是**经验值**，真实老化受温度、功率循环、器件批次影响。**必须按你的实际情况校准** |
| **残值折价系数 γ** | ⚠️ 二手算力设备折价率差异极大，默认 0.5 仅为占位值 |
| **回收抵扣系数** | ⚠️ 平台不内置，**必须按你实际回收商的数据填写** |
| 排放因子的权威性 | ⚠️ 内置为**参考值**，使用前必须按地区、年份核对更新 |
| 电价表 / PUE / 待机功率 | ⚠️ **必须自行实测填写**，内置值仅为示例 |
| 隐含碳数据 | ⚠️ 厂商 EPD 报告覆盖率低，多数需用行业均值估算 |
| 时序分解的严谨性 | ⚠️ 阶段二用 pandas 自实现，不如专业统计库严谨 |
| 复杂 Excel（多层合并表头） | ⚠️ 尽力解析，解析不了在预览区提示，需人工调整源文件 |
| 实时采集的权限前提 | ⚠️ 需能访问目标机器。拿不到就退回文件导入，粒度变粗 |

---

# 第四部分 · 协作约定

## 15. 常用命令

> ⚠️ 阶段一代码还没写，这些命令**要等阶段一完成后才能用**。先放在这里备查。

### Windows（在 PowerShell 或 CMD 里执行）

```powershell
# 第一次使用：创建独立环境并安装依赖
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 配置 API 密钥（只有要用 Agent 才需要）
copy .env.example .env
# 然后用记事本打开 .env，把密钥填进去

# 每次启动平台
.venv\Scripts\activate
streamlit run app.py

# 停止平台：在命令窗口按 Ctrl + C
```

### macOS（在「终端」里执行）

```bash
# 第一次使用：创建独立环境并安装依赖
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 配置 API 密钥（只有要用 Agent 才需要）
cp .env.example .env
# 然后用文本编辑器打开 .env，把密钥填进去

# 每次启动平台
source .venv/bin/activate
streamlit run app.py

# 停止平台：在终端窗口按 Control + C
```

启动成功后浏览器自动打开 `http://localhost:8501`。

### 常见问题速查

| 现象 | 怎么办 |
|---|---|
| `python 不是内部或外部命令` | Python 没装或没勾「Add to PATH」，重装时勾上 |
| `ModuleNotFoundError` | 依赖没装全，重跑 `pip install -r requirements.txt` |
| 浏览器没自动打开 | 手动输入 `http://localhost:8501` |
| 端口被占用 | 换端口：`streamlit run app.py --server.port 8502` |
| 提示「未配置 API 密钥」 | 只影响 Agent，其余功能正常 |
| `pip` 下载慢 | 换镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| Mac 提示「无法验证开发者」 | 阶段六打包版才会遇到。系统设置 → 隐私与安全性 → 「仍要打开」 |

---

## 16. 给 Claude Code 的工作约定

> 这一节是给 AI 助手看的规则，每次会话都会自动读到。

### 16.1 语言与命名

- **面向用户的文字一律中文**：界面文案、提示、错误信息、报告标题
- **代码注释用中文**，让零基础的项目所有者看得懂
- **变量名、函数名、文件名用英文**（`pages/` 下的页面文件名例外，用中文以便侧边栏显示）
- 术语统一用[第 2 节](#2-名词解释零基础必读)的表述，不要自创同义词

### 16.2 开发原则

1. **先跑通，再优化**：每个功能先做最简可用版本。不要一上来写复杂抽象
2. **小步交付**：一次只做一个功能点，做完让项目所有者立刻能看到效果
3. **不静默失败**：解析出错、采集失败、API 报错，一律在界面明确提示，绝不悄悄跳过或填默认值
4. **保持依赖精简**：不为一个小功能引入新库。确需新增时先说明理由并补记到 [12.2](#122-关键决策记录)
5. **任何依赖变更必须同步更新 `requirements.txt`**

### 16.3 数据相关的硬性规则

- **解析和采集结果必须经用户确认才入库**（[3.2](#32-统一接入流程)），不允许直接写库
- **每条入库数据必须记录来源和时间**
- **每条碳测算结果必须存完整口径标注**：`token_scope` / `include_embodied` / `embodied_method` / `energy_boundary` / `factor_version` 五个字段缺一不可（见 [5.6](#56-口径标注硬性要求)）
- **修改基础表必须保留版本快照**，历史结果不能被静默改变
- **不要删除或覆盖 `data/app.db`**，那是用户的真实数据
- **地域法和市场法必须同时计算并同时展示**
- **口径不一致的数据禁止直接对比**：`compare_periods` 遇到口径不一致必须报错，不允许静默对比

### 16.4 分析计算的硬性规则

1. **参数必须可见可调**：窗口大小、阈值系数、寿命分档系数、折价系数、权重，一律在界面显示当前取值，**不允许隐藏的魔法数字**
2. **参数必须随结果存档**（`analysis_results.params`）：同一份数据换个窗口大小结果就不同
3. **机型能效对比必须用 IT 电耗**，不含 PUE
4. **只标记统计异常，不下业务结论**：平台说「这是统计上的突变点」，不说「这是故障」
5. **负荷突增只做时间对齐，不做因果推断**：输出「疑似触发源」而非「原因」
6. **样本量不足要明确提示**：某个组合样本太少时必须标注「样本不足，结论不可靠」
7. **储能净减碳为负时必须标红并说明原因**，不允许显示为 0 或隐藏
8. **寿命折损模型必须标注为「工程简化模型」**，界面上不得暗示可预测失效

### 16.5 Agent 相关的硬性规则

> 这几条是安全底线，**违反任何一条都是严重问题**。

1. **数据边界不可绕过**：所有出网内容必须经过 `agent/boundary.py` 过滤。禁止在 `agent/` 以外的地方直接调 API
2. **禁止发送原始明细**：逐条记录、设备序列号/IP/主机名、机房位置、文件原文、Token 明细日志、合同编号，一律不得出网
3. **API 密钥只从环境变量读**（`ANTHROPIC_API_KEY`）。**绝不硬编码，绝不提交进 Git**，`.env` 必须在 `.gitignore` 里
4. **每次调用必须写审计日志**（`data/agent_audit.log`）
5. **Agent 不得写数据库**：工具集只提供只读、只返回聚合值的函数
6. **测算层不得依赖 Agent**：关掉 Agent 后 `core/` 的功能必须全部正常。**写代码时保证 `core/` 不 import `agent/`**
7. **Agent 输出必须标注**：界面加 🤖 图标和「AI 分析建议，请自行核实」
8. **数据不足时 Agent 必须说「答不了」**，不许编造数字或结论

**v5.0 多 Agent 架构追加的规则：**

9. **工具集在代码层按 Agent 分开挂载**，不靠提示词约束。SubAgent 1 的会话里**根本不挂载** SubAgent 3 的工具——越界要成为不可能，而不是不应该
10. **边界过滤只有一个出口**：所有 Agent 共用 `agent/boundary.py`，**任何 SubAgent 不许自带过滤逻辑**。多套规则必然出现漏洞
11. **SubAgent 之间传递的也必须是聚合值**：SubAgent 1 传给 2 的是预测曲线，不是逐条任务明细
12. **审计日志必须记录发起方**：`data/agent_audit.log` 每条须标明是主 Agent 还是哪个 SubAgent 发起
13. **主 Agent 不得直接算数或读库**，只能通过 SubAgent；**SubAgent 之间不得互相调用**，只能由主 Agent 编排
14. **单个 SubAgent 失败不得拖垮整体**：其余结果照常返回，并明确说明缺失了哪一环、因此哪些结论给不出
15. **预测值必须与实测值视觉区分**，且必须带区间与样本数；样本不足时标注"不可靠"
16. **跨库因子分歧必须呈现，不许自动选定后隐藏**。报告须写明最终用了哪个库、哪个版本、为什么

### 16.6 调用 Claude API 的编码约定

- **SDK**：用官方 `anthropic` Python SDK，不要用 `requests` 手写 HTTP
- **模型**：默认 `claude-opus-5`。不要为省钱擅自降级——那是项目所有者的决定
- **客户端**：用零参构造 `anthropic.Anthropic()`，让 SDK 自己从环境变量解析凭据
- **思考模式**：用 `thinking={"type": "adaptive"}`。**不要用 `budget_tokens`**，它在 `claude-opus-5` 上会返回 400
- **Agent 循环**：用 Tool Runner（`client.beta.messages.tool_runner`），工具用 `@beta_tool` 装饰器定义，不要手写 while 循环
- **长输出**：用 `.stream()` + `.get_final_message()`，避免请求超时
- **`max_tokens`**：非流式默认 `16000`，流式默认 `64000`
- **拒绝回退**：写 `claude-opus-5` 请求时默认带上服务端 fallback（`betas=["server-side-fallback-2026-07-01"]` + `fallbacks="default"`），并检查 `stop_reason` 是否为 `"refusal"` 再读 `content`
- **错误处理**：按具体到宽泛的顺序捕获（`NotFoundError` → `RateLimitError` → `APIStatusError` → `APIConnectionError`），不要一个 `except` 兜底
- **不要重复造轮子**：用 SDK 自带的类型和辅助方法

### 16.7 跨平台注意事项

- 文件路径一律用 `pathlib.Path`，不要手写 `\` 或 `/`
- 读 CSV 要处理 UTF-8 和 GBK 两种编码（中文 Windows 常见 GBK）
- 不使用仅 Windows 或仅 macOS 可用的库
- `pynvml` 在没有 NVIDIA 卡的机器上会导入失败，必须 try/except 并优雅降级

### 16.8 测试要求

六个模块**必须有测试**，其余尽量：

1. **`core/carbon.py`**——公式算错整个平台就没意义。重点测每百万 Token 碳足迹在各种口径组合下的正确性
2. **`agent/boundary.py`**——过滤失效敏感数据就出网了。**多 Agent 下要测每个 SubAgent 的出网内容都经过同一出口**
3. **`core/storage.py`**——**必须测出净减碳为负的情形**，这是问题二最容易做错的地方
4. **`core/lifecycle.py`**——等效运行小时的分档加权和摊销比例
5. **`core/forecast.py`** ⭐——**必须测样本量不足时是否正确标注"不可靠"**，以及区间上下界不会颠倒
6. **`core/factor_align.py`** ⭐——**必须测同一活动多库取值不同时是否呈现分歧**，而不是静默选一个；以及匹配分级（精确/近似/无匹配）是否正确

### 16.9 沟通方式

项目所有者是**编程零基础**的初学者：

- 解释技术问题用大白话和类比，避免堆术语
- 给出的命令要能**直接复制粘贴执行**，并说明在哪里执行
- 报错时先说「这是什么意思」，再说「怎么解决」
- 涉及技术选型的决策，列出选项和优劣，让项目所有者拍板，并把结论补记到 [12.2](#122-关键决策记录)

---

# 附录 A · 基础参数初始值

> 🔴 **重要声明**：下列数值为**参考值**，用于让平台开箱即用，**不构成任何权威依据**。
> 正式使用前必须按你所在**地区、年份、设备**的实际数据核对更新。可在「⚙️ 设置与基础表」页随时编辑。

### A.1 电力排放因子（最关键，务必替换）

> 单位统一用 **gCO₂e/kWh**，与每百万 Token 碳足迹口径一致。

| 因子名称 | 单位 | 参考值 | 结果单位 | 备注 |
|---|---|---|---|---|
| 电力（全国电网平均） | kWh | 570.3 | gCO2e | 需按最新年度值更新 |
| 电力（区域电网） | kWh | 按实际填 | gCO2e | 各区域差异大，**必须自行填入** |
| 绿电 / 绿证抵扣 | kWh | 0.0 | gCO2e | 仅市场法下生效 |

> **分时碳强度**：理想情况用电网发布的分时碳强度曲线；拿不到时可用「时段电价档位 → 碳强度档位」的近似映射，但**必须在结果上标注这是近似值**。

### A.2 其他能源因子

| 因子名称 | 单位 | 参考值 | 结果单位 |
|---|---|---|---|
| 柴油（应急发电） | L | 2630.0 | gCO2e |
| 天然气 | m³ | 2162.2 | gCO2e |
| 外购蒸汽 | GJ | 110000.0 | gCO2e |
| 自来水 | t | 300.0 | gCO2e |

### A.3 硬件隐含碳与待机功率

> 🔴 隐含碳数据公开度低，下列为**粗略行业估算**。有厂商 EPD 报告的务必替换。
> **待机功率必须实测**——它直接决定空载能耗，用经验值会算错。

| 设备类型 | 隐含碳（kgCO₂e/台） | 设计寿命（年） | 单卡待机功率参考（W） |
|---|---|---|---|
| GPU 服务器（8 卡，高端） | 3000 | 5 | 50 – 80 |
| GPU 服务器（8 卡，中端） | 2200 | 5 | 30 – 60 |
| 通用 CPU 服务器（2U） | 1200 | 5 | — |
| 存储服务器 | 1500 | 6 | — |
| 交换机（数据中心级） | 400 | 7 | — |
| UPS（模块化） | 2000 | 10 | — |
| 精密空调 | 2500 | 10 | — |

### A.4 PUE 参考区间

| 机房类型 | 典型 PUE |
|---|---|
| 老旧风冷机房 | 1.6 – 2.0 |
| 常规风冷数据中心 | 1.3 – 1.5 |
| 优化风冷 / 间接蒸发 | 1.2 – 1.3 |
| 液冷智算中心 | 1.1 – 1.2 |

> 平台不内置 PUE，**必须用你机房的实测值**（总电耗 ÷ IT 电耗），填在[园区结构表](#9-基础表管理)。

### A.5 分析参数默认值

> 全部可在界面调整，此处为初始值。

| 参数 | 默认值 | 用在哪 |
|---|---|---|
| 滚动窗口 | 24 个采样点 | 滚动标准差、滚动均值 |
| 高能耗阈值系数 | 1.2 | 高能耗时段识别 |
| 最短持续时长 | 30 分钟 | 高能耗时段识别 |
| 区间合并间隔 | 10 分钟 | 高能耗时段识别 |
| 离群判定 | 均值 ± 2×标准差 | 差异性离群标记 |
| 尖峰分位数 | P95 | 尖峰事件识别 |
| 变点检测窗口 | 前后各 12 个点 | 突变点识别 |
| 变点显著性阈值 | 前后段均值差 > 20% | 突变点识别 |
| 最小样本量 | 10 | 低于此值标注「样本不足」 |
| **寿命分档系数 k** | 轻载 0.6 / 常规 1.0 / 重载 1.3 | 等效运行小时 |
| **负载率分档界限** | 30% / 70% | 等效运行小时 |
| **残值折价系数 γ** | 0.5 | 残值测算 |
| **梯次利用潜力阈值** | 30% | 退役决策建议 |
| **储能往返效率 η** | 0.90 | 净减碳测算（**应按实测填写**） |
| 消纳率「充分发挥」阈值 | 90% | 风光储有效性分级 |
| 消纳率「未有效发挥」阈值 | 60% | 风光储有效性分级 |

---

# 附录 B · 核心公式与单位换算

### B.1 能耗与 PUE

```
机房总电耗  E_total = E_IT × PUE
PUE        = E_total ÷ E_IT
IT 电耗     E_IT = Σ(设备功率_kW × 时长_h)
卡时估算    E_IT ≈ 卡时 × 单卡平均功率_kW
```

### B.2 空置率与空载能耗（问题一）

```
【使用强度】
  使用强度(t)   = 实际运行卡时(t) ÷ 额定可运行卡时(t)
  额定可运行卡时 = 装机卡数 × 时段小时数

【空置率】
  逻辑空置率(t) = (装机卡数 − 被任务占用卡数) ÷ 装机卡数 = 1 − 使用强度(t)
  物理空置率    = (可容纳机位 − 已装机位) ÷ 可容纳机位

【空载能耗（纯浪费）】
  空载能耗(t)   = 空置卡数(t) × 单卡待机功率_kW × 时长_h
  空载能耗占比  = 空载能耗 ÷ E_IT × 100%
  空载碳排      = 空载能耗 × PUE × EF(t)
  空载电费      = Σ(空载能耗(t) × 该时段电价)
```

### B.3 风光储有效性（问题二）

```
【供给侧】
  出力系数      = 实际发电量 ÷ 理论可发量
  等效利用小时  = 发电量 ÷ 装机容量
  间歇指数      = 出力为零的时段数 ÷ 总时段数 × 100%

【消纳侧】
  消纳率        = 实际消纳量 ÷ 供给量 × 100%
  弃电量        = 供给量 − 消纳量
  分时绿电比例  = 绿电消纳量(t) ÷ 总用电量(t) × 100%
  总量绿电比例  = 绿电消纳量 ÷ 总用电量 × 100%

【减碳侧（反事实基线）】
  风光减碳量    = Σ[ 绿电消纳量(t) × ( EF_市电(t) − EF_绿电 ) ]
                （EF_绿电 通常为 0；必须用同时段 EF，不能用月均值）

【储能 —— 可能为负，必须算清楚】
  往返效率  η   = 放电量 ÷ 充电量                    典型 0.85 ~ 0.92
  为放出 E_out，需充入  E_in = E_out ÷ η

  储能净减碳    = E_out × ( EF_放电时段 − EF_充电时段 ÷ η )

    · > 0 → 真的在减碳 ✅
    · < 0 → 在增碳 ⚠️（充电时段不够干净，或效率损失吃掉收益）

  特例：若充的是本会被弃掉的绿电，则 EF_充电时段 取 0
        → 净减碳 = E_out × EF_放电时段，全额为正
```

### B.4 每百万 Token 指标（问题三）

```
【Token 总量】
  Tokens = tokens_in + tokens_out     （默认口径）
         = tokens_out                  （可切换）
         = tokens_train                （训练口径，单独统计）

【每百万 Token 碳足迹】⭐
  gCO2e/MTok = C_total_gCO2e ÷ (Tokens ÷ 1,000,000)

【分时段】
  gCO2e/MTok(t) = [ E_total(t) × EF(t) + C_embodied(t) ] ÷ ( Tokens(t) ÷ 1e6 )

【每百万 Token 电耗】
  kWh/MTok   = E_total_kWh ÷ (Tokens ÷ 1,000,000)

【能效】
  Tokens/kWh = Tokens ÷ E_IT_kWh       （用 IT 电耗，不含 PUE）

【每百万 Token 成本】
  元/MTok    = 总电费_元 ÷ (Tokens ÷ 1,000,000)
```

> ⚠️ 以上指标**必须连同口径标注一起展示**，见 [5.6](#56-口径标注硬性要求)。

### B.5 碳足迹与生命周期（问题三）

```
【运营碳排 · 地域法】
  C_location = E_total × EF_grid

【运营碳排 · 市场法】
  C_market   = (E_total − E_green − E_gec) × EF_grid + E_green × EF_green

【等效运行小时（寿命折损）】
  等效运行小时 = Σ ( 实际运行小时 × k )
    k 按负载率分档：<30% → 0.6 ｜ 30~70% → 1.0 ｜ >70% → 1.3

  设计寿命等效小时 = 设计寿命年数 × 365 × 24
  等效寿命消耗率   = 累计等效运行小时 ÷ 设计寿命等效小时

【隐含碳摊销（v4.0：按使用强度）】
  C_embodied(本期) = 设备隐含碳_gCO2e × (本期等效运行小时 ÷ 设计寿命等效小时)

【总碳足迹】
  C_total = C_operational + C_embodied

【生命周期成本】
  LCC   = 采购成本 + Σ运行电费 + Σ运维成本 − 残值
  残值  ≈ 采购成本 × (1 − 等效寿命消耗率) × γ        γ 默认 0.5

【回收与再利用】
  可再利用潜力     = 1 − 等效寿命消耗率
  回收可抵扣隐含碳 = 设备隐含碳 × 回收再利用率 r × 材料抵扣系数 m
                    （r 和 m 按实际回收商数据填写，平台不内置）
```

> ⚠️ **单位一致性**：排放因子用 `gCO₂e/kWh`，设备隐含碳表用 `kgCO₂e/台`（`server_specs.embodied_kgco2e`）。
> 计算 `C_embodied` 时**必须先 ×1000 换算为 gCO₂e** 再与运营碳排相加，否则结果差三个数量级。
> 数据库 `carbon_records.emission_gco2e` 统一以 **gCO₂e** 存储。

### B.6 三性分析

```
【差异性】
  能效           = Tokens ÷ E_IT_kWh
  单位Token电耗  = E_IT_kWh ÷ (Tokens ÷ 1e6)
  机型能效比     = 机型A能效 ÷ 机型B能效
  变异系数 CV    = 标准差 ÷ 均值
  离群判定       = |x − 均值| > 2 × 标准差

【时序性】
  时序分解       原始 = 趋势 + 周期 + 残差
                 趋势 ≈ 滚动均值（窗口 = 一个完整周期）
                 周期 ≈ 按小时/星期分组的均值 − 总均值
                 残差 = 原始 − 趋势 − 周期
  自相关 ACF(k)  = corr( x[t], x[t−k] )
                   k=24 显著 → 有日周期；k=168 显著 → 有周周期
  相位差         = argmax_k { 互相关(曲线A, 曲线B, 滞后k) }

【不稳定性】
  滚动标准差     窗口内的标准差（默认窗口 24 点）
  极差比         (max − min) ÷ 均值
  尖峰频次       超过 P95 分位数的次数
  变点判定       |前段均值 − 后段均值| ÷ 前段均值 > 20%
```

### B.7 高能耗时段识别

```
【基线】     所选窗口内功率中位数（或手工指定）
【判定条件】 功率 > 基线 × 阈值系数(默认1.2)
             且 持续时长 > 最短时长(默认30分钟)
【区间合并】 间隔 < 10 分钟的相邻区间合并
【超出幅度】 (区间平均功率 − 基线) ÷ 基线 × 100%
```

### B.8 成本与碳资产

```
【电量电费】  Σ(各时段电量 × 该时段电价)
【需量电费】  当月最高需量_kW × 需量单价
【总电费】    电量电费 + 需量电费 + 其他费用
【度电成本】  总电费 ÷ 总电量
【度电碳强度】总碳排 ÷ 总电量

【履约缺口】  核算排放 − 免费配额 − 已购配额 − 绿电抵扣 − CCER抵销
             （CCER 抵销受比例上限约束，超出部分自动截断并提示）
```

### B.9 单位换算

| 换算关系 | 系数 |
|---|---|
| 1 tCO₂e | = 1000 kgCO₂e = 1,000,000 gCO₂e |
| 1 kWh | = 3.6 MJ |
| 1 MWh | = 1000 kWh |
| 1 GJ | = 1000 MJ = 277.78 kWh |
| 1 tce（吨标准煤） | = 29.3076 GJ |
| 1 kW 持续 1 小时 | = 1 kWh |
| 1 卡时（单卡 400W） | ≈ 0.4 kWh |
| 1 MTok | = 1,000,000 Token |
| 1 年 | = 8760 小时（设计寿命等效小时用） |

---

**文档结束** ｜ 有任何需求变更，请直接修改本文件，它是这个项目的唯一事实来源。
