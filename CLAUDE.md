# 能碳监测平台 · 产品文档

> 本文件有两个作用：
> 1. **给你看**——这是产品说明书，讲清楚这个软件要做成什么样。
> 2. **给 Claude Code 看**——每次你打开 Claude Code，它会自动读这个文件。所以以后你不用重复解释需求，直接说「按 CLAUDE.md 做」就行。
>
> 文档版本：v1.0 ｜ 状态：产品定义已定稿，尚未开始编码

---

## 目录

- [第一部分 · 产品定义](#第一部分--产品定义)
  - [1. 产品概述](#1-产品概述)
  - [2. 目标用户与典型场景](#2-目标用户与典型场景)
  - [3. 名词解释（零基础必读）](#3-名词解释零基础必读)
- [第二部分 · 功能规格](#第二部分--功能规格)
  - [4. 三大功能模块](#4-三大功能模块)
  - [5. 文件解析规格](#5-文件解析规格)
  - [6. 数据模型](#6-数据模型)
  - [7. 界面结构](#7-界面结构)
- [第三部分 · 技术与工程](#第三部分--技术与工程)
  - [8. 技术方案决策记录](#8-技术方案决策记录)
  - [9. 技术栈清单](#9-技术栈清单)
  - [10. 目录结构](#10-目录结构)
  - [11. 开发路线图](#11-开发路线图)
  - [12. 已知限制与不做清单](#12-已知限制与不做清单)
- [第四部分 · 协作约定](#第四部分--协作约定)
  - [13. 常用命令](#13-常用命令)
  - [14. 给 Claude Code 的工作约定](#14-给-claude-code-的工作约定)
- [附录 A · 内置排放因子表初始条目](#附录-a--内置排放因子表初始条目)

---

# 第一部分 · 产品定义

## 1. 产品概述

### 一句话定位

**能碳监测平台**是一个运行在你自己电脑上的文件分析工具：把散落在 Excel、PDF、Word 里的用能数据、物料数据、碳排放数据导进来，自动整理成统一的表格和图表，让你能快速看清「用了多少能、耗了多少料、排了多少碳」。

### 它解决什么问题

| 你现在的痛点 | 平台怎么解决 |
|---|---|
| 能耗数据散在十几个 Excel 里，每月手工汇总 | 一次导入，自动归集到统一数据库 |
| 电、气、蒸汽单位不同，没法直接加总比较 | 自动做单位换算，统一口径对比 |
| 供应商给的碳足迹报告是 PDF，要人工抄数 | 自动提取表格和数字，人工确认后入库 |
| 想看趋势要现做透视表和图表 | 打开页面就有现成的趋势图和构成图 |
| 历史数据翻文件夹找，找不到 | 全部存在本地数据库，随时按时间/产品筛选 |

### 它**不**解决什么问题（重要，先说清楚）

- ❌ **不是碳资产管理系统**——不做配额交易、CCER、碳资产台账
- ❌ **不出具认证级报告**——本平台输出的是内部分析结果，不能直接用于 ISO 14064 / ISO 14067 / CBAM 等第三方核证提交。正式核证请以认证机构要求为准。
- ❌ **不联网、不上传任何数据**——所有文件和数据只存在你自己的电脑上
- ❌ **不做多人协作**——单机个人使用，没有账号、权限、共享数据库
- ❌ **不替你判断数据对错**——平台负责整理和计算，数据质量由你把关

### 核心设计原则

1. **本地优先**：数据不出本机，无需联网，无需注册
2. **不静默采信**：所有从文件里解析出来的数据，一律先进「待确认」区，你点确认后才真正入库
3. **可追溯**：每一条数据都记录来源文件名和导入时间，随时能查「这个数从哪来的」
4. **跨平台**：同一套代码，Windows 和 macOS 都能跑

---

## 2. 目标用户与典型场景

### 目标用户

企业里负责能源管理、EHS、双碳或生产数据统计的个人。**不需要懂编程**，会用 Excel 就行。

### 典型场景一 · 月度能耗盘点

> 每月初，把上月的电表、水表、燃气台账 Excel 导进平台 → 平台自动归集并换算单位 → 打开「用能分析」页，看这个月比上个月多用了多少电、哪条产线是能耗大户 → 导出图表贴进月报。

### 典型场景二 · 产品物料核算

> 拿到某产品的 BOM 清单和实际领用记录 → 导入「物质使用分析」→ 平台算出理论用量 vs 实际用量的差额（损耗率）→ 找出损耗异常的物料。

### 典型场景三 · 碳数据汇总

> 收到 5 家供应商发来的 PDF 碳足迹报告 → 逐份导入，平台提取碳排放数字，你人工核对确认 → 在「碳排放分析」页看到汇总总量和各家占比 → 导出汇总表。

---

## 3. 名词解释（零基础必读）

后面章节会反复用这些词，先在这里讲明白。

| 名词 | 大白话解释 |
|---|---|
| **用能 / 能耗** | 消耗了多少能源。比如用了 12000 度电、800 立方米天然气。 |
| **物质使用** | 消耗了多少材料。比如用了 2.5 吨钢材、300 公斤纸包装。 |
| **活动数据** | 上面两类「消耗了多少」的原始数字的统称，是所有计算的起点。 |
| **排放因子** | 一个换算系数，把「消耗量」换算成「碳排放量」。<br>例：用 1 度电排 0.57 公斤 CO₂ → 这个 0.57 就是电力排放因子。 |
| **碳排放量 / 碳足迹** | 一件事或一个产品，从头到尾一共排放了多少温室气体。 |
| **tCO₂e** | 吨二氧化碳当量。因为温室气体不止 CO₂ 一种（还有甲烷等），统一折算成「相当于多少吨 CO₂」来比较。<br>1 tCO₂e = 1000 kgCO₂e |
| **kWh** | 千瓦时，就是俗称的「度电」。 |
| **tce** | 吨标准煤。把各种能源统一折算成煤来比较总量的单位。 |
| **Scope 1（范围一）** | 你自己烧的。比如厂里烧天然气、公司车队烧汽油。 |
| **Scope 2（范围二）** | 你买的能源。主要是外购电力、外购蒸汽。 |
| **Scope 3（范围三）** | 上下游产生的。比如原材料生产时的排放、产品运输的排放。 |
| **BOM** | 物料清单（Bill of Materials），一张表列清楚做一个产品需要哪些料、各多少。 |
| **损耗率** | （实际用量 − 理论用量）÷ 理论用量。衡量浪费了多少。 |

---

# 第二部分 · 功能规格

## 4. 三大功能模块

平台按三级指标划分为**三个平行模块**，各自独立录入、独立分析、独立导出，互相之间没有强制依赖。

```
┌─────────────────────────────────────────────────────┐
│                   能碳监测平台                        │
├─────────────┬─────────────────┬─────────────────────┤
│  模块一      │   模块二         │   模块三             │
│  用能分析    │   物质使用分析    │   碳排放分析          │
│             │                 │                     │
│  电/水/气/   │   原料/辅料/     │   碳排放量           │
│  蒸汽/燃油   │   包装/废弃物     │                     │
└─────────────┴─────────────────┴─────────────────────┘
       独立录入        独立录入            独立录入
                                              ↑
                                    可选：调用因子表换算
                                    （见 4.3 的路径 B）
```

### 4.1 模块一 · 用能分析

**输入**：能耗台账文件（Excel / CSV 为主）

需要包含的列（列名可以不一样，导入时你手工做一次映射，平台会记住）：

| 字段 | 说明 | 示例 |
|---|---|---|
| 日期 | 统计周期 | 2026-07 或 2026-07-15 |
| 能源类型 | 电/水/天然气/蒸汽/柴油/汽油… | 电 |
| 消耗量 | 数字 | 12000 |
| 单位 | 原始单位 | kWh |
| 归属对象 | 产线/车间/产品/设备（可空） | 一号产线 |

**处理**

1. **单位归一**：把各种单位换算成统一口径
   - 电 → kWh
   - 气体燃料 → m³ 及 MJ
   - 液体燃料 → L 及 MJ
   - 蒸汽/热 → GJ
   - 水 → t
   - 同时提供「统一折标煤 tce」的总量视图，方便横向加总
2. **时间聚合**：按日 / 月 / 季 / 年汇总
3. **分组**：按能源类型、按归属对象分组

**输出**

- 📈 能耗趋势图（折线，可切换能源类型）
- 📊 同比 / 环比对比（本月 vs 上月、本月 vs 去年同月）
- 🥧 能源结构构成图（各能源类型占比）
- 🏆 Top 能耗排名（哪条产线/哪个设备最耗能）
- 📐 单位产品能耗（能耗总量 ÷ 产量，需你填入产量）
- 💾 导出：Excel 明细表 + 图表 PNG

---

### 4.2 模块二 · 物质使用分析

**输入**：物料清单 / BOM / 领用记录（Excel / CSV 为主）

| 字段 | 说明 | 示例 |
|---|---|---|
| 日期 | 统计周期 | 2026-07 |
| 物料名称 | | 冷轧钢板 |
| 物料分类 | 原材料/辅料/包装/废弃物 | 原材料 |
| 理论用量 | BOM 标准用量（可空） | 2.30 |
| 实际用量 | 实际领用量 | 2.50 |
| 单位 | | t |
| 归属对象 | 产品/产线（可空） | 产品A |

**处理**

1. 按物料分类、按归属对象汇总
2. **损耗率计算**：`(实际用量 − 理论用量) ÷ 理论用量 × 100%`（理论用量为空时跳过该行）
3. 损耗异常识别：损耗率超过设定阈值（默认 10%，可调）的行标红提示

**输出**

- 📊 物料用量排名（用得最多的物料）
- 🥧 物料分类构成图（原料/辅料/包装/废弃物占比）
- ⚠️ 损耗率排行 + 异常清单
- 📈 单物料用量趋势
- 💾 导出：Excel 明细表 + 图表 PNG

---

### 4.3 模块三 · 碳排放分析

这个模块提供**两条并行的数据录入路径**，你可以只用其中一条，也可以两条混用。

#### 路径 A · 直接导入已有碳数据（默认路径）

供应商的碳足迹报告、第三方核算结果里已经算好了碳排放数字，直接提取进来。

| 字段 | 说明 | 示例 |
|---|---|---|
| 日期 | | 2026-07 |
| 排放源名称 | | 外购电力 |
| 排放范围 | Scope 1 / 2 / 3（可空） | Scope 2 |
| 排放量 | 数字 | 6.84 |
| 单位 | kgCO₂e / tCO₂e | tCO₂e |
| 数据来源 | 文件名或说明 | 供应商A_碳足迹报告.pdf |

#### 路径 B · 用内置因子表换算（选配工具）

如果你手上只有消耗量、没有现成的碳数字，可以用这条路径：

```
选一条消耗量记录（可来自模块一/模块二，也可手工输入）
        ↓
从内置因子表里选一个匹配的排放因子
        ↓
排放量 = 消耗量 × 排放因子
        ↓
结果写入碳排放记录表，并记录用了哪个因子、哪个版本
```

> ⚠️ **重要**：路径 B 是**手动触发的辅助工具**，不是自动流水线。平台不会在你导入能耗数据时偷偷帮你算碳——必须你主动点「用因子换算」才会执行。这样做的原因是：排放因子的选择需要专业判断（地区、年份、口径都会影响结果），不能自动化。

**处理**

1. 统一单位到 tCO₂e
2. 按排放源、按范围（Scope 1/2/3）汇总
3. 记录每条数据的来源路径（A 还是 B）和依据

**输出**

- 📊 碳排放总量卡片
- 🥧 排放构成图（按排放源 / 按 Scope）
- 📈 排放趋势图
- 🔍 明细表可下钻，每条都能看到来源文件和计算依据
- 💾 导出：Excel 明细表 + 图表 PNG

---

### 4.4 模块四（辅助）· 因子表管理

不算主功能，但必须有。

- 查看内置排放因子表（表格形式）
- **编辑**：修改数值、增加新因子、删除不用的
- **导入**：用你公司自己的因子表 Excel 覆盖内置的
- **导出**：把当前因子表导出备份
- **版本记录**：每次修改自动存一个快照，历史计算结果记录当时用的是哪版，保证可追溯

---

## 5. 文件解析规格

不同格式的解析难度天差地别，这里对每种格式的**可靠程度说实话**，避免你产生不切实际的期待。

| 格式 | 可靠性 | 能做到什么 | 注意事项 |
|---|---|---|---|
| **Excel** (.xlsx/.xls) | 🟢 **高** | 读取指定 sheet 的表格、多 sheet 选择、列名映射、合并单元格基本处理 | 首选格式。表格越规整越好 |
| **CSV / 文本** | 🟢 **高** | 读取分隔符表格、自动识别编码（UTF-8 / GBK） | 同上 |
| **Word** (.docx) | 🟡 **中** | 提取正文中的表格、提取段落文字中的数字 | 格式自由度高时容易漏，**必须人工核对** |
| **PDF**（文本型） | 🟠 **中低** | 提取文本、尝试识别表格结构 | 表格线复杂、跨页表格容易错行，**必须人工核对** |
| **PDF**（扫描件/图片） | 🔴 **不支持** | —— | 需要 OCR，**阶段一不做**，见第 12 节 |

### 统一解析流程（所有格式都走这一套）

```
① 上传文件
      ↓
② 平台解析，抽出候选数据
      ↓
③ 【待确认预览区】 ← 关键环节
   以表格形式展示解析结果
   你可以：逐行修改 / 删除错误行 / 补充缺失值 / 做列名映射
      ↓
④ 点击「确认入库」
      ↓
⑤ 数据写入本地数据库，同时记录来源文件名 + 导入时间
```

**绝不跳过第 ③ 步。** 解析出错是常态，尤其是 PDF 和 Word；让你在入库前看一眼，是保证数据质量最有效的办法。

---

## 6. 数据模型

所有数据存在本地一个 SQLite 数据库文件（`data/app.db`）里。四张核心表：

### 表 1 · `energy_records`（能耗记录）

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | 自动生成 |
| `record_date` | 日期 | 是 | 2026-07-01 | 统计周期 |
| `energy_type` | 文本 | 是 | 电 | 能源类型 |
| `amount` | 小数 | 是 | 12000.0 | 原始消耗量 |
| `unit` | 文本 | 是 | kWh | 原始单位 |
| `amount_std` | 小数 | 是 | 12000.0 | 归一后数值（平台算） |
| `unit_std` | 文本 | 是 | kWh | 归一后单位（平台算） |
| `scope_object` | 文本 | 否 | 一号产线 | 归属对象 |
| `source_file` | 文本 | 是 | 7月电费.xlsx | 来源文件 |
| `imported_at` | 时间 | 是 | 2026-08-21 10:30 | 导入时间 |
| `note` | 文本 | 否 | | 备注 |

### 表 2 · `material_records`（物料记录）

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | 自动生成 |
| `record_date` | 日期 | 是 | 2026-07-01 | |
| `material_name` | 文本 | 是 | 冷轧钢板 | |
| `category` | 文本 | 是 | 原材料 | 原材料/辅料/包装/废弃物 |
| `qty_planned` | 小数 | 否 | 2.30 | 理论用量 |
| `qty_actual` | 小数 | 是 | 2.50 | 实际用量 |
| `unit` | 文本 | 是 | t | |
| `loss_rate` | 小数 | 否 | 8.7 | 损耗率%（平台算） |
| `scope_object` | 文本 | 否 | 产品A | |
| `source_file` | 文本 | 是 | BOM.xlsx | |
| `imported_at` | 时间 | 是 | | |
| `note` | 文本 | 否 | | |

### 表 3 · `carbon_records`（碳排放记录）

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | 自动生成 |
| `record_date` | 日期 | 是 | 2026-07-01 | |
| `source_name` | 文本 | 是 | 外购电力 | 排放源 |
| `scope` | 文本 | 否 | Scope 2 | Scope 1/2/3 |
| `emission` | 小数 | 是 | 6.84 | 原始排放量 |
| `unit` | 文本 | 是 | tCO2e | |
| `emission_std` | 小数 | 是 | 6.84 | 归一到 tCO₂e |
| `input_path` | 文本 | 是 | A | **A**=直接导入，**B**=因子换算 |
| `factor_id` | 整数 | 否 | 3 | 路径 B 时记录用了哪个因子 |
| `factor_version` | 文本 | 否 | v1.0 | 因子表版本 |
| `scope_object` | 文本 | 否 | 产品A | |
| `source_file` | 文本 | 是 | 供应商A报告.pdf | |
| `imported_at` | 时间 | 是 | | |
| `note` | 文本 | 否 | | |

### 表 4 · `emission_factors`（排放因子表）

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 3 | |
| `factor_name` | 文本 | 是 | 电力（华东电网） | |
| `category` | 文本 | 是 | 能源 | 能源 / 材料 |
| `input_unit` | 文本 | 是 | kWh | 消耗量单位 |
| `factor_value` | 小数 | 是 | 0.5703 | 换算系数 |
| `output_unit` | 文本 | 是 | kgCO2e | 结果单位 |
| `data_source` | 文本 | 是 | 生态环境部公布值 | 数据出处 |
| `applicable_year` | 文本 | 否 | 2024 | 适用年份 |
| `version` | 文本 | 是 | v1.0 | |
| `is_builtin` | 布尔 | 是 | 是 | 内置 or 用户添加 |
| `updated_at` | 时间 | 是 | | |

---

## 7. 界面结构

Streamlit 左侧边栏导航 + 主区域内容，共五个页面：

```
┌──────────────┬──────────────────────────────────────┐
│              │                                      │
│  能碳监测平台  │        （主内容区）                    │
│              │                                      │
│  🏠 首页总览  │   当前页面的图表、表格、操作按钮         │
│  ⚡ 用能分析  │                                      │
│  📦 物质使用  │                                      │
│  🌱 碳排放    │                                      │
│  ⚙️ 因子表    │                                      │
│              │                                      │
└──────────────┴──────────────────────────────────────┘
```

| 页面 | 内容 |
|---|---|
| 🏠 **首页总览** | 三个模块的关键数字卡片（本月能耗、本月物料、本月碳排）+ 最近导入记录 + 快捷导入入口 |
| ⚡ **用能分析** | 上传区 → 待确认预览 → 筛选条件（日期/类型/对象）→ 图表区 → 明细表 → 导出 |
| 📦 **物质使用分析** | 同上结构，图表换成物料相关 |
| 🌱 **碳排放分析** | 同上结构，额外有「路径 B 因子换算」的操作面板 |
| ⚙️ **因子表管理** | 因子表格（可直接编辑）+ 导入/导出按钮 + 版本历史 |

**每个分析页统一的从上到下顺序**：`上传 → 确认 → 筛选 → 看图 → 看明细 → 导出`。五个页面结构一致，学会一个就会用全部。

---

# 第三部分 · 技术与工程

## 8. 技术方案决策记录

> 这一节记录「为什么选这个方案、当初还考虑过什么」。以后你或者任何人问「为啥不用 XX 技术」，看这里就有答案。

### 最终决策：渐进式路线（方案 D）

**阶段一** → Python + Streamlit，快速做出能用的版本
**阶段二** → PyInstaller 打包成 Windows / macOS 双击即用的启动包

**决策日期**：2026-08-21 ｜ **决策人**：项目所有者

### 为什么选它

1. **风险最低**：先花小成本做出能跑的东西，用起来发现需求不对可以马上改，不会在错误方向上投入太多
2. **代码零浪费**：阶段二只是给阶段一的代码加一层打包配置，不需要重写
3. **匹配使用者水平**：Streamlit 代码量最少，一个功能页可能只要几十行，零基础也看得懂、改得动
4. **匹配任务性质**：这是**数据分析**工具，Python 在 Excel 解析、表格处理、图表上生态最成熟

### 备选方案对比（留档）

| 维度 | A · Streamlit | B · PySide6 原生 | C · Tauri/Electron + React |
|---|---|---|---|
| **最终形态** | 浏览器打开的本地网页 | 原生桌面窗口 | 现代化桌面客户端 |
| **界面美观度** | 一般，工具感 | 中等，系统原生风格 | 最好，接近商业软件 |
| **代码复杂度** | 最低 ⭐ | 中高（约 A 的 3–5 倍） | 最高 |
| **零基础可维护性** | 最好 ⭐ | 一般 | 最差 |
| **数据分析能力** | 最强（Python 生态）⭐ | 最强（Python 生态）⭐ | 弱，Excel/科学计算要造轮子 |
| **Excel/PDF 解析** | 现成库，成熟 ⭐ | 现成库，成熟 ⭐ | 库少且弱 |
| **跨平台成本** | 零，代码完全一致 ⭐ | 打包需分别在 Win/Mac 上做 | 需分别打包 |
| **打包成安装包** | 需 PyInstaller，中等难度 | 原生支持，但 Mac 版必须有 Mac 机器 | 原生支持 |
| **开发周期** | 最短 ⭐ | 中等偏长 | 最长 |
| **后续加 AI 分析** | 容易 ⭐ | 容易 ⭐ | 别扭 |

**为什么没单独选 A**：A 的最终形态是「浏览器网页」，不够像一个正经软件，也不方便发给别人用。
**为什么没选 B**：界面代码复杂度对零基础使用者不友好，且 macOS 打包必须有 Mac 实机，门槛高。
**为什么没选 C**：需要同时掌握前端 + 后端两套技术栈，学习成本最高；而且它最擅长的「界面漂亮」不是本项目的核心矛盾，本项目的核心矛盾是「把文件里的数据准确捞出来算对」——那是 Python 的主场。

### 其他已确认决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 三级结构形态 | 三个平行模块 | 使用者希望三块数据独立管理，不做强制计算链 |
| 排放因子 | 内置表 + 可编辑 | 开箱即用，同时允许按公司实际数据校准 |
| 因子换算 | 手动触发的选配工具（路径 B） | 因子选择需专业判断，不能自动化 |
| 数据存储 | 本地 SQLite | Python 标准库自带，零安装，单文件好备份 |
| 部署方式 | 纯单机，不联网 | 数据安全，且无服务器成本 |
| 支持格式 | Excel / CSV / PDF / Word | 使用者实际手头的文件类型 |

---

## 9. 技术栈清单

| 依赖 | 版本要求 | 它负责干什么（大白话） |
|---|---|---|
| **Python** | 3.11 或更高 | 编程语言本身，是这个软件的地基 |
| **streamlit** | ≥1.30 | 把 Python 代码变成网页界面。你看到的按钮、图表、上传框都靠它 |
| **pandas** | ≥2.0 | 处理表格数据的核心工具，相当于「代码版的 Excel」 |
| **openpyxl** | ≥3.1 | 读写 .xlsx 文件 |
| **xlrd** | ≥2.0 | 读老格式的 .xls 文件 |
| **python-docx** | ≥1.1 | 读 Word (.docx) 文件里的文字和表格 |
| **pdfplumber** | ≥0.11 | 读 PDF 里的文字和表格 |
| **plotly** | ≥5.18 | 画交互式图表（能用鼠标悬停看数值那种） |
| **SQLite** | Python 自带 | 本地数据库，不用单独安装 |
| **PyInstaller** | ≥6.0 | **阶段三才用**：把程序打包成双击就能开的启动包 |

**为什么不用更多库**：依赖越少，安装越不容易出错，对零基础使用者越友好。上面每个库都是必需的，没有可以砍掉的。

---

## 10. 目录结构

```
LBY97/
├── CLAUDE.md                  # 本文档（产品说明书 + 项目宪法）
├── README.md                  # 怎么装、怎么启动
├── requirements.txt           # 依赖清单，装环境时用
├── .gitignore                 # 不提交到仓库的文件（数据库、缓存等）
│
├── app.py                     # 程序入口，运行它就启动平台
│
├── pages/                     # 各个功能页面（Streamlit 自动读这个文件夹）
│   ├── 1_用能分析.py
│   ├── 2_物质使用分析.py
│   ├── 3_碳排放分析.py
│   └── 4_因子表管理.py
│
├── core/                      # 核心逻辑，界面代码调用它
│   ├── __init__.py
│   ├── db.py                  # 数据库读写
│   ├── models.py              # 四张表的结构定义
│   ├── parsers/               # 文件解析
│   │   ├── excel_parser.py
│   │   ├── pdf_parser.py
│   │   └── docx_parser.py
│   ├── units.py               # 单位换算
│   ├── calculator.py          # 损耗率、碳换算等计算
│   ├── charts.py              # 图表生成
│   └── exporter.py            # 导出 Excel / 图片
│
├── data/
│   ├── factors.csv            # 内置排放因子表（可编辑）
│   ├── app.db                 # 本地数据库（自动生成，不进 Git）
│   └── factor_versions/       # 因子表历史版本快照
│
├── samples/                   # 示例文件，用来测试解析功能
│   ├── 示例_能耗台账.xlsx
│   ├── 示例_物料清单.xlsx
│   └── 示例_碳足迹报告.pdf
│
└── tests/                     # 自动化测试
    └── test_parsers.py
```

**当前进度**：只有 `CLAUDE.md` 和 `README.md`，其余是待建蓝图。

---

## 11. 开发路线图

每个阶段都标注了「做完之后你能干什么」，方便你验收。

### ✅ 阶段零 · 产品文档（已完成）

**产出**：`CLAUDE.md`、`README.md`
**你能干什么**：明确知道要做什么、怎么做；以后跟 Claude Code 说「按 CLAUDE.md 做」即可

---

### 🔨 阶段一 · MVP 最小可用版本（下一步）

**做什么**
1. 搭起 Streamlit 框架和五个页面骨架
2. 建立 SQLite 数据库和四张表
3. **Excel / CSV 导入**（含列名映射 + 待确认预览区）
4. 三个模块的基础汇总和图表
5. 因子表管理页（查看 / 编辑 / 导入 / 导出）
6. 导出 Excel 明细

**做完之后你能干什么**
> 把手头的能耗 Excel 和物料 Excel 导进去，在浏览器里看到自动生成的趋势图、构成图、Top 排名，并导出整理好的汇总表。**这时候平台已经能替代你的手工汇总工作了。**

**验收标准**：拿一份真实的能耗 Excel 走完「上传 → 确认 → 看图 → 导出」全流程不报错，数字对得上。

---

### 🔨 阶段二 · 文件解析增强 + 报告导出

**做什么**
1. PDF 解析（文本型）
2. Word 解析
3. 碳排放路径 B（因子换算面板）
4. 图表增强：同比环比、单位产品能耗、损耗异常提示
5. 一键导出图文报告

**做完之后你能干什么**
> 供应商发来的 PDF 碳足迹报告可以直接拖进来提数；只有消耗量没有碳数据时能用因子表算出来；一键导出一份带图带表的完整分析报告。

---

### 🔨 阶段三 · 打包分发

**做什么**
1. PyInstaller 打包配置
2. Windows 启动包（.exe 启动器）
3. macOS 启动包（.app）
4. 使用手册

**做完之后你能干什么**
> 双击图标就打开平台，不用装 Python、不用敲命令；也可以把这个包发给同事，对方解压双击就能用。

---

### 💭 阶段四 · 可选增强（远期，视需要）

- 扫描件 PDF 的 OCR 识别
- 自定义报告模板
- 数据对比：多产品 / 多期间横向对比
- AI 辅助：自动识别表头、自动匹配排放因子

---

## 12. 已知限制与不做清单

诚实列出来，避免以后产生误解。

| 项目 | 状态 | 原因 |
|---|---|---|
| 扫描件 / 图片型 PDF | ❌ 阶段一到三都不做 | 需要 OCR 引擎，额外依赖重、准确率不稳定。列入阶段四可选 |
| 多人协作 / 共享数据库 | ❌ 不做 | 使用者明确选择单机使用；做这个需要服务器、账号、权限体系，成本剧增 |
| 云端同步 / 在线版 | ❌ 不做 | 同上，且与「数据不出本机」的设计原则冲突 |
| 认证级碳核算报告 | ❌ 不做 | 平台输出仅供内部分析。ISO 14064/14067、CBAM 等需第三方核证机构按其规程出具 |
| 自动判断数据正确性 | ❌ 不做 | 平台只做整理和计算，数据质量由使用者把关 |
| 复杂 Excel（多层合并表头、公式引用外部文件） | ⚠️ 有限支持 | 尽力解析，解析不了会在预览区提示，需人工调整源文件 |
| 排放因子的权威性 | ⚠️ 需自行校准 | 内置因子为**参考值**，实际使用前必须按你所在地区、年份、行业的官方数据核对更新 |

---

# 第四部分 · 协作约定

## 13. 常用命令

> ⚠️ 阶段一代码还没写，这些命令**要等阶段一完成后才能用**。先放在这里备查。

### Windows（在 PowerShell 或 CMD 里执行）

```powershell
# 第一次使用：创建独立环境并安装依赖
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 每次启动平台
.venv\Scripts\activate
streamlit run app.py

# 停止平台
# 在命令窗口按 Ctrl + C
```

### macOS（在「终端」里执行）

```bash
# 第一次使用：创建独立环境并安装依赖
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 每次启动平台
source .venv/bin/activate
streamlit run app.py

# 停止平台
# 在终端窗口按 Control + C
```

启动成功后浏览器会自动打开 `http://localhost:8501`，看到界面就说明跑起来了。

### 常见问题速查

| 现象 | 怎么办 |
|---|---|
| 提示 `python 不是内部或外部命令` | Python 没装或没勾选「Add to PATH」，重装 Python 时勾上这个选项 |
| 提示 `ModuleNotFoundError` | 依赖没装全，重新跑一遍 `pip install -r requirements.txt` |
| 浏览器没自动打开 | 手动在浏览器地址栏输入 `http://localhost:8501` |
| 提示端口被占用 | 换个端口：`streamlit run app.py --server.port 8502` |
| Mac 提示「无法验证开发者」 | 阶段三打包版才会遇到。系统设置 → 隐私与安全性 → 点「仍要打开」 |

---

## 14. 给 Claude Code 的工作约定

> 这一节是给 AI 助手看的规则。以后每次会话它都会自动读到。

### 语言与命名

- **所有面向用户的文字用中文**：界面文案、提示信息、错误提示、报告标题
- **代码注释用中文**，让零基础的项目所有者能看懂
- **变量名、函数名、文件名用英文**（`pages/` 下的页面文件名例外，用中文以便侧边栏显示中文）
- 术语统一使用第 3 节名词解释里的表述，不要自创同义词

### 开发原则

1. **先跑通，再优化**：每个功能先做出能用的最简版本，验证没问题后再改进。不要一上来就写复杂抽象
2. **小步交付**：一次只做一个功能点，做完让项目所有者能立刻看到效果
3. **不静默失败**：解析出错、数据缺失，一律在界面上明确提示用户，绝不悄悄跳过或填默认值
4. **保持依赖精简**：不要为了一个小功能引入新的第三方库。确需新增时，先说明理由
5. **任何依赖变更必须同步更新 `requirements.txt`**

### 数据相关的硬性规则

- **不联网**：不引入任何需要联网、需要 API Key、需要注册的服务
- **解析结果必须经用户确认才入库**（第 5 节的第 ③ 步），不允许直接写库
- **每条入库数据必须记录 `source_file` 和 `imported_at`**，保证可追溯
- **修改因子表逻辑时必须保留版本快照**，已有的历史计算结果不能因为因子改动而被静默改变
- **不要删除或覆盖 `data/app.db`**，那是用户的真实数据

### 跨平台注意事项

- 文件路径一律用 `pathlib.Path`，不要手写 `\` 或 `/`
- 读取 CSV 时要处理 UTF-8 和 GBK 两种编码（中文 Windows 常见 GBK）
- 不使用任何仅 Windows 或仅 macOS 可用的库

### 沟通方式

项目所有者是**编程零基础**的初学者：

- 解释技术问题时用大白话和类比，避免堆术语
- 给出的命令要能**直接复制粘贴执行**，并说明在哪里执行
- 报错时先说「这是什么意思」，再说「怎么解决」
- 涉及技术选型的决策，列出选项和优劣，让项目所有者拍板，并把结论补记到第 8 节

---

# 附录 A · 内置排放因子表初始条目

> 🔴 **重要声明**：下表数值为**参考值**，用于让平台开箱即用，**不构成任何权威依据**。
> 正式使用前，请务必按你所在**地区、年份、行业**的官方发布数据核对更新。
> 因子表可在「⚙️ 因子表管理」页随时编辑。

### A.1 能源类因子

| 因子名称 | 消耗量单位 | 参考数值 | 结果单位 | 数据来源类型 | 备注 |
|---|---|---|---|---|---|
| 电力（全国电网平均） | kWh | 0.5703 | kgCO2e | 生态环境部公布值 | 需按最新年度值更新 |
| 电力（区域电网） | kWh | 按区域填 | kgCO2e | 区域电网因子 | 华北/东北/华东/华中/西北/南方各不同，需自行填入 |
| 天然气 | m³ | 2.1622 | kgCO2e | IPCC 缺省值折算 | |
| 柴油 | L | 2.6300 | kgCO2e | IPCC 缺省值折算 | |
| 汽油 | L | 2.3000 | kgCO2e | IPCC 缺省值折算 | |
| 液化石油气 LPG | kg | 3.1000 | kgCO2e | IPCC 缺省值折算 | |
| 原煤 | kg | 1.9800 | kgCO2e | IPCC 缺省值折算 | 随煤种差异大 |
| 外购蒸汽 | GJ | 110.0000 | kgCO2e | 行业经验值 | 随供热源差异极大，务必替换 |
| 自来水 | t | 0.3000 | kgCO2e | 行业经验值 | |

### A.2 材料类因子（隐含碳 / 摇篮到大门）

| 因子名称 | 消耗量单位 | 参考数值 | 结果单位 | 数据来源类型 | 备注 |
|---|---|---|---|---|---|
| 钢材（粗钢，高炉工艺） | t | 2100.0 | kgCO2e | 行业平均 | 电炉短流程显著更低 |
| 钢材（电炉短流程） | t | 700.0 | kgCO2e | 行业平均 | |
| 铝材（原生铝） | t | 16000.0 | kgCO2e | 行业平均 | 与电力结构强相关 |
| 铝材（再生铝） | t | 700.0 | kgCO2e | 行业平均 | |
| 铜材（原生） | t | 3800.0 | kgCO2e | 行业平均 | |
| 塑料 PE | t | 2000.0 | kgCO2e | 行业平均 | |
| 塑料 PP | t | 1900.0 | kgCO2e | 行业平均 | |
| 塑料 PVC | t | 2400.0 | kgCO2e | 行业平均 | |
| 纸包装（瓦楞纸） | t | 900.0 | kgCO2e | 行业平均 | 再生比例影响大 |
| 玻璃 | t | 1200.0 | kgCO2e | 行业平均 | |
| 水泥 | t | 750.0 | kgCO2e | 行业平均 | |

### A.3 单位换算参考（平台内部使用）

| 换算关系 | 系数 |
|---|---|
| 1 tCO₂e | = 1000 kgCO₂e |
| 1 kWh | = 3.6 MJ |
| 1 GJ | = 1000 MJ |
| 1 tce（吨标准煤） | = 29.3076 GJ |
| 1 kWh 电 | ≈ 0.0001229 tce（当量值，按等价值折算另有系数） |
| 1 m³ 天然气 | ≈ 38.93 MJ |

---

**文档结束** ｜ 有任何需求变更，请直接修改本文件，它是这个项目的唯一事实来源。
