# 能碳监测平台 · 产品文档

> **面向智算中心的低碳算力能碳管理平台**
>
> 本文件有两个作用：
> 1. **给你看**——这是产品说明书，讲清楚这个软件要做成什么样。
> 2. **给 Claude Code 看**——每次打开 Claude Code 会自动读这个文件。以后你不用重复解释需求，直接说「按 CLAUDE.md 做」。
>
> 文档版本：**v2.0** ｜ 状态：产品定义已定稿，尚未开始编码

---

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-21 | 初版。定位为通用工厂能碳分析工具（电表/物料台账 → 能耗、损耗、碳排） |
| **v2.0** | **2026-09-01** | **方向性重构**。分析对象从「厂房里的电和料」改为「智算中心的算力」；核心从「整理数据」改为「Agent 自动识别与测算」；新增能源成本与碳资产分析；新增低碳算力工作流 |

### v2.0 相对 v1.0 的四处关键变化

1. **分析对象变了**：能源+物料 → **算力**（IT 设备、制冷、供配电，园区级）
2. **核心能力变了**：被动整理数据 → **大模型驱动的 Agent 主动识别、归因、给建议**
3. **碳资产从「不做」变成「要做」**：v1.0 明确写了不做碳资产管理，v2.0 把碳配额、绿电绿证、履约缺口纳入核心功能
4. **「不联网」原则被替换**：Agent 必须调用云端大模型 API，原「数据不出本机」无法成立。改为更精确的 **[数据边界原则](#15-核心设计原则)**——原始明细留本地，只有聚合摘要出网

---

## 目录

- [第一部分 · 产品定义](#第一部分--产品定义)
  - [1. 产品概述](#1-产品概述)
  - [2. 目标用户与典型场景](#2-目标用户与典型场景)
  - [3. 名词解释（零基础必读）](#3-名词解释零基础必读)
- [第二部分 · 功能规格](#第二部分--功能规格)
  - [4. 总体架构](#4-总体架构)
  - [5. 模块一 · 数据接入](#5-模块一--数据接入)
  - [6. 模块二 · 高能耗阶段识别](#6-模块二--高能耗阶段识别)
  - [7. 模块三 · 算力碳足迹测算](#7-模块三--算力碳足迹测算)
  - [8. 模块四 · 能源成本与碳资产分析](#8-模块四--能源成本与碳资产分析)
  - [9. 模块五 · 低碳算力能碳管理工作流](#9-模块五--低碳算力能碳管理工作流)
  - [10. Agent 设计规格](#10-agent-设计规格)
  - [11. 因子表与电价表管理](#11-因子表与电价表管理)
  - [12. 数据模型](#12-数据模型)
  - [13. 界面结构](#13-界面结构)
- [第三部分 · 技术与工程](#第三部分--技术与工程)
  - [14. 技术方案决策记录](#14-技术方案决策记录)
  - [15. 技术栈清单](#15-技术栈清单)
  - [16. 目录结构](#16-目录结构)
  - [17. 开发路线图](#17-开发路线图)
  - [18. 已知限制与不做清单](#18-已知限制与不做清单)
- [第四部分 · 协作约定](#第四部分--协作约定)
  - [19. 常用命令](#19-常用命令)
  - [20. 给 Claude Code 的工作约定](#20-给-claude-code-的工作约定)
- [附录 A · 排放因子初始条目](#附录-a--排放因子初始条目)
- [附录 B · 核心公式与单位换算](#附录-b--核心公式与单位换算)

---

# 第一部分 · 产品定义

## 1. 产品概述

### 1.1 一句话定位

**能碳监测平台**是一个跑在你自己电脑上的智算中心能碳分析工具：接入机房的用电台账、硬件功耗和任务日志，由一个 **Agent** 自动找出高能耗阶段、测算算力的碳足迹、分析能源成本和碳资产状况，最终形成一套可复用的**低碳算力能碳管理工作流**。

### 1.2 它解决什么问题

| 你现在的痛点 | 平台怎么解决 |
|---|---|
| 只知道机房总电费，不知道哪个环节、哪段时间在浪费 | Agent 自动切分并识别高能耗阶段，指出是 IT 负载、制冷还是供配电的问题 |
| 说不清「跑这批任务到底排了多少碳」 | 按任务/时段/设备三个维度测算碳足迹，可下钻到计算依据 |
| 电费账单和碳排放是两本账，没法一起看 | 成本与碳双维对齐到同一时间轴，同一度电既看钱也看碳 |
| 碳配额、绿电、绿证散在各处，履约缺口靠手算 | 碳资产台账 + 自动测算履约缺口 |
| 分析结论靠人写，每月重复劳动 | Agent 出结论和减碳建议，一键生成报告 |
| 好不容易理出的分析方法，换个人就失传 | 固化成工作流，每月照着跑 |

### 1.3 它**不**解决什么问题（重要，先说清楚）

- ❌ **不做碳交易**——碳配额、绿证只做**台账和测算**，不接交易所、不做撮合、不做资产定价
- ❌ **不做实时调度**——平台给建议，不直接控制算力调度或制冷系统。执行由你和运维决定
- ❌ **不出具认证级报告**——输出仅供内部分析，不能直接用于 ISO 14064 / ISO 14067 / GHG Protocol 第三方核证提交
- ❌ **不做多人协作**——单机个人使用，没有账号、权限、共享数据库
- ❌ **不替你判断数据对错**——平台负责测算和分析，数据质量由你把关
- ⚠️ **不是「满足服务质量约束下的成本-碳协同优化」**——本平台不做带约束的联合优化求解，它做的是**测算、识别、归因、建议**

### 1.4 三条主线

整个平台围绕三件事，其余功能都是为它们服务的：

```
① 算力用了多少能、排了多少碳  →  测得准
② 哪个阶段最费能、为什么      →  找得出
③ 花了多少钱、碳资产够不够    →  算得清
                ↓
        固化成可复用的工作流
```

### 1.5 核心设计原则

1. **数据边界优先**（替代 v1.0 的「不联网」）
   - 原始明细数据（逐条功耗记录、设备序列号、任务名、机房位置）**只存在本地 SQLite**
   - 发给大模型 API 的**只有聚合统计量**（如「7月3日 14:00-18:00 平均功率 420kW，较基线高 38%」）
   - 每次调用 API 前，界面**必须展示将要发送的完整内容**，你确认后才发出
   - 提供**纯本地模式开关**：关掉 Agent 后，测算、图表、导出全部照常可用，只是没有智能分析
2. **不静默采信**：所有解析和采集来的数据，先进「待确认」区，你确认后才入库
3. **可追溯**：每条数据记录来源和时间；每次碳测算记录用了哪版因子；每条 Agent 结论记录基于哪些数据
4. **Agent 的话不是结论**：Agent 输出一律标注为「AI 分析建议」，与平台确定性计算的结果在界面上**视觉区分**，不混为一谈
5. **跨平台**：同一套代码，Windows 和 macOS 都能跑

---

## 2. 目标用户与典型场景

### 目标用户

智算中心 / 数据中心园区里负责能源管理、双碳、运维或成本分析的个人。**不需要懂编程**，会用 Excel 就行。

### 典型场景一 · 月度能碳复盘

> 月初把上月的机房用电台账、UPS/制冷分项电表、任务调度日志导进平台 → 平台自动测算全月碳足迹 → 打开「高能耗阶段识别」页，Agent 指出「7 月 12-15 日夜间制冷电耗异常，PUE 从 1.28 升到 1.51，同期 IT 负载并未上升，疑似冷机策略问题」→ 你核实后采纳 → 导出月度报告。

### 典型场景二 · 单任务碳足迹核算

> 业务方问「我们训练那个模型排了多少碳」→ 在「碳足迹测算」页按任务 ID 筛选 → 平台算出该任务占用的卡时、折算 IT 电耗、乘 PUE 和区域电网因子，再摊上硬件隐含碳 → 得到该任务的完整碳足迹，可下钻看每一步的计算依据。

### 典型场景三 · 履约缺口盘点

> 年中盘点：导入碳配额台账和绿电采购合同 → 平台算出「已核算排放 12,400 tCO₂e，免费配额 9,800 t，绿电抵扣 1,200 t，**履约缺口 1,400 t**」→ Agent 分析缺口来源并给出「把 X 类任务迁到夜间低碳时段」等建议 → 导出盘点表。

---

## 3. 名词解释（零基础必读）

后面章节会反复用这些词，先讲明白。

### 3.1 算力与机房

| 名词 | 大白话解释 |
|---|---|
| **算力** | 计算能力。这里具体指机房里的服务器、GPU 卡在跑计算任务。 |
| **智算中心** | 专门跑 AI 计算的数据中心，以 GPU 服务器为主，功率密度比普通机房高得多。 |
| **IT 负载 / IT 电耗** | 服务器、GPU、存储、网络设备本身用掉的电。**不含**制冷和供配电损耗。 |
| **PUE** | 电能利用效率 = 机房总电耗 ÷ IT 电耗。<br>例：PUE=1.3 表示 IT 每用 1 度电，机房总共要用 1.3 度（多出的 0.3 度是制冷、UPS 损耗等）。**越接近 1 越好。** |
| **卡时** | 一张 GPU 卡运行一小时 = 1 卡时。衡量任务用了多少算力的常用单位。 |
| **负载率** | 设备实际功率 ÷ 额定功率。反映设备用得满不满。 |

### 3.2 能与碳

| 名词 | 大白话解释 |
|---|---|
| **活动数据** | 「消耗了多少」的原始数字（用了多少度电、跑了多少卡时），是所有计算的起点。 |
| **排放因子** | 换算系数，把「消耗量」换算成「碳排放量」。<br>例：用 1 度电排 0.57 公斤 CO₂ → 0.57 就是电力排放因子。 |
| **碳足迹** | 一件事或一个产品从头到尾一共排放了多少温室气体。 |
| **tCO₂e** | 吨二氧化碳当量。温室气体不止 CO₂ 一种，统一折算成「相当于多少吨 CO₂」来比较。<br>1 tCO₂e = 1000 kgCO₂e |
| **运营碳排** | 机房**运行时用电**产生的碳排放。占大头。 |
| **隐含碳** | 服务器、GPU 这些**硬件在生产制造时**就已经排放的碳。按设备寿命摊到每年/每个任务上。 |
| **Scope 1（范围一）** | 你自己烧的。机房场景主要是柴油发电机、备用锅炉。 |
| **Scope 2（范围二）** | 你买的能源。机房场景的**绝对大头**——外购电力。 |
| **Scope 3（范围三）** | 上下游的。机房场景主要是硬件隐含碳、上游燃料开采运输。 |
| **location-based（地域法）** | 用你所在**电网的平均排放因子**算碳排。反映物理现实。 |
| **market-based（市场法）** | 扣掉你买的**绿电/绿证**之后再算碳排。反映采购选择。<br>⚠️ 两种算法结果不同，报告里必须**同时给出并注明用的哪种**。 |

### 3.3 成本与碳资产

| 名词 | 大白话解释 |
|---|---|
| **分时电价** | 电价按时段不同。一般分尖峰/高峰/平段/低谷四档，谷段可能只有峰段的三分之一。 |
| **需量电费** | 按你**用电的最高功率**收的固定费用，和用了多少度电无关。功率冲高一次，整月都要按那个数交钱。 |
| **碳配额（CEA）** | 政府免费或有偿分配给你的「允许排放量」。排放超了要买，省下了可以卖。 |
| **绿电** | 直接采购的风电、光伏等零碳电力。 |
| **绿证（GEC）** | 绿色电力证书。买了证书就相当于声明用了对应的绿电（market-based 下可抵扣）。 |
| **CCER** | 国家核证自愿减排量。可用来抵销一部分排放。 |
| **履约缺口** | 核算排放 − 可用配额 − 各类抵销量。**大于零就是要花钱补的窟窿。** |

### 3.4 Agent 相关

| 名词 | 大白话解释 |
|---|---|
| **Agent（智能体）** | 一个能自己看数据、自己判断、自己给结论的 AI 助手。不是简单的问答，它会主动调用平台的功能去查数、算数，再综合出结论。 |
| **工具（Tool）** | Agent 能调用的功能，比如「查某段时间的能耗」「跑一次碳测算」。工具全部在**你本地运行**，Agent 只拿到返回结果。 |
| **数据边界** | 明确规定哪些数据可以发给云端模型、哪些绝对不行的一条线。见 [1.5 核心设计原则](#15-核心设计原则)。 |

---

# 第二部分 · 功能规格

## 4. 总体架构

四层结构，从下往上。**关键：Agent 层在测算层之上，它调用工具拿聚合结果，碰不到原始数据。**

```
┌──────────────────────────────────────────────────────────┐
│  ④ 应用层        页面 / 图表 / 报告导出 / 工作流           │
├──────────────────────────────────────────────────────────┤
│  ③ Agent 层      识别高能耗 · 归因 · 给建议 · 写报告        │
│                  ↕ 只传聚合摘要                            │
│                  ┄┄┄┄┄ 数据边界 ┄┄┄┄┄  ← 云端 API 到此为止  │
├──────────────────────────────────────────────────────────┤
│  ② 测算层        碳足迹测算 · 高能耗分段 · 成本 · 碳资产    │
│                  （确定性计算，全部本地，不依赖 Agent）      │
├──────────────────────────────────────────────────────────┤
│  ① 数据层        文件导入 · 实时采集 · 任务日志             │
│                  → 本地 SQLite (data/app.db)              │
└──────────────────────────────────────────────────────────┘
```

**这个分层带来两个重要性质：**

1. **关掉 Agent，平台照样能用。**②③④ 层中只有 ③ 依赖联网，把它关掉后测算、图表、导出全部正常。
2. **测算结果是确定的，Agent 结论是建议。**碳足迹数字由 ② 层按公式算死，同样输入永远同样输出；Agent 的话是解读，可能不同次不一样，界面上必须区分开。

---

## 5. 模块一 · 数据接入

三个来源，可以只用其中一个，也可以三个都用。

### 5.1 来源 A · 文件导入

| 格式 | 可靠性 | 能做到什么 | 注意事项 |
|---|---|---|---|
| **Excel** (.xlsx/.xls) | 🟢 **高** | 读取指定 sheet、多 sheet 选择、列名映射 | 首选格式 |
| **CSV / 文本** | 🟢 **高** | 分隔符表格、自动识别 UTF-8 / GBK 编码 | 同上 |
| **Word** (.docx) | 🟡 **中** | 提取正文表格、段落中的数字 | 格式自由时易漏，**必须人工核对** |
| **PDF**（文本型） | 🟠 **中低** | 提取文本、尝试识别表格 | 跨页表格易错行，**必须人工核对** |
| **PDF**（扫描件） | 🔴 **不支持** | —— | 需 OCR，见[第 18 节](#18-已知限制与不做清单) |

**典型文件**：机房月度用电台账、分项电表报表（IT/制冷/照明）、服务器资产清单、电费账单、绿电采购合同、碳配额分配通知。

### 5.2 来源 B · 实时采集硬件数据

从机器上直接读功耗，数据最准。

| 采集对象 | 采集方式 | 拿到什么 |
|---|---|---|
| GPU | NVML（`pynvml`，即 `nvidia-smi` 的底层库） | 每张卡的实时功率(W)、利用率、显存、温度 |
| 服务器整机 | IPMI / Redfish | 整机功率、进出风温度 |
| 机房级 | 电表 / 动环系统导出 | 总电耗、制冷电耗、UPS 电耗 → 算 PUE |

**采样策略**：默认 60 秒采一次，写入本地库。可调。长期数据自动降采样（原始明细保留 30 天，之后按 5 分钟均值归档），避免数据库无限膨胀。

> ⚠️ **前提**：这条路需要平台能访问到目标机器（同一内网、有权限）。如果拿不到，就用来源 A 的台账文件，只是粒度粗一些。

### 5.3 来源 C · 任务日志

把能耗对应到**具体任务**，这是算「单个任务碳足迹」的前提。

| 字段 | 说明 | 示例 |
|---|---|---|
| 任务 ID | 唯一标识 | job-20260703-001 |
| 任务名称 | 可脱敏 | 模型A训练 |
| 任务类型 | 训练 / 微调 / 推理 / 数据处理 | 训练 |
| 起止时间 | | 2026-07-03 09:00 ~ 07-08 17:30 |
| 占用节点 / 卡数 | | node-12~15，32 卡 |
| 归属对象 | 项目 / 团队 / 产品 | 视觉组 |

**来源**：Slurm / K8s / 自研调度系统导出的 CSV，或手工填写。

### 5.4 统一接入流程（三个来源都走这一套）

```
① 数据进来（上传文件 / 采集器写入 / 导入日志）
      ↓
② 平台解析，抽出候选数据
      ↓
③ 【待确认预览区】 ← 关键环节，绝不跳过
   表格展示解析结果，你可以逐行改 / 删错行 / 补缺失 / 做列名映射
      ↓
④ 点击「确认入库」
      ↓
⑤ 写入本地数据库，记录来源 + 时间
```

> 实时采集数据量大，不逐条确认，改为**批次确认**：每批采集完展示统计摘要（条数、时间范围、功率区间、异常值个数），你确认后整批入库。

---

## 6. 模块二 · 高能耗阶段识别

回答「**哪段时间在费能、费在哪个环节**」。

### 6.1 什么叫「高能耗阶段」

一段**持续的、显著高于基线**的时间区间。三个要素缺一不可：持续时长、超出幅度、所在环节。

### 6.2 识别方法（本地确定性算法，不依赖 Agent）

分两层，先粗后细：

**第一层 · 阈值分段**（默认方法，简单可解释）
1. 算基线：取所选时间窗内功率的中位数，或用户手工指定
2. 标记：连续超过「基线 × 阈值系数」（默认 1.2）且持续超过最短时长（默认 30 分钟）的区间
3. 合并：间隔小于 10 分钟的相邻区间合并为一段

**第二层 · 变点检测**（可选，用于趋势突变）
- 用滑动窗口比较前后两段均值，标记显著变化点
- 适合发现「PUE 从某天起整体抬升」这类缓变问题

> 所有阈值参数都在界面上**可调且显示当前取值**，不做隐藏的魔法数字。

### 6.3 归因维度

识别出高能耗时段后，从四个维度拆解「费在哪」：

| 维度 | 回答什么问题 | 输出 |
|---|---|---|
| **按环节** | 是 IT 在跑，还是制冷在费？ | IT / 制冷 / 供配电 / 其他 的电耗构成变化 |
| **按时间** | 集中在哪些时段？ | 24 小时热力图、日/周周期性 |
| **按任务** | 哪些任务撞在这段时间？ | 时段内任务清单 + 各自卡时占比 |
| **按设备** | 哪些节点/卡功率异常？ | 设备功率排名、离群设备标记 |

### 6.4 输出

- 📋 高能耗时段清单（起止、时长、超出幅度、涉及环节）
- 🔥 24小时 × 日期 的功率热力图
- 📊 环节构成变化对比（正常时段 vs 高能耗时段）
- 🏆 高能耗任务 / 设备 Top 排名
- 📈 PUE 趋势曲线 + 异常点标记
- 🤖 **Agent 归因结论**（可选，见[第 10 节](#10-agent-设计规格)）

---

## 7. 模块三 · 算力碳足迹测算

回答「**排了多少碳**」。这是平台最需要「算得准」的部分，全部为确定性计算。

### 7.1 计算链路

```
IT 电耗 E_IT
   ├─ 来源①：实时功率积分   Σ(功率 × 采样间隔)
   ├─ 来源②：卡时估算       卡时 × 单卡平均功率
   └─ 来源③：电表台账直接读
         ↓
机房总电耗 E_total = E_IT × PUE
         ↓
    ┌────┴────┐
    ↓         ↓
运营碳排    隐含碳
（用电）    （硬件摊销）
    ↓         ↓
    └────┬────┘
         ↓
   总碳足迹（tCO₂e）
```

### 7.2 三个核心公式

**① 运营碳排 —— 地域法（location-based）**

```
运营碳排 = E_total × 区域电网排放因子
```

**② 运营碳排 —— 市场法（market-based）**

```
运营碳排 = (E_total − E_绿电) × 区域电网排放因子 + E_绿电 × 绿电因子
```
> 绿电因子通常取 0。**两种算法必须同时输出**，报告中注明依据。

**③ 隐含碳（硬件摊销）**

```
隐含碳 = Σ [ 设备隐含碳总量 × (设备本次使用时长 ÷ 设备设计寿命) ]
```
> 例：一台隐含碳 3000 kgCO₂e、寿命 5 年的 GPU 服务器，某任务用了它 100 小时
> → 摊到该任务：3000 × (100 ÷ 43800) ≈ 6.85 kgCO₂e

完整公式和单位换算见[附录 B](#附录-b--核心公式与单位换算)。

### 7.3 三个测算口径

同一批数据可以按三种口径出结果，各自回答不同问题：

| 口径 | 回答 | 典型用途 |
|---|---|---|
| **按时段** | 这个月/这一天排了多少 | 月度报告、趋势分析 |
| **按任务** | 这个任务/这个模型排了多少 | 对业务方交代、产品碳足迹 |
| **按设备** | 这批机器排了多少 | 设备选型、退役决策 |

### 7.4 Scope 归集

| Scope | 机房场景包含什么 |
|---|---|
| Scope 1 | 柴油发电机、备用锅炉（通常很小） |
| Scope 2 | **外购电力**（绝对大头） |
| Scope 3 | 硬件隐含碳、上游燃料、外购蒸汽/冷 |

### 7.5 输出

- 🎯 碳足迹总量卡片（同时显示地域法 / 市场法两个数）
- 🥧 构成图：按 Scope / 按环节（IT vs 制冷）/ 按运营碳 vs 隐含碳
- 📈 碳排趋势图，可叠加 PUE 曲线
- 🏆 任务碳足迹排名
- 🔍 **明细可下钻**：每条结果都能展开看「用了哪些活动数据 × 哪个因子（哪个版本）= 多少」
- 💾 导出 Excel + 图表 PNG

---

## 8. 模块四 · 能源成本与碳资产分析

回答「**花了多少钱、碳资产够不够**」。

### 8.1 能源成本

**电费构成**

```
总电费 = 电量电费 + 需量电费 + 其他费用
        ↑              ↑
  Σ(各时段电量 × 该时段电价)   按最高需量计收
```

**分时电价表**（可编辑，见[第 11 节](#11-因子表与电价表管理)）

| 时段类型 | 典型时间 | 电价特点 |
|---|---|---|
| 尖峰 | 夏季午后、晚高峰 | 最贵 |
| 高峰 | 白天 | 贵 |
| 平段 | 上午、傍晚 | 中 |
| 低谷 | 夜间 | 最便宜，且**通常碳强度也更低** |

**成本-碳双维分析**（本模块的核心价值）

把同一时间轴上的「度电成本」和「度电碳强度」并排看，找出**又贵又脏**的时段——这类时段是转移负载的首选目标。

| 时段 | 电价 | 碳强度 | 结论 |
|---|---|---|---|
| 尖峰 | 高 | 高 | ⚠️ 优先转移 |
| 低谷 | 低 | 低 | ✅ 优先安排 |
| 高峰 | 高 | 中 | 视情况 |

### 8.2 碳资产台账

**只做台账和测算，不做交易**（见 [1.3](#13-它不解决什么问题重要先说清楚)）。

| 资产类型 | 记录什么 |
|---|---|
| 碳配额（CEA） | 免费分配量、有偿购买量、当前持仓、已履约量 |
| 绿电 | 采购电量、对应时段、合同编号 |
| 绿证（GEC） | 证书数量、对应电量、购买时间 |
| CCER | 持有量、可抵销比例上限 |

**履约缺口测算**

```
履约缺口 = 核算排放量 − 免费配额 − 已购配额 − 绿电抵扣 − CCER抵销
```

> - 结果 **> 0**：缺口，需补足 → 界面标红
> - 结果 **< 0**：盈余
> - CCER 抵销通常有比例上限（如不超过应履约量的 5%），平台按你在设置里填的上限自动截断，并提示截断了多少

### 8.3 输出

- 💰 电费总额卡片 + 分时段构成图
- ⚡ 需量曲线 + 最高需量标记
- 🔀 成本-碳双维散点图（横轴成本、纵轴碳强度，按时段着色）
- 📒 碳资产台账表
- 🎯 履约缺口卡片（缺口时标红 + 缺口来源拆解）
- 💾 导出 Excel + 图表 PNG

---

## 9. 模块五 · 低碳算力能碳管理工作流

前四个模块是能力，这一节把它们串成**每月照着跑的固定动作**。这是平台的最终产出物。

### 9.1 工作流闭环

```
      ┌──────────────────────────────────────┐
      ↓                                      │
  ① 采集    → 导入台账 / 采集功耗 / 导入任务日志
      ↓                                      │
  ② 测算    → 碳足迹（地域法+市场法）+ 成本    │
      ↓                                      │
  ③ 识别    → 高能耗阶段 + 归因               │
      ↓                                      │
  ④ 建议    → Agent 给减碳/降本措施 + 预期收益 │
      ↓                                      │
  ⑤ 执行    → 你和运维去落地（平台不自动执行）  │
      ↓                                      │
  ⑥ 复核    → 下个周期回测，措施到底有没有效 ───┘
```

**⑥ 复核是闭环的关键**，也是最容易被略过的一步。平台会把每条采纳的措施记录下来，下个周期自动对比措施前后的同口径数据，给出「实际减碳量 vs 预期减碳量」。措施有没有用，用数字说话。

### 9.2 工作流的固化形式

- **模板**：内置「月度能碳复盘」「季度履约盘点」两个模板，规定每步做什么、看哪些图、填哪些结论
- **进度**：界面上以清单形式呈现，做完一步勾一步，中断了下次接着做
- **留痕**：每次跑完存成一次「工作流实例」，含当时的数据快照、因子版本、Agent 结论、你的批注
- **复用**：下个月点「新建实例」，自动带上上次的参数配置

### 9.3 措施台账

| 字段 | 说明 |
|---|---|
| 措施描述 | 如「将 X 类训练任务调整至 23:00-07:00 执行」 |
| 提出来源 | Agent 建议 / 人工提出 |
| 预期收益 | 预计减碳量、预计降本金额 |
| 状态 | 待评估 / 已采纳 / 已执行 / 已复核 / 已否决 |
| 执行时间 | |
| 实际收益 | 复核时由平台按同口径回测填入 |

---

## 10. Agent 设计规格

这是 v2.0 的核心新增。**先读完 [1.5 核心设计原则](#15-核心设计原则)的数据边界部分再看本节。**

### 10.1 Agent 是什么、不是什么

| Agent **是** | Agent **不是** |
|---|---|
| 一个会主动调用平台功能查数、算数，再综合出结论的分析助手 | 一个聊天机器人 |
| 高能耗归因、减碳建议、报告初稿的产出者 | 碳足迹数字的计算者（数字由测算层算死） |
| 可以随时关掉的**增强功能** | 平台运行的必需品 |

**一句话**：**数字由代码算，话由 Agent 说。**

### 10.2 Agent 的四项职责

| 职责 | 输入 | 输出 |
|---|---|---|
| **归因** | 高能耗时段的聚合特征 | 「制冷电耗上升而 IT 负载持平，指向冷机策略」 |
| **建议** | 归因结论 + 成本碳双维数据 | 具体措施 + 预期减碳/降本量 + 可行性说明 |
| **报告** | 本期全部聚合结果 | 报告初稿（结论、图表说明、建议清单） |
| **答疑** | 你的自然语言提问 | 调工具查数后回答，并附上依据 |

### 10.3 工具集（Agent 能调用的本地函数）

**所有工具都在你本地执行**，Agent 只收到返回值。工具返回的一律是**聚合结果**，不含原始明细。

| 工具 | 作用 | 返回什么 |
|---|---|---|
| `query_energy_summary` | 查某时间段能耗汇总 | 总电耗、IT/制冷分项、PUE、功率区间统计 |
| `detect_high_load` | 跑高能耗阶段识别 | 时段清单（起止、时长、超出幅度） |
| `calc_carbon` | 跑碳足迹测算 | 碳排总量、构成、用的因子版本 |
| `query_cost` | 查成本 | 分时段电费、需量费、度电成本 |
| `query_carbon_asset` | 查碳资产 | 配额、绿电、绿证、履约缺口 |
| `list_jobs` | 查时段内任务 | 任务数、类型分布、卡时占比（**任务名默认脱敏**） |
| `get_factors` | 查当前因子表 | 因子名称、数值、版本、来源 |
| `compare_periods` | 同口径对比两个时段 | 各指标差异和变化率 |

> **不提供的工具**：没有「读原始记录」「读设备序列号」「执行调度命令」「改数据库」这类工具。Agent **只读、只拿聚合值**。

### 10.4 数据边界的具体执行

**允许出网的**：聚合统计量、时间范围、指标名、因子数值、你在对话框里主动输入的问题。

**禁止出网的**：逐条功耗明细、设备序列号 / IP / 主机名、机房物理位置、原始任务名（默认脱敏为 `job-001` 形式，可在设置里关闭脱敏）、任何文件原文。

**执行机制**：
1. 每次调 API 前，界面弹出 **「本次将发送的内容」** 完整预览，你点确认才发出
2. 可勾选「本会话内不再询问」，但**换一个会话就恢复询问**
3. 所有 API 交互写入 `data/agent_audit.log`，含时间、发送内容摘要、返回内容摘要、token 消耗
4. 设置页有全局开关：**纯本地模式**（关闭 Agent，②④ 层功能全部保留）

### 10.5 技术实现要点

| 项 | 选择 | 说明 |
|---|---|---|
| SDK | `anthropic`（Python 官方 SDK） | 见[第 15 节](#15-技术栈清单) |
| 模型 | `claude-opus-5` | 除非你明确要求换 |
| 循环方式 | Tool Runner（`client.beta.messages.tool_runner`） | 不手写 agent 循环，工具用 `@beta_tool` 装饰器定义 |
| 思考模式 | `thinking={"type": "adaptive"}` | 归因分析属于需要推理的任务 |
| 流式 | 长回答用 `.stream()` + `.get_final_message()` | 避免请求超时 |
| 密钥 | 读环境变量 `ANTHROPIC_API_KEY` | **绝不硬编码，绝不提交进 Git** |

> 详细的编码约定（含容错、fallback）见[第 20 节](#20-给-claude-code-的工作约定)。

### 10.6 Agent 出错时怎么办（不静默失败）

| 情况 | 平台行为 |
|---|---|
| 没配 API Key | 界面明确提示「未配置密钥，Agent 功能不可用，其余功能正常」，不报错崩溃 |
| 网络不通 / API 报错 | 显示具体错误和建议，**测算结果照常展示** |
| Agent 结论与测算数字矛盾 | 以**测算数字为准**，界面提示「AI 结论与计算结果存在出入，请以计算结果为准」 |
| 超出 token 预算 | 提示已用量，询问是否继续 |

### 10.7 界面上的视觉区分（硬性要求）

| 内容类型 | 呈现方式 |
|---|---|
| 平台计算结果 | 常规样式，直接展示 |
| Agent 输出 | **统一加 🤖 图标 + 浅色底框 + 「AI 分析建议，请自行核实」标注** |

---

## 11. 因子表与电价表管理

三张可编辑的基础表，是所有测算的依据。

### 11.1 排放因子表

- 查看 / 编辑 / 新增 / 删除
- 导入公司自己的因子表 Excel 覆盖内置的；导出备份
- **版本快照**：每次修改自动存一版。历史测算结果记录当时用的版本，**因子改动不影响已有结果**
- 初始条目见[附录 A](#附录-a--排放因子初始条目)

### 11.2 分时电价表

| 字段 | 示例 |
|---|---|
| 时段类型 | 尖峰 / 高峰 / 平段 / 低谷 |
| 生效月份 | 7-9月（夏季） |
| 时间区间 | 14:00-17:00 |
| 电价（元/kWh） | 1.2038 |

> 电价随地区、季节、电压等级差异极大，**必须按你的实际电费单填写**。

### 11.3 设备隐含碳表

| 字段 | 示例 |
|---|---|
| 设备型号 | GPU服务器-8卡 |
| 隐含碳总量（kgCO₂e） | 3000 |
| 设计寿命（年） | 5 |
| 数据来源 | 厂商 EPD 报告 |

---

## 12. 数据模型

本地 SQLite（`data/app.db`）。八张表。

### 表 1 · `energy_records`（能耗记录）

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` | 时间 | 是 | 2026-07-03 14:00 | 统计区间起 |
| `ts_end` | 时间 | 是 | 2026-07-03 15:00 | 统计区间止 |
| `segment` | 文本 | 是 | IT | IT / 制冷 / 供配电 / 其他 / 总计 |
| `energy_kwh` | 小数 | 是 | 420.5 | 电耗 |
| `avg_power_kw` | 小数 | 否 | 420.5 | 平均功率 |
| `source_type` | 文本 | 是 | 文件 | 文件 / 采集 / 手工 |
| `device_id` | 文本 | 否 | node-12 | 关联设备 |
| `source_file` | 文本 | 否 | 7月台账.xlsx | |
| `imported_at` | 时间 | 是 | | |

### 表 2 · `job_records`（任务记录）

| 字段名 | 类型 | 必填 | 示例 |
|---|---|---|---|
| `id` | 整数 | 是 | 1 |
| `job_id` | 文本 | 是 | job-20260703-001 |
| `job_name` | 文本 | 否 | 模型A训练 |
| `job_type` | 文本 | 是 | 训练 |
| `ts_start` / `ts_end` | 时间 | 是 | |
| `gpu_hours` | 小数 | 否 | 3840 |
| `node_list` | 文本 | 否 | node-12~15 |
| `owner` | 文本 | 否 | 视觉组 |
| `source_file` / `imported_at` | | 是 | |

### 表 3 · `carbon_records`（碳排放记录）

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` / `ts_end` | 时间 | 是 | | |
| `scope` | 文本 | 是 | Scope 2 | |
| `category` | 文本 | 是 | 运营碳排 | 运营碳排 / 隐含碳 |
| `emission_tco2e` | 小数 | 是 | 68.4 | |
| `method` | 文本 | 是 | location | location / market |
| `factor_id` | 整数 | 是 | 3 | 用了哪个因子 |
| `factor_version` | 文本 | 是 | v1.0 | 因子表版本 |
| `job_id` | 文本 | 否 | job-...-001 | 归属任务 |
| `calc_basis` | 文本 | 是 | | 计算依据（可下钻展示） |
| `calculated_at` | 时间 | 是 | | |

### 表 4 · `emission_factors`（排放因子表）

| 字段名 | 类型 | 必填 | 示例 |
|---|---|---|---|
| `id` | 整数 | 是 | 3 |
| `factor_name` | 文本 | 是 | 电力（华东电网） |
| `category` | 文本 | 是 | 电力 |
| `input_unit` | 文本 | 是 | kWh |
| `factor_value` | 小数 | 是 | 0.5703 |
| `output_unit` | 文本 | 是 | kgCO2e |
| `data_source` | 文本 | 是 | 生态环境部公布值 |
| `applicable_year` | 文本 | 否 | 2024 |
| `version` | 文本 | 是 | v1.0 |
| `is_builtin` | 布尔 | 是 | 是 |
| `updated_at` | 时间 | 是 | |

### 表 5 · `tariff_table`（分时电价表）

| 字段名 | 类型 | 必填 | 示例 |
|---|---|---|---|
| `id` / `period_type` | | 是 | 尖峰 |
| `month_range` | 文本 | 是 | 7-9 |
| `time_start` / `time_end` | 时间 | 是 | 14:00 / 17:00 |
| `price_per_kwh` | 小数 | 是 | 1.2038 |
| `demand_charge` | 小数 | 否 | 42.0 |

### 表 6 · `carbon_assets`（碳资产台账）

| 字段名 | 类型 | 必填 | 示例 |
|---|---|---|---|
| `id` / `asset_type` | | 是 | 碳配额 |
| `year` | 文本 | 是 | 2026 |
| `amount` | 小数 | 是 | 9800 |
| `unit` | 文本 | 是 | tCO2e |
| `direction` | 文本 | 是 | 获得 / 使用 |
| `contract_no` | 文本 | 否 | |
| `note` / `imported_at` | | | |

### 表 7 · `measures`（措施台账）

字段见 [9.3](#93-措施台账)。

### 表 8 · `workflow_runs`（工作流实例）

| 字段名 | 类型 | 说明 |
|---|---|---|
| `id` / `template` | | 用了哪个模板 |
| `period` | 文本 | 覆盖周期，如 2026-07 |
| `status` | 文本 | 进行中 / 已完成 |
| `steps_done` | 文本 | 已完成步骤（JSON） |
| `agent_conclusion` | 文本 | Agent 结论存档 |
| `user_note` | 文本 | 你的批注 |
| `created_at` | 时间 | |

---

## 13. 界面结构

Streamlit 侧边栏导航 + 主区域，共七页。

```
┌──────────────────┬──────────────────────────────────┐
│                  │                                  │
│  能碳监测平台      │        （主内容区）                │
│                  │                                  │
│  🏠 总览          │   当前页的图表、表格、操作按钮      │
│  📥 数据接入      │                                  │
│  🔥 高能耗识别    │                                  │
│  🌱 碳足迹测算    │                                  │
│  💰 成本与碳资产  │                                  │
│  🔄 工作流        │                                  │
│  ⚙️ 设置与基础表  │                                  │
│                  │                                  │
│  ─────────────   │                                  │
│  🤖 Agent  [开/关]│  ← 全局开关，随时可关             │
└──────────────────┴──────────────────────────────────┘
```

| 页面 | 内容 |
|---|---|
| 🏠 **总览** | 本期关键卡片（总电耗、PUE、碳排、电费、履约缺口）+ 最近导入 + 待办工作流 |
| 📥 **数据接入** | 三个来源的上传/采集/导入 → 待确认预览区 → 确认入库 |
| 🔥 **高能耗识别** | 参数设置 → 时段清单 → 热力图 → 四维归因 → 🤖Agent 归因 |
| 🌱 **碳足迹测算** | 口径选择（时段/任务/设备）→ 总量卡片（双算法）→ 构成图 → 明细下钻 → 导出 |
| 💰 **成本与碳资产** | 电费构成 → 需量曲线 → 成本-碳双维图 → 碳资产台账 → 履约缺口 |
| 🔄 **工作流** | 模板选择 → 六步清单（勾选推进）→ 措施台账 → 历史实例 |
| ⚙️ **设置与基础表** | 因子表 / 电价表 / 隐含碳表（可编辑+版本历史）+ API 密钥 + 数据边界设置 |

**统一的页面顺序**：`输入 → 确认 → 参数 → 看图 → 看明细 → 导出`。七个页面结构一致，学会一个就会用全部。

---

# 第三部分 · 技术与工程

## 14. 技术方案决策记录

> 记录「为什么选这个方案、还考虑过什么」。以后谁问「为啥不用 XX」，看这里。

### 14.1 主方案：渐进式路线（v1.0 决策，v2.0 沿用）

**阶段一~四** → Python + Streamlit，快速做出能用的版本
**阶段五** → PyInstaller 打包成 Windows / macOS 双击即用的启动包

**决策日期**：2026-08-21 ｜ **决策人**：项目所有者

**为什么选它**
1. **风险最低**：先小成本做出能跑的东西，发现需求不对可以马上改
2. **代码零浪费**：打包阶段只是加一层配置，不需要重写
3. **匹配使用者水平**：Streamlit 代码量最少，零基础也看得懂、改得动
4. **匹配任务性质**：这是数据分析 + AI 工具，Python 在文件解析、表格处理、图表、LLM SDK 上生态最成熟

**备选方案对比（留档）**

| 维度 | A · Streamlit | B · PySide6 原生 | C · Tauri/Electron + React |
|---|---|---|---|
| **最终形态** | 浏览器打开的本地网页 | 原生桌面窗口 | 现代化桌面客户端 |
| **界面美观度** | 一般，工具感 | 中等，系统原生风格 | 最好 |
| **代码复杂度** | 最低 ⭐ | 中高（约 A 的 3–5 倍） | 最高 |
| **零基础可维护性** | 最好 ⭐ | 一般 | 最差 |
| **数据分析能力** | 最强（Python 生态）⭐ | 最强 ⭐ | 弱，要造轮子 |
| **LLM SDK 生态** | 最成熟 ⭐ | 最成熟 ⭐ | 中等 |
| **GPU 功耗采集** | `pynvml` 现成 ⭐ | 现成 ⭐ | 需绕道调 Python |
| **跨平台成本** | 零，代码完全一致 ⭐ | 需分别打包 | 需分别打包 |
| **开发周期** | 最短 ⭐ | 中等偏长 | 最长 |

**为什么没单独选 A**：最终形态是浏览器网页，不够像正经软件。
**为什么没选 B**：界面代码复杂度对零基础不友好，且 macOS 打包必须有 Mac 实机。
**为什么没选 C**：需同时掌握前后端两套技术栈，学习成本最高；而它擅长的「界面漂亮」不是本项目的核心矛盾——本项目的核心矛盾是**把算力的能碳算准、把高能耗阶段找出来**，那是 Python 的主场。

### 14.2 v2.0 新增决策（2026-09-01）

| 决策 | 选择 | 理由 | 代价 |
|---|---|---|---|
| **Agent 类型** | 大模型驱动的智能体 | 归因和建议需要处理非结构化情况、需要解释原因，规则引擎做不到 | 必须联网调 API、有费用、结果有不确定性 |
| **数据边界** | 原始明细留本地，只发聚合摘要 | 在「能用 AI」和「数据安全」之间取平衡 | 实现复杂度上升，Agent 拿不到全部细节 |
| **数字 vs 结论分离** | 碳足迹由代码算死，Agent 只做解读 | 测算结果必须确定可复现，不能因模型随机性变化 | Agent 能力被限制在解读层 |
| **Agent 可关闭** | 提供纯本地模式全局开关 | 没网、没密钥、数据敏感时平台仍可用 | 需保证②④层不依赖③层 |
| **算力层级** | 智算中心 / 园区级 | 使用者的实际场景 | 需处理 PUE、分项计量、多机房 |
| **碳资产** | 做台账和履约测算，不做交易 | 交易涉及资质和外部接口，超出单机工具范畴 | —— |
| **数据来源** | 文件 + 实时采集 + 任务日志，三者并行 | 三种粒度互补，缺一不可 | 三套接入逻辑，工作量增加 |
| **双算法并行** | 地域法和市场法同时输出 | 两者口径不同、用途不同，只给一个会误导 | 界面和数据模型要同时容纳两套结果 |

### 14.3 沿用 v1.0 的决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 数据存储 | 本地 SQLite | Python 自带，零安装，单文件好备份 |
| 部署方式 | 单机 | 数据可控，无服务器成本 |
| 解析结果确认 | 必须人工确认才入库 | 解析出错是常态 |
| 因子版本快照 | 每次修改存版本 | 保证历史结果可追溯、不被静默改变 |

---

## 15. 技术栈清单

| 依赖 | 版本要求 | 它负责干什么（大白话） | 阶段 |
|---|---|---|---|
| **Python** | 3.11 或更高 | 编程语言本身，软件的地基 | 全程 |
| **streamlit** | ≥1.30 | 把 Python 代码变成网页界面，按钮/图表/上传框都靠它 | 一 |
| **pandas** | ≥2.0 | 处理表格数据，相当于「代码版的 Excel」 | 一 |
| **openpyxl** | ≥3.1 | 读写 .xlsx 文件 | 一 |
| **xlrd** | ≥2.0 | 读老格式 .xls 文件 | 一 |
| **plotly** | ≥5.18 | 画交互式图表（鼠标悬停看数值那种） | 一 |
| **SQLite** | Python 自带 | 本地数据库，无需安装 | 一 |
| **pdfplumber** | ≥0.11 | 读 PDF 里的文字和表格 | 二 |
| **python-docx** | ≥1.1 | 读 Word 里的文字和表格 | 二 |
| **anthropic** | ≥1.0 | **Claude 官方 Python SDK，Agent 靠它调模型** | 三 |
| **pynvml** | ≥11.5 | 读 GPU 实时功耗（`nvidia-smi` 的底层库） | 四 |
| **PyInstaller** | ≥6.0 | 打包成双击就能开的启动包 | 五 |

**为什么不用更多库**：依赖越少，安装越不容易出错。上面每个都是必需的。

> **`anthropic` 1.x 注意**：它基于 `httpx2` 而非 `httpx`。如果自己要用 HTTP 库，写 `import httpx2 as httpx`，用 `httpx` 包的对象会在请求时失败。

---

## 16. 目录结构

```
LBY97/
├── CLAUDE.md                      # 本文档（产品说明书 + 项目宪法）
├── README.md                      # 怎么装、怎么启动
├── requirements.txt               # 依赖清单
├── .env.example                   # 环境变量模板（API 密钥填这里）
├── .gitignore                     # 不提交的文件（数据库、密钥、缓存）
│
├── app.py                         # 程序入口，运行它启动平台
│
├── pages/                         # 功能页面（Streamlit 自动读这个文件夹）
│   ├── 1_数据接入.py
│   ├── 2_高能耗识别.py
│   ├── 3_碳足迹测算.py
│   ├── 4_成本与碳资产.py
│   ├── 5_工作流.py
│   └── 6_设置与基础表.py
│
├── core/                          # 核心逻辑，界面代码调用它
│   ├── __init__.py
│   ├── db.py                      # 数据库读写
│   ├── models.py                  # 八张表的结构定义
│   ├── parsers/                   # 文件解析
│   │   ├── excel_parser.py
│   │   ├── pdf_parser.py
│   │   └── docx_parser.py
│   ├── collectors/                # 实时采集
│   │   ├── gpu_collector.py       # pynvml
│   │   └── ipmi_collector.py
│   ├── detector.py                # 高能耗阶段识别算法
│   ├── carbon.py                  # 碳足迹测算
│   ├── cost.py                    # 电费与成本
│   ├── assets.py                  # 碳资产与履约缺口
│   ├── workflow.py                # 工作流引擎
│   ├── charts.py                  # 图表生成
│   └── exporter.py                # 导出 Excel / 图片 / 报告
│
├── agent/                         # Agent 层（可整体禁用）
│   ├── __init__.py
│   ├── client.py                  # Claude SDK 封装
│   ├── tools.py                   # 工具集定义（@beta_tool）
│   ├── boundary.py                # 数据边界过滤 + 脱敏
│   ├── prompts.py                 # 系统提示词
│   └── audit.py                   # 调用审计日志
│
├── data/
│   ├── factors.csv                # 内置排放因子表
│   ├── tariff.csv                 # 分时电价表
│   ├── embodied.csv               # 设备隐含碳表
│   ├── app.db                     # 本地数据库（自动生成，不进 Git）
│   ├── agent_audit.log            # Agent 调用审计（不进 Git）
│   └── versions/                  # 基础表历史版本快照
│
├── samples/                       # 示例文件，用来测试
│   ├── 示例_机房用电台账.xlsx
│   ├── 示例_任务日志.csv
│   └── 示例_碳配额通知.pdf
│
└── tests/
    ├── test_parsers.py
    ├── test_carbon.py             # 碳测算公式的正确性（最重要）
    ├── test_detector.py
    └── test_boundary.py           # 数据边界过滤是否真的生效（最重要）
```

**当前进度**：只有 `CLAUDE.md` 和 `README.md`，其余是待建蓝图。

---

## 17. 开发路线图

每个阶段都标注「做完之后你能干什么」，方便验收。

### ✅ 阶段零 · 产品文档（已完成）

**产出**：`CLAUDE.md`、`README.md`
**你能干什么**：明确知道要做什么；以后跟 Claude Code 说「按 CLAUDE.md 做」即可

---

### 🔨 阶段一 · 地基（下一步）

**做什么**
1. Streamlit 框架 + 七个页面骨架
2. SQLite 数据库 + 八张表
3. **Excel / CSV 导入**（列名映射 + 待确认预览区）
4. **碳足迹测算**（地域法 + 市场法 + 隐含碳摊销）
5. 基础表管理（因子表 / 电价表 / 隐含碳表，含版本快照）
6. 基础图表 + 导出 Excel

**你能干什么**
> 把机房用电台账导进去，得到按时段的碳足迹（两种算法），看到构成图和趋势图，下钻查每个数的计算依据，导出汇总表。**这时平台已能替代手工碳核算。**

**验收标准**：拿一份真实台账走完「导入 → 确认 → 测算 → 下钻 → 导出」，数字与手算一致。

---

### 🔨 阶段二 · 分析能力

**做什么**
1. **高能耗阶段识别**（阈值分段 + 四维归因）
2. **能源成本**（分时电价 + 需量 + 成本-碳双维图）
3. **碳资产台账 + 履约缺口测算**
4. PDF / Word 解析
5. 任务日志导入 + 按任务测算碳足迹

**你能干什么**
> 平台能告诉你哪几段时间在费能、费在制冷还是 IT；能算清这个月电费怎么构成的、哪些时段又贵又脏；能盘出履约缺口有多大。

---

### 🔨 阶段三 · Agent 接入

**做什么**
1. `anthropic` SDK 封装 + 密钥管理
2. **数据边界过滤 + 脱敏**（先做这个，再接模型）
3. 工具集（8 个工具，`@beta_tool` 定义）
4. Tool Runner 循环 + 归因/建议/答疑
5. 发送前预览确认 + 审计日志
6. 纯本地模式开关
7. Agent 生成报告初稿

**你能干什么**
> 点一下，Agent 自己去查数、分析，告诉你「7月12-15日夜间制冷电耗异常，IT 负载持平，指向冷机策略」，并给出减碳建议和预期收益。报告初稿自动写好。

**验收标准**：① 关掉 Agent 后其余功能全部正常；② 审计日志里确认发出的内容**不含**任何原始明细和设备标识。

---

### 🔨 阶段四 · 实时采集与工作流闭环

**做什么**
1. GPU 功耗采集（`pynvml`）+ 采样调度 + 降采样归档
2. IPMI / Redfish 整机功耗
3. 工作流模板（月度复盘 / 季度盘点）+ 六步清单
4. 措施台账 + 下周期自动回测

**你能干什么**
> 功耗数据自动流进来，不用手工导；每月照着工作流跑一遍；上个月采纳的措施，这个月平台自动告诉你实际减了多少碳。

---

### 🔨 阶段五 · 打包分发

**做什么**：PyInstaller 配置 → Windows 启动包 → macOS 启动包 → 使用手册

**你能干什么**
> 双击图标就打开，不用装 Python、不用敲命令；也能发给同事，解压双击就用。

---

### 💭 阶段六 · 可选增强（远期）

- 扫描件 PDF 的 OCR
- 多机房 / 多园区横向对比
- 碳排预测（基于历史趋势）
- 自定义报告模板

---

## 18. 已知限制与不做清单

诚实列出来，避免误解。

| 项目 | 状态 | 原因 |
|---|---|---|
| 扫描件 / 图片型 PDF | ❌ 阶段一到五不做 | 需 OCR 引擎，依赖重、准确率不稳定 |
| 碳交易 / 配额撮合 / 资产定价 | ❌ 不做 | 涉及资质和外部接口，超出单机工具范畴 |
| 自动执行调度或制冷控制 | ❌ 不做 | 平台只给建议，执行由人决定。自动控制涉及生产安全 |
| 带约束的成本-碳联合优化求解 | ❌ 不做 | 本平台做测算、识别、归因、建议，不做优化求解 |
| 多人协作 / 共享数据库 | ❌ 不做 | 明确单机使用；做这个需服务器、账号、权限体系 |
| 认证级碳核算报告 | ❌ 不做 | 输出仅供内部分析。ISO 14064/14067 等需第三方核证机构出具 |
| 自动判断数据正确性 | ❌ 不做 | 平台只做测算，数据质量由使用者把关 |
| **Agent 完全离线运行** | ❌ 做不到 | 大模型 API 必须联网。**代价已知并接受**，用数据边界和纯本地模式来控制风险 |
| Agent 结论的准确性 | ⚠️ 需自行核实 | 大模型有不确定性。**结论仅供参考，数字以平台计算为准** |
| 排放因子的权威性 | ⚠️ 需自行校准 | 内置因子为**参考值**，使用前必须按地区、年份核对更新 |
| 电价表 | ⚠️ 必须自行填写 | 电价随地区、季节、电压等级差异极大，内置值仅为示例 |
| 隐含碳数据 | ⚠️ 精度有限 | 厂商 EPD 报告覆盖率低，多数需用行业均值估算 |
| 复杂 Excel（多层合并表头） | ⚠️ 有限支持 | 尽力解析，解析不了在预览区提示，需人工调整源文件 |
| 实时采集的权限前提 | ⚠️ 依赖环境 | 需能访问目标机器。拿不到就退回文件导入，粒度变粗 |

---

# 第四部分 · 协作约定

## 19. 常用命令

> ⚠️ 阶段一代码还没写，这些命令**要等阶段一完成后才能用**。先放在这里备查。

### Windows（在 PowerShell 或 CMD 里执行）

```powershell
# 第一次使用：创建独立环境并安装依赖
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 配置 API 密钥（只有要用 Agent 才需要）
copy .env.example .env
# 然后用记事本打开 .env，把密钥填进去

# 每次启动平台
.venv\Scripts\activate
streamlit run app.py

# 停止平台：在命令窗口按 Ctrl + C
```

### macOS（在「终端」里执行）

```bash
# 第一次使用：创建独立环境并安装依赖
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 配置 API 密钥（只有要用 Agent 才需要）
cp .env.example .env
# 然后用文本编辑器打开 .env，把密钥填进去

# 每次启动平台
source .venv/bin/activate
streamlit run app.py

# 停止平台：在终端窗口按 Control + C
```

启动成功后浏览器自动打开 `http://localhost:8501`，看到界面就说明跑起来了。

### 常见问题速查

| 现象 | 怎么办 |
|---|---|
| `python 不是内部或外部命令` | Python 没装或没勾「Add to PATH」，重装时勾上 |
| `ModuleNotFoundError` | 依赖没装全，重跑 `pip install -r requirements.txt` |
| 浏览器没自动打开 | 手动输入 `http://localhost:8501` |
| 端口被占用 | 换端口：`streamlit run app.py --server.port 8502` |
| 提示「未配置 API 密钥」 | 只影响 Agent，其余功能正常。要用就在 `.env` 里填密钥 |
| `pip` 下载慢 | 换镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| Mac 提示「无法验证开发者」 | 阶段五打包版才会遇到。系统设置 → 隐私与安全性 → 「仍要打开」 |

---

## 20. 给 Claude Code 的工作约定

> 这一节是给 AI 助手看的规则，每次会话都会自动读到。

### 20.1 语言与命名

- **面向用户的文字一律中文**：界面文案、提示、错误信息、报告标题
- **代码注释用中文**，让零基础的项目所有者看得懂
- **变量名、函数名、文件名用英文**（`pages/` 下的页面文件名例外，用中文以便侧边栏显示中文）
- 术语统一用[第 3 节](#3-名词解释零基础必读)的表述，不要自创同义词

### 20.2 开发原则

1. **先跑通，再优化**：每个功能先做最简可用版本，验证没问题再改进。不要一上来写复杂抽象
2. **小步交付**：一次只做一个功能点，做完让项目所有者能立刻看到效果
3. **不静默失败**：解析出错、采集失败、API 报错，一律在界面明确提示，绝不悄悄跳过或填默认值
4. **保持依赖精简**：不为一个小功能引入新库。确需新增时先说明理由
5. **任何依赖变更必须同步更新 `requirements.txt`**

### 20.3 数据相关的硬性规则

- **解析和采集结果必须经用户确认才入库**（[5.4](#54-统一接入流程三个来源都走这一套)），不允许直接写库
- **每条入库数据必须记录来源和时间**，保证可追溯
- **每条碳测算结果必须记录用了哪个因子、哪个版本**
- **修改基础表（因子/电价/隐含碳）必须保留版本快照**，历史结果不能因基础表改动被静默改变
- **不要删除或覆盖 `data/app.db`**，那是用户的真实数据
- **地域法和市场法必须同时计算并同时展示**，只给一个会误导

### 20.4 Agent 相关的硬性规则

> 这几条是 v2.0 的安全底线，**违反任何一条都是严重问题**。

1. **数据边界不可绕过**：所有出网内容必须经过 `agent/boundary.py` 过滤。禁止在 `agent/` 以外的地方直接调 API
2. **禁止发送原始明细**：逐条记录、设备序列号/IP/主机名、机房位置、文件原文，一律不得出网
3. **API 密钥只从环境变量读**（`ANTHROPIC_API_KEY`）。**绝不硬编码，绝不写进代码或提交进 Git**，`.env` 必须在 `.gitignore` 里
4. **每次调用必须写审计日志**（`data/agent_audit.log`）
5. **Agent 不得写数据库**：工具集只提供只读、只返回聚合值的函数
6. **测算层不得依赖 Agent**：关掉 Agent 后，②④层功能必须全部正常。写代码时保证 `core/` 不 import `agent/`
7. **Agent 输出必须标注**：界面上加 🤖 图标和「AI 分析建议，请自行核实」

### 20.5 调用 Claude API 的编码约定

- **SDK**：用官方 `anthropic` Python SDK，不要用 `requests` 手写 HTTP
- **模型**：默认 `claude-opus-5`。不要为省钱擅自降级——那是项目所有者的决定
- **客户端**：用零参构造 `anthropic.Anthropic()`，让 SDK 自己从环境变量解析凭据，不要硬编码 key
- **思考模式**：用 `thinking={"type": "adaptive"}`。**不要用 `budget_tokens`**，它在 `claude-opus-5` 上会返回 400
- **Agent 循环**：用 Tool Runner（`client.beta.messages.tool_runner`），工具用 `@beta_tool` 装饰器定义，不要手写 while 循环
- **长输出**：用 `.stream()` + `.get_final_message()`，避免请求超时
- **`max_tokens`**：非流式默认 `16000`，流式默认 `64000`。不要给太小导致输出被截断
- **拒绝回退**：写 `claude-opus-5` 请求时默认带上服务端 fallback（`betas=["server-side-fallback-2026-07-01"]` + `fallbacks="default"`），并检查 `stop_reason` 是否为 `"refusal"` 再读 `content`
- **错误处理**：按具体到宽泛的顺序捕获（`NotFoundError` → `RateLimitError` → `APIStatusError` → `APIConnectionError`），不要一个 `except` 兜底
- **不要重复造轮子**：用 SDK 自带的类型和辅助方法，别自己定义等价的数据结构

### 20.6 跨平台注意事项

- 文件路径一律用 `pathlib.Path`，不要手写 `\` 或 `/`
- 读 CSV 要处理 UTF-8 和 GBK 两种编码（中文 Windows 常见 GBK）
- 不使用仅 Windows 或仅 macOS 可用的库
- `pynvml` 在没有 NVIDIA 卡的机器上会导入失败，必须 try/except 并优雅降级

### 20.7 测试要求

两个模块**必须有测试**，其余尽量：

1. **`core/carbon.py`**（碳测算）——公式算错，整个平台就没意义了
2. **`agent/boundary.py`**（数据边界）——过滤失效，敏感数据就出网了

### 20.8 沟通方式

项目所有者是**编程零基础**的初学者：

- 解释技术问题用大白话和类比，避免堆术语
- 给出的命令要能**直接复制粘贴执行**，并说明在哪里执行
- 报错时先说「这是什么意思」，再说「怎么解决」
- 涉及技术选型的决策，列出选项和优劣，让项目所有者拍板，并把结论补记到[第 14 节](#14-技术方案决策记录)

---

# 附录 A · 排放因子初始条目

> 🔴 **重要声明**：下表数值为**参考值**，用于让平台开箱即用，**不构成任何权威依据**。
> 正式使用前，请务必按你所在**地区、年份、行业**的官方发布数据核对更新。
> 可在「⚙️ 设置与基础表」页随时编辑。

### A.1 电力因子（最关键，务必替换）

| 因子名称 | 单位 | 参考数值 | 结果单位 | 数据来源类型 | 备注 |
|---|---|---|---|---|---|
| 电力（全国电网平均） | kWh | 0.5703 | kgCO2e | 生态环境部公布值 | 需按最新年度值更新 |
| 电力（华北区域电网） | kWh | 按实际填 | kgCO2e | 区域电网因子 | 各区域差异大，**必须自行填入** |
| 电力（华东区域电网） | kWh | 按实际填 | kgCO2e | 区域电网因子 | 同上 |
| 电力（南方区域电网） | kWh | 按实际填 | kgCO2e | 区域电网因子 | 同上 |
| 绿电 / 绿证抵扣 | kWh | 0.0000 | kgCO2e | market-based 口径 | 仅市场法下生效 |

### A.2 其他能源因子

| 因子名称 | 单位 | 参考数值 | 结果单位 | 备注 |
|---|---|---|---|---|
| 柴油（应急发电） | L | 2.6300 | kgCO2e | IPCC 缺省值折算 |
| 天然气 | m³ | 2.1622 | kgCO2e | IPCC 缺省值折算 |
| 外购蒸汽 | GJ | 110.0000 | kgCO2e | 随供热源差异极大，务必替换 |
| 自来水 | t | 0.3000 | kgCO2e | 行业经验值 |

### A.3 硬件隐含碳参考值

> 🔴 隐含碳数据公开度低，下列为**粗略行业估算**，精度有限。有厂商 EPD 报告的务必替换。

| 设备类型 | 隐含碳（kgCO₂e/台） | 设计寿命（年） | 备注 |
|---|---|---|---|
| GPU 服务器（8 卡） | 3000 | 5 | 随卡型差异大 |
| 通用 CPU 服务器（2U） | 1200 | 5 | |
| 存储服务器 | 1500 | 6 | |
| 交换机（数据中心级） | 400 | 7 | |
| UPS（模块化） | 2000 | 10 | |
| 精密空调 | 2500 | 10 | |

### A.4 PUE 参考区间

| 机房类型 | 典型 PUE | 说明 |
|---|---|---|
| 老旧风冷机房 | 1.6 – 2.0 | 改造空间大 |
| 常规风冷数据中心 | 1.3 – 1.5 | 主流水平 |
| 优化风冷 / 间接蒸发 | 1.2 – 1.3 | |
| 液冷智算中心 | 1.1 – 1.2 | 先进水平 |

> 平台不内置 PUE，**必须用你机房的实测值**（总电耗 ÷ IT 电耗）。

---

# 附录 B · 核心公式与单位换算

### B.1 能耗与 PUE

```
机房总电耗  E_total = E_IT × PUE
PUE        = E_total ÷ E_IT
IT 电耗     E_IT = Σ(设备功率_kW × 时长_h)
卡时估算    E_IT ≈ 卡时 × 单卡平均功率_kW
```

### B.2 碳足迹

```
【运营碳排 · 地域法】
  C_location = E_total × EF_grid

【运营碳排 · 市场法】
  C_market   = (E_total − E_green) × EF_grid + E_green × EF_green
             （EF_green 通常为 0）

【隐含碳摊销】
  C_embodied = Σ [ EC_device × (使用时长_h ÷ 设计寿命_h) ]
             设计寿命_h = 寿命年数 × 365 × 24

【总碳足迹】
  C_total    = C_operational + C_embodied

【任务级碳足迹】
  C_job      = (任务卡时 × 单卡功率 × PUE × EF_grid) + 分摊隐含碳
```

### B.3 成本

```
【电量电费】 Σ(各时段电量 × 该时段电价)
【需量电费】 当月最高需量_kW × 需量单价
【总电费】  电量电费 + 需量电费 + 其他费用
【度电成本】 总电费 ÷ 总电量
【度电碳强度】总碳排 ÷ 总电量
```

### B.4 碳资产

```
【履约缺口】核算排放 − 免费配额 − 已购配额 − 绿电抵扣 − CCER抵销
           （CCER 抵销受比例上限约束，超出部分自动截断并提示）
```

### B.5 高能耗阶段识别

```
【基线】     所选窗口内功率中位数（或手工指定）
【判定条件】 功率 > 基线 × 阈值系数(默认1.2)
             且 持续时长 > 最短时长(默认30分钟)
【区间合并】 间隔 < 10 分钟的相邻区间合并
【超出幅度】 (区间平均功率 − 基线) ÷ 基线 × 100%
```

### B.6 单位换算

| 换算关系 | 系数 |
|---|---|
| 1 tCO₂e | = 1000 kgCO₂e |
| 1 kWh | = 3.6 MJ |
| 1 MWh | = 1000 kWh |
| 1 GJ | = 1000 MJ = 277.78 kWh |
| 1 tce（吨标准煤） | = 29.3076 GJ |
| 1 kW 持续 1 小时 | = 1 kWh |
| 1 卡时（单卡 400W） | ≈ 0.4 kWh |

---

**文档结束** ｜ 有任何需求变更，请直接修改本文件，它是这个项目的唯一事实来源。
