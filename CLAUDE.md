# 能碳监测平台 · 产品文档

> **面向算力工业园区的低碳算力能碳分析平台**
>
> 本文件有两个作用：
> 1. **给你看**——这是产品说明书，讲清楚这个软件要做成什么样。
> 2. **给 Claude Code 看**——每次打开 Claude Code 会自动读这个文件。以后你不用重复解释需求，直接说「按 CLAUDE.md 做」。
>
> 文档版本：**v3.0** ｜ 状态：产品定义已定稿，尚未开始编码

---

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-21 | 初版。通用工厂能碳分析工具（电表/物料台账 → 能耗、损耗、碳排） |
| v2.0 | 2026-09-01 | 方向性重构。改为智算中心算力能碳管理；引入 Agent；新增碳资产 |
| **v3.0** | **2026-09-02** | **确立方法论内核**。以「算力工业园区」为分析框架；核心指标定为**每百万 Token 碳足迹**；新增**三性分析**（差异性/时序性/不稳定性）作为方法论主线；新增任务×服务器异构归因、绿电利用比例分析 |

### v3.0 相对 v2.0 的五处关键变化

1. **确立了分析框架**：把低碳算力中心当作**算力工业园区**——Token 就是产品，电力就是原料，服务器机型就是生产线
2. **确立了核心指标**：**每百万 Token 碳足迹（gCO₂e/MTok）**，等价于工业里的「单位产品碳足迹」
3. **新增方法论主线**：平台的价值不在「把数据存起来」，而在**分析能碳数据的三个固有特性**——差异性、时序性、不稳定性。见[第 6 节](#6-模块二--三性分析方法论内核)
4. **新增异构归因**：识别**不同计算任务在不同服务器机型上**的能耗差异，这是差异性分析的主战场
5. **新增绿电利用比例**：不只算碳排总量，还要算**每个时段用了多少绿电**，以及负载与绿电供给的时间错配

> **一句话说明白 v3.0 的定位**：
> 它**不是**一个能碳数据平台（存数、画图、出报表）。
> 它是一个**能碳数据的差异性、时序性、不稳定性分析工具**——回答「为什么这批任务比那批费电」「为什么这个时段又贵又脏」「为什么 PUE 这周抖得厉害」，最后固化成可复用的低碳算力工作流。

---

## 目录

- [第一部分 · 产品定义](#第一部分--产品定义)
  - [1. 产品概述](#1-产品概述)
  - [2. 算力工业园区框架](#2-算力工业园区框架)
  - [3. 目标用户与典型场景](#3-目标用户与典型场景)
  - [4. 名词解释（零基础必读）](#4-名词解释零基础必读)
- [第二部分 · 功能规格](#第二部分--功能规格)
  - [5. 模块一 · 数据接入](#5-模块一--数据接入)
  - [6. 模块二 · 三性分析（方法论内核）](#6-模块二--三性分析方法论内核)
  - [7. 模块三 · 高能耗时段与绿电利用识别](#7-模块三--高能耗时段与绿电利用识别)
  - [8. 模块四 · 百万 Token 碳足迹测算](#8-模块四--百万-token-碳足迹测算)
  - [9. 模块五 · 能源成本与碳资产分析](#9-模块五--能源成本与碳资产分析)
  - [10. 模块六 · 低碳算力工作流](#10-模块六--低碳算力工作流)
  - [11. Agent 设计规格](#11-agent-设计规格)
  - [12. 基础表管理](#12-基础表管理)
  - [13. 数据模型](#13-数据模型)
  - [14. 界面结构](#14-界面结构)
- [第三部分 · 技术与工程](#第三部分--技术与工程)
  - [15. 技术方案决策记录](#15-技术方案决策记录)
  - [16. 技术栈清单](#16-技术栈清单)
  - [17. 目录结构](#17-目录结构)
  - [18. 开发路线图](#18-开发路线图)
  - [19. 已知限制与不做清单](#19-已知限制与不做清单)
- [第四部分 · 协作约定](#第四部分--协作约定)
  - [20. 常用命令](#20-常用命令)
  - [21. 给 Claude Code 的工作约定](#21-给-claude-code-的工作约定)
- [附录 A · 排放因子初始条目](#附录-a--排放因子初始条目)
- [附录 B · 核心公式与单位换算](#附录-b--核心公式与单位换算)

---

# 第一部分 · 产品定义

## 1. 产品概述

### 1.1 一句话定位

**能碳监测平台**是一个跑在你自己电脑上的算力能碳分析工具：把算力园区当工厂看，接入用电、绿电、服务器和任务 Token 数据，由一个 **Agent** 分析这些数据的**差异性、时序性和不稳定性**，测算出**每百万 Token 的碳足迹**，找出高能耗时段和绿电利用短板，最终形成一套可复用的**低碳算力工作流**。

### 1.2 它解决什么问题

| 你现在的痛点 | 平台怎么解决 |
|---|---|
| 只知道园区总电费，不知道**哪类任务、哪种机型**在浪费 | 差异性分析：同类任务跨机型能效对比，找出低效组合 |
| 说不清「我们的模型服务，每百万 Token 排多少碳」 | 核心指标直接给出 gCO₂e/MTok，可按机型/任务/时段下钻 |
| 买了绿电，但说不清**到底用上了多少** | 绿电利用比例分时测算 + 负载与绿电供给的错配分析 |
| 能耗数据忽高忽低，看不出是异常还是正常波动 | 不稳定性分析：滚动波动率、突变点、尖峰频次 |
| 只会看月度总量，看不出日内规律和趋势 | 时序性分析：周期分解、自相关、碳-负载相位差 |
| 电费和碳排是两本账 | 成本与碳双维对齐到同一时间轴 |
| 分析结论靠人写，每月重复劳动 | Agent 出归因和减碳建议，一键生成报告 |
| 好不容易理出的方法，换个人就失传 | 固化成工作流，每月照着跑 |

### 1.3 它**不**解决什么问题（重要，先说清楚）

- ❌ **不是单纯的能碳数据平台**——存数、画图、出报表只是手段，**分析数据的三性才是目的**
- ❌ **不做碳交易**——碳配额、绿证只做台账和测算，不接交易所、不做撮合、不做定价
- ❌ **不做实时调度**——平台给建议，不直接控制算力调度或制冷系统，执行由人决定
- ❌ **不做带约束的成本-碳联合优化求解**——本平台做**测算、识别、归因、建议**，不做优化求解器
- ❌ **不出具认证级报告**——输出仅供内部分析，不能直接用于 ISO 14064 / 14067 / GHG Protocol 第三方核证
- ❌ **不做多人协作**——单机个人使用，无账号、权限、共享数据库
- ❌ **不替你判断数据对错**——平台负责测算和分析，数据质量由你把关

### 1.4 四条主线

```
① 数据有多不一样？   →  差异性：不同任务 × 不同机型，能效差多少
② 数据怎么随时间变？ →  时序性：周期、趋势、碳与负载的时间错配
③ 数据有多不稳？     →  不稳定性：波动、突变、尖峰、绿电间歇
                ↓
④ 折算成一个能对外说的数字：每百万 Token 碳足迹
                ↓
        固化成可复用的低碳算力工作流
```

### 1.5 核心设计原则

1. **分析优先于展示**：每张图都必须回答一个具体问题。做不出结论的图不做
2. **数据边界优先**
   - 原始明细（逐条功耗、设备序列号、任务名、机房位置）**只存本地 SQLite**
   - 发给大模型 API 的**只有聚合统计量**（如「7月3日14-18点平均功率420kW，较基线高38%」）
   - 每次调 API 前，界面**必须展示将要发送的完整内容**，你确认后才发
   - 提供**纯本地模式开关**：关掉 Agent 后测算、图表、导出全部照常
3. **不静默采信**：解析和采集的数据先进「待确认」区，你确认后才入库
4. **可追溯**：每条数据记来源和时间；每次测算记因子版本；每条 Agent 结论记基于哪些数据
5. **Agent 的话不是结论**：Agent 输出标注为「AI 分析建议」，与确定性计算结果**视觉区分**
6. **跨平台**：同一套代码，Windows 和 macOS 都能跑

---

## 2. 算力工业园区框架

这是 v3.0 的分析框架，整份文档都建立在这个类比上。

### 2.1 类比映射

| 传统工业园区 | **算力工业园区** | 在平台里对应什么 |
|---|---|---|
| 园区 | 算力园区 | `park_id` |
| 车间 | 机房 / 集群 | `room_id` |
| 生产线 | **服务器机型（异构）** | `server_model` |
| 设备 | 单台节点 / 单张卡 | `device_id` |
| 原料 | **电力**（含绿电、市电） | `energy_records` |
| 工序 | **计算任务类型**（训练/微调/推理/数据处理） | `job_type` |
| **产品** | **Token** | `token_records` |
| 产量 | Token 产出量 | `tokens_total` |
| 单位产品能耗 | **每百万 Token 电耗**（kWh/MTok） | 核心指标 |
| **单位产品碳排** | **每百万 Token 碳足迹**（gCO₂e/MTok） | **最核心指标** |
| 设备能效 | **Tokens/kWh** | 差异性分析主指标 |
| 边角料 / 损耗 | PUE 损耗（制冷、供配电） | PUE − 1 部分 |

### 2.2 为什么这个框架有用

工业碳核算几十年的方法论可以直接搬过来：

- **单位产品碳足迹**是工业界通用语言，「每百万 Token 排 X 克碳」业务方一听就懂
- **生产线对比**变成**机型能效对比**——同样产 100 万 Token，A100 和 H100 谁更省
- **工序能耗拆解**变成**任务类型能耗拆解**——训练、微调、推理各占多少
- **产能利用率**变成**卡利用率 / 负载率**——设备买了没用满也是浪费

### 2.3 核心指标体系

| 层级 | 指标 | 单位 | 说明 |
|---|---|---|---|
| **顶层** | **每百万 Token 碳足迹** | gCO₂e/MTok | 对外交代用的那个数 |
| 顶层 | 每百万 Token 电耗 | kWh/MTok | 去掉因子影响，看纯能效 |
| 顶层 | 绿电利用比例 | % | 用电里有多少是绿的 |
| 中层 | 能效 | Tokens/kWh | 机型/任务对比用 |
| 中层 | PUE | — | 机房基础设施效率 |
| 中层 | 度电碳强度 | gCO₂e/kWh | 随时段变化 |
| 中层 | 度电成本 | 元/kWh | 随时段变化 |
| 底层 | 卡时、功率、负载率、利用率 | 卡·h / kW / % | 原始活动数据 |

---

## 3. 目标用户与典型场景

### 目标用户

算力园区 / 智算中心里负责能源管理、双碳、运维或成本分析的个人。**不需要懂编程**，会用 Excel 就行。

### 典型场景一 · 对外交代模型服务的碳足迹

> 业务方问「我们这个模型服务，每百万 Token 排多少碳」→ 打开「碳足迹测算」页，选推理口径和统计周期 → 平台算出 `每百万 Token 碳足迹 = 42 gCO₂e`（地域法）/ `28 gCO₂e`（市场法，扣绿电）→ 可下钻看这 42 克里，多少来自 IT 用电、多少来自制冷、多少来自硬件隐含碳。

### 典型场景二 · 找出低效的「任务×机型」组合

> 打开「三性分析 → 差异性」页 → 平台列出各机型跑同类推理任务的能效（Tokens/kWh）→ 发现 `机型B 跑长文本推理的能效只有机型A 的 62%`，而调度系统一直往机型B 上派长文本任务 → Agent 归因：「长文本任务显存带宽受限，机型B 的 HBM 带宽较低导致卡利用率不足」→ 提出把长文本任务优先调度到机型A。

### 典型场景三 · 绿电用得好不好

> 打开「高能耗时段与绿电利用」页 → 看到全月绿电利用比例 31%，但**分时看**：光伏出力高峰（10:00-15:00）时园区负载反而处于低谷，绿电大量弃用；而夜间负载高峰只能用市电 → Agent 建议把可延迟的批量推理和微调任务迁到 10:00-15:00，预计绿电利用比例提升到 44%。

### 典型场景四 · 数据抖得厉害，是异常还是正常

> 运维反馈「这周 PUE 忽高忽低」→ 打开「三性分析 → 不稳定性」页 → 平台算出本周 PUE 滚动标准差是上周的 2.3 倍，并标出 3 个突变点，都集中在凌晨 2-4 点 → 结合环节拆解发现是制冷电耗突增而 IT 负载持平 → 指向冷机夜间策略切换问题。

---

## 4. 名词解释（零基础必读）

后面章节会反复用这些词，先讲明白。

### 4.1 算力与园区

| 名词 | 大白话解释 |
|---|---|
| **算力工业园区** | 把算力中心当工厂看的一种视角：电是原料，Token 是产品，服务器是生产线。见[第 2 节](#2-算力工业园区框架)。 |
| **Token** | 大模型处理文字的最小单位，大约相当于半个汉字或 0.75 个英文单词。**它是算力园区的「产品」。** |
| **百万 Token（MTok）** | 100 万个 Token。因为单个 Token 的能耗极小，行业习惯按百万个来衡量。 |
| **异构** | 园区里有多种不同型号的服务器/加速卡，性能和能效各不相同。**异构是差异性分析的前提。** |
| **IT 负载 / IT 电耗** | 服务器、GPU、存储、网络设备本身用掉的电。**不含**制冷和供配电损耗。 |
| **PUE** | 电能利用效率 = 机房总电耗 ÷ IT 电耗。<br>例：PUE=1.3 表示 IT 每用 1 度电，机房总共要用 1.3 度。**越接近 1 越好。** |
| **卡时** | 一张 GPU 卡运行一小时 = 1 卡时。 |
| **负载率 / 利用率** | 设备实际功率 ÷ 额定功率；GPU 计算单元的繁忙程度。反映设备用得满不满。 |
| **能效** | 每度电能产出多少 Token（Tokens/kWh）。**数值越大越好。** |

### 4.2 三性（本平台的方法论核心）

| 名词 | 大白话解释 | 回答什么问题 |
|---|---|---|
| **差异性** | 同样一件事，在不同条件下结果差多少。<br>本平台指：**不同任务类型 × 不同服务器机型**，能耗和能效的差异。 | 「为什么这批任务比那批费电？」 |
| **时序性** | 数据随时间变化的规律：日内周期、周周期、长期趋势、以及**不同曲线之间的时间错配**。 | 「什么时候费电？绿电来的时候我在用吗？」 |
| **不稳定性** | 数据的波动、突变和异常程度。稳定的系统好管理，不稳的系统藏着问题。 | 「这个波动是正常的还是出事了？」 |
| **变异系数（CV）** | 标准差 ÷ 均值。衡量波动大小的**无量纲**指标，可以跨不同量级的数据比较。 | 「哪个指标抖得最厉害？」 |
| **相位差** | 两条周期曲线高峰之间的时间差。<br>例：负载高峰在 20:00，绿电高峰在 12:00，相位差 8 小时。 | 「负载和绿电对得上吗？」 |
| **突变点（变点）** | 数据统计特性发生明显改变的时刻。 | 「什么时候开始变的？」 |

### 4.3 能与碳

| 名词 | 大白话解释 |
|---|---|
| **活动数据** | 「消耗了多少」的原始数字（多少度电、多少卡时、多少 Token），是所有计算的起点。 |
| **排放因子** | 换算系数，把消耗量换算成碳排放量。例：1 度电排 0.57 公斤 CO₂ → 0.57 就是电力排放因子。 |
| **碳足迹** | 一件事或一个产品从头到尾一共排放了多少温室气体。 |
| **tCO₂e / gCO₂e** | 吨/克二氧化碳当量。温室气体不止 CO₂ 一种，统一折算成 CO₂ 来比较。<br>1 tCO₂e = 1000 kgCO₂e = 1,000,000 gCO₂e |
| **功能单位** | 碳足迹核算里「按什么来算」的基准。**本平台的功能单位是「百万 Token」。** |
| **运营碳排** | 机房运行时用电产生的碳排放。占大头。 |
| **隐含碳** | 服务器、GPU 这些硬件**在生产制造时**就已排放的碳。按寿命摊到每年/每个任务上。 |
| **Scope 1/2/3** | 范围一：自己烧的（柴油发电机）；范围二：买的能源（**外购电力，绝对大头**）；范围三：上下游（硬件隐含碳）。 |
| **location-based（地域法）** | 用所在电网的平均排放因子算碳排。反映物理现实。 |
| **market-based（市场法）** | 扣掉买的绿电/绿证之后再算。反映采购选择。<br>⚠️ 两种算法结果不同，报告里必须**同时给出并注明用的哪种**。 |

### 4.4 绿电与成本

| 名词 | 大白话解释 |
|---|---|
| **绿电** | 直接采购或自建的风电、光伏等零碳电力。 |
| **绿电利用比例** | 绿电用量 ÷ 总用电量。**分时段算才有意义**——月度总量会掩盖时间错配问题。 |
| **弃用绿电** | 绿电供给来了但负载没跟上，白白浪费掉的部分。 |
| **绿电间歇性** | 光伏晚上不发电、风电看天吃饭，供给天然不稳。**这是不稳定性分析的一个重点。** |
| **分时电价** | 电价按时段不同，一般分尖峰/高峰/平段/低谷，谷段可能只有峰段的三分之一。 |
| **需量电费** | 按用电的**最高功率**收的固定费用，和用了多少度电无关。功率冲高一次，整月都按那个数交钱。 |
| **碳配额（CEA）** | 政府分配的「允许排放量」。超了要买，省下可以卖。 |
| **绿证（GEC）** | 绿色电力证书。买了相当于声明用了对应绿电（市场法下可抵扣）。 |
| **CCER** | 国家核证自愿减排量，可抵销一部分排放，通常有比例上限。 |
| **履约缺口** | 核算排放 − 可用配额 − 各类抵销量。**大于零就是要花钱补的窟窿。** |

### 4.5 Agent 相关

| 名词 | 大白话解释 |
|---|---|
| **Agent（智能体）** | 能自己看数据、自己判断、自己给结论的 AI 助手。它会主动调用平台功能去查数、算数，再综合出结论。 |
| **工具（Tool）** | Agent 能调用的功能，比如「查某时段能耗」「跑一次三性分析」。工具**全部在你本地运行**，Agent 只拿返回结果。 |
| **数据边界** | 规定哪些数据可以发给云端模型、哪些绝对不行的一条线。见 [1.5](#15-核心设计原则)。 |

---

# 第二部分 · 功能规格

## 5. 模块一 · 数据接入

四类数据。**Token 数据和绿电供给曲线是 v3.0 新增的，也是新增指标的前提**——没有它们，算不出每百万 Token 碳足迹和分时绿电利用比例。

### 5.1 数据清单

| # | 数据类型 | 干什么用 | 没有它会怎样 |
|---|---|---|---|
| A | **能耗数据** | 算电耗、PUE、碳排 | 平台没法用 |
| B | **Token 计量** | 算每百万 Token 碳足迹 | **核心指标算不出来** |
| C | **绿电供给曲线** | 算分时绿电利用比例 | 只能算月度总量，看不出时间错配 |
| D | **任务与服务器台账** | 差异性分析（任务×机型） | 做不了异构归因 |

### 5.2 来源 A · 能耗数据

| 途径 | 方式 | 拿到什么 |
|---|---|---|
| 文件导入 | Excel / CSV / PDF / Word | 机房用电台账、分项电表报表、电费账单 |
| 实时采集 · GPU | NVML（`pynvml`） | 每张卡的功率(W)、利用率、显存、温度 |
| 实时采集 · 整机 | IPMI / Redfish | 整机功率、进出风温度 |
| 实时采集 · 机房 | 电表 / 动环系统导出 | 总电耗、制冷电耗、UPS 电耗 → 算 PUE |

**采样策略**：默认 60 秒一次。原始明细保留 30 天，之后按 5 分钟均值降采样归档，避免数据库膨胀。

### 5.3 来源 B · Token 计量（v3.0 新增）

> **假设**：Token 数据来自推理服务或网关的统计日志（如 vLLM / TGI 的 metrics、API 网关访问日志、自研调度系统导出）。

| 字段 | 说明 | 示例 |
|---|---|---|
| 统计区间 | 起止时间 | 2026-07-03 14:00 ~ 15:00 |
| 服务 / 模型标识 | 可脱敏 | svc-chat-a |
| 任务类型 | 推理 / 训练 / 微调 | 推理 |
| `tokens_in` | 输入（prompt）Token 数 | 1,240,000 |
| `tokens_out` | 输出（completion）Token 数 | 380,000 |
| 关联机型 | 跑在哪种服务器上 | H100-8卡 |
| 关联节点 | | node-12~15 |

**训练任务怎么办**：训练不产出推理 Token，改用**训练消耗 Token 数**（喂进去的训练数据量）作为产量口径。两种口径在界面上分开展示，**不混算**。

> ⚠️ **口径必须说清楚**：每百万 Token 碳足迹用 `tokens_in + tokens_out` 还是只用 `tokens_out`，行业没有统一标准。平台**默认用总和**，但在设置里可切换，且**测算结果上必须标注用的哪种口径**。

### 5.4 来源 C · 绿电供给曲线（v3.0 新增）

> **假设**：绿电供给是有时间分布的，不是一个月度总数。

| 字段 | 说明 | 示例 |
|---|---|---|
| 时间区间 | | 2026-07-03 12:00 ~ 13:00 |
| 绿电类型 | 自建光伏 / 风电 PPA / 绿证 | 自建光伏 |
| 供给电量 | 该时段可用绿电 | 180 kWh |
| 实际消纳 | 该时段实际用掉的 | 145 kWh |
| 合同编号 | | PPA-2026-007 |

> **绿证（GEC）是例外**：绿证不带时间属性，只在月度/年度总量上抵扣。平台把它单列，**不参与分时绿电利用比例计算**，只进市场法碳排和碳资产台账。

### 5.5 来源 D · 任务与服务器台账

**任务日志**

| 字段 | 示例 |
|---|---|
| 任务 ID | job-20260703-001 |
| 任务名称（可脱敏） | 模型A训练 |
| 任务类型 | 训练 / 微调 / 推理 / 数据处理 |
| 起止时间 | 2026-07-03 09:00 ~ 07-08 17:30 |
| 占用节点 / 卡数 | node-12~15，32 卡 |
| 归属对象 | 视觉组 |

**服务器机型台账**（差异性分析的基准表）

| 字段 | 示例 |
|---|---|
| 机型标识 | H100-8卡 |
| 加速卡型号 / 数量 | H100 / 8 |
| 单卡额定功率（W） | 700 |
| 整机额定功率（kW） | 10.2 |
| 隐含碳（kgCO₂e） | 3000 |
| 设计寿命（年） | 5 |
| 所属机房 | room-A |

### 5.6 统一接入流程

```
① 数据进来（上传文件 / 采集器写入 / 导入日志）
      ↓
② 平台解析，抽出候选数据
      ↓
③ 【待确认预览区】 ← 关键环节，绝不跳过
   表格展示解析结果，可逐行改 / 删错行 / 补缺失 / 做列名映射
      ↓
④ 点击「确认入库」
      ↓
⑤ 写入本地数据库，记录来源 + 时间
```

> 实时采集数据量大，改为**批次确认**：每批展示统计摘要（条数、时间范围、功率区间、异常值个数），确认后整批入库。

### 5.7 文件格式可靠性（说实话）

| 格式 | 可靠性 | 注意事项 |
|---|---|---|
| Excel (.xlsx/.xls) | 🟢 高 | 首选 |
| CSV / 文本 | 🟢 高 | 自动识别 UTF-8 / GBK |
| Word (.docx) | 🟡 中 | 格式自由时易漏，**必须人工核对** |
| PDF（文本型） | 🟠 中低 | 跨页表格易错行，**必须人工核对** |
| PDF（扫描件） | 🔴 不支持 | 需 OCR，见[第 19 节](#19-已知限制与不做清单) |

---

## 6. 模块二 · 三性分析（方法论内核）

**这一节是平台区别于普通「能碳数据平台」的地方。** 前面模块负责把数据弄进来，这一节负责**从数据里读出东西**。

三性各自回答一类问题，用不同的方法，产出不同的图。

### 6.1 差异性分析 —— 「为什么这批任务比那批费电」

**核心问题**：同样产出 100 万 Token，不同的「任务类型 × 服务器机型」组合，能耗差多少？差在哪？

#### 分析矩阵（主产出）

平台的核心视图是一张 **任务类型 × 服务器机型** 的交叉矩阵：

```
              机型A(A100)   机型B(H100)   机型C(国产卡)
短文本推理      2,150         3,400          1,780      ← 单位：Tokens/kWh
长文本推理      1,240         2,980            890
批量推理        2,600         4,100          2,050
微调              —             —              —
训练              —             —              —
```

每格填 **能效（Tokens/kWh）**，可切换填充指标：单位 Token 电耗、平均功率、卡利用率、每百万 Token 碳足迹。

#### 计算方法

| 指标 | 公式 | 说明 |
|---|---|---|
| 能效 | `Token数 ÷ IT电耗_kWh` | 越大越好 |
| 单位 Token 电耗 | `IT电耗_kWh ÷ (Token数 ÷ 1e6)` | kWh/MTok |
| 机型能效比 | `机型A能效 ÷ 机型B能效` | 相对差距，>1 说明 A 更优 |
| 组内变异系数 | `标准差 ÷ 均值` | 同一组合内部的离散程度 |
| 离群标记 | 超出 `均值 ± 2×标准差` | 标出反常的任务实例 |

> **能耗归因用 IT 电耗，不用总电耗。** 因为 PUE 是机房层面的，把它摊进机型对比会污染结论——那反映的是机房好坏，不是机型好坏。机房层面的差异单独在 PUE 对比里看。

#### 输出

- 🔲 **任务×机型能效矩阵**（热力图，颜色深浅表示能效高低）
- 📊 机型能效排名 + 相对差距条形图
- 📊 任务类型能耗构成（训练/微调/推理/数据处理各占多少）
- 📈 同一任务类型在不同机型上的能效分布箱线图（看离散程度）
- ⚠️ 低效组合清单（能效显著低于同类的「任务×机型」组合）
- 🤖 Agent 归因：为什么这个组合低效

---

### 6.2 时序性分析 —— 「什么时候费电，绿电来的时候我在用吗」

**核心问题**：数据随时间怎么变？不同曲线之间对得上吗？

#### 三个层次

**① 周期识别**

把一条时序曲线拆成三部分（用滚动均值做简易分解，不引入重依赖）：

```
原始曲线 = 长期趋势 + 周期成分 + 残差
           ↑           ↑          ↑
      是不是整体   日内/周内   剩下的
      在往上走     的规律      随机波动
```

- **日内周期**：24 小时 × 日期 的热力图，一眼看出每天什么时段最忙
- **周内周期**：工作日 vs 周末的差异
- **长期趋势**：滚动 7 日/30 日均值曲线

**② 自相关**

计算不同滞后步长的自相关系数，用来验证周期是否真实存在：
- 滞后 24 小时的自相关高 → 确实有日周期
- 滞后 168 小时（7天）的自相关高 → 确实有周周期

**③ 相位差与错配分析（本节重点）**

把多条曲线叠在同一时间轴上，看它们的高峰低谷**对不对得上**：

| 曲线对 | 看什么 | 错配意味着什么 |
|---|---|---|
| **负载 vs 绿电供给** | 相位差 | 绿电来了没人用 → 弃电；负载高峰只能用市电 → 碳排高 |
| **负载 vs 电价** | 相位差 | 高负载撞上尖峰电价 → 电费高 |
| **负载 vs 碳强度** | 相位差 | 高负载撞上高碳时段 → 碳排高 |
| **IT 负载 vs 制冷电耗** | 滞后关系 | 制冷响应滞后多久，冷机策略是否合理 |

**相位差算法**：对两条归一化后的曲线做互相关，取相关系数最大处的滞后量作为相位差。

#### 输出

- 🔥 24小时 × 日期 功率热力图
- 📈 时序分解三联图（原始 / 趋势 / 周期 / 残差）
- 📊 自相关图（ACF），标出显著周期
- 🔀 **多曲线叠加图**：负载 + 绿电供给 + 电价 + 碳强度，同一时间轴
- 🎯 **错配矩阵**：各曲线对之间的相位差和相关系数
- 🤖 Agent 解读：哪个错配最值得优先解决

---

### 6.3 不稳定性分析 —— 「这个波动是正常的还是出事了」

**核心问题**：数据有多不稳？不稳到什么程度算异常？

#### 四类指标

| 类别 | 指标 | 算法 | 说明 |
|---|---|---|---|
| **波动幅度** | 滚动标准差 | 窗口内标准差，窗口默认 24 点 | 绝对波动 |
| | 变异系数 CV | `标准差 ÷ 均值` | 无量纲，可跨指标比较 |
| | 极差比 | `(最大值−最小值) ÷ 均值` | 直观的摆动范围 |
| **突变** | 变点检测 | 滑动窗口比较前后段均值，差异超阈值则标记 | 「什么时候开始变的」 |
| **尖峰** | 尖峰频次 | 超过 P95 分位数的次数 | 尖峰直接推高需量电费 |
| | 尖峰持续时长 | 每次尖峰持续多久 | 短尖峰和长高位性质不同 |
| **间歇性** | 绿电间歇指数 | 绿电供给为零的时段占比 | 绿电靠不靠得住 |
| | 供给波动率 | 绿电供给的 CV | 波动越大越难消纳 |

#### 稳定性评分（便于横向比较）

给每个被监测对象（机房 / 机型 / 服务）算一个综合稳定性评分，把上面几个指标归一化后加权。**权重可在设置里调，界面上显示当前权重**，不做隐藏的魔法数字。

> ⚠️ **平台只标记「统计上反常」，不判断「业务上是否有问题」。** 一次计划内的压测也会被标为突变点。**是不是问题由你判断**——这符合[第 1.3 节](#13-它不解决什么问题重要先说清楚)「不替你判断数据对错」的原则。

#### 输出

- 📈 滚动波动率曲线（可叠加原始曲线对照）
- 📍 突变点标记图（在时序曲线上打点，标注前后段均值变化）
- 📊 各监测对象稳定性评分排名
- ⚡ 尖峰事件清单（时刻、峰值、持续时长、是否触发新的月度最高需量）
- 🌤 绿电供给间歇性图（供给曲线 + 零供给时段阴影）
- 🤖 Agent 归因：这段异常波动可能的原因

---

### 6.4 三性之间的关系

三性不是三个孤立的功能，它们**互相印证**：

```
       差异性                时序性               不稳定性
    「谁跟谁不一样」      「什么时候变」        「变得稳不稳」
          │                    │                     │
          └────────┬───────────┴──────────┬──────────┘
                   ↓                      ↓
            找到问题组合            找到问题时段
                   └──────────┬───────────┘
                              ↓
                    Agent 综合归因 + 减碳建议
                              ↓
                    折算为每百万 Token 碳足迹的改善空间
```

**举例**：差异性发现「机型B 跑长文本效率低」→ 时序性发现「长文本任务集中在夜间高碳时段」→ 不稳定性发现「这些任务功率尖峰频繁，推高了需量电费」→ 三条线索指向同一个建议：**把长文本任务从机型B 迁到机型A，并错峰到绿电充足时段**。

---

## 7. 模块三 · 高能耗时段与绿电利用识别

三性分析给的是方法，这一节给的是**具体结论清单**：哪些时段该管，绿电用得怎么样。

### 7.1 高能耗时段识别

**什么叫高能耗时段**：一段**持续的、显著高于基线**的区间。三要素缺一不可——持续时长、超出幅度、所在环节。

**识别算法（本地确定性计算，不依赖 Agent）**

1. **算基线**：所选窗口内功率的中位数（或手工指定）
2. **标记**：连续超过 `基线 × 阈值系数`（默认 1.2）且持续超过最短时长（默认 30 分钟）的区间
3. **合并**：间隔小于 10 分钟的相邻区间合并为一段
4. **分级**：按超出幅度分为「关注 / 重要 / 严重」三档

> 所有阈值参数在界面上**可调且显示当前取值**，不做隐藏的魔法数字。

**四维归因**（识别出时段后，拆解「费在哪」）

| 维度 | 回答什么 | 输出 |
|---|---|---|
| **按环节** | IT 在跑，还是制冷在费？ | IT / 制冷 / 供配电 / 其他 的构成变化 |
| **按任务** | 哪些任务撞在这段时间？ | 任务清单 + 卡时占比 + Token 产出 |
| **按机型** | 哪些机型贡献最大？ | 机型能耗排名（关联[差异性分析](#61-差异性分析--为什么这批任务比那批费电)） |
| **按设备** | 哪些节点/卡功率反常？ | 设备功率排名、离群标记 |

### 7.2 绿电利用比例分析（v3.0 新增）

**三个层次的绿电指标**

| 指标 | 公式 | 说明 |
|---|---|---|
| **总量绿电比例** | `绿电消纳量 ÷ 总用电量` | 月报上写的那个数 |
| **分时绿电比例** | `绿电消纳量(t) ÷ 总用电量(t)` | **真正有分析价值的** |
| **绿电消纳率** | `绿电消纳量 ÷ 绿电供给量` | 供给的绿电用掉了多少 |
| **弃用绿电量** | `绿电供给量 − 绿电消纳量` | 白白浪费的部分 |

**错配识别（本节的核心价值）**

把绿电供给曲线和园区负载曲线叠在一起，识别两类问题时段：

| 问题类型 | 特征 | 意味着 | 建议方向 |
|---|---|---|---|
| **弃电时段** | 绿电供给高、负载低 | 绿电白白浪费 | 把可延迟任务迁进来 |
| **高碳时段** | 负载高、绿电供给低 | 只能烧市电，碳排高 | 把可延迟任务迁出去 |

平台自动列出这两类时段清单，并测算**「如果把 X 度电的可延迟负载从高碳时段迁到弃电时段，能减多少碳」**——这是给工作流里「措施建议」用的量化依据。

> ⚠️ **平台只算「理论可迁移量」，不判断任务能不能迁。** 任务有没有时效要求、能不能中断，只有你知道。这符合「不做实时调度」的边界（[1.3](#13-它不解决什么问题重要先说清楚)）。

### 7.3 输出

- 📋 高能耗时段清单（起止、时长、超出幅度、分级、涉及环节/任务/机型）
- 🔥 24小时 × 日期 功率热力图
- 🌤 **绿电供给 vs 负载 双曲线叠加图**，弃电时段和高碳时段着色标记
- 📊 分时绿电利用比例柱状图（24 小时分布）
- 📋 弃电时段清单 + 高碳时段清单
- 🎯 理论可迁移量与减碳潜力测算
- 📈 PUE 趋势曲线 + 异常点标记
- 🤖 Agent 归因结论

---

## 8. 模块四 · 百万 Token 碳足迹测算

**这是平台的顶层产出**——一个能对外说的数字。全部为确定性计算。

### 8.1 计算链路

```
       Token 产出量                    电耗
      (tokens_in + tokens_out)      E_IT (IT 电耗)
              │                          │
              │                    E_total = E_IT × PUE
              │                          │
              │                   ┌──────┴──────┐
              │                   ↓             ↓
              │             运营碳排        隐含碳
              │            (地域法/市场法)  (硬件摊销)
              │                   └──────┬──────┘
              │                          ↓
              │                    总碳足迹 C_total
              └──────────┬───────────────┘
                         ↓
        每百万 Token 碳足迹 = C_total ÷ (Token数 ÷ 1,000,000)
                         ↓
                   单位：gCO₂e / MTok
```

### 8.2 核心公式

**① 每百万 Token 碳足迹（顶层指标）**

```
每百万Token碳足迹 = 总碳足迹_gCO2e ÷ (Token数 ÷ 1,000,000)
```

**② 运营碳排 —— 地域法（location-based）**

```
C_location = E_total × 区域电网排放因子
```

**③ 运营碳排 —— 市场法（market-based）**

```
C_market = (E_total − E_绿电 − E_绿证抵扣) × 区域电网因子 + E_绿电 × 绿电因子
```
> 绿电因子通常取 0。**两种算法必须同时输出**，报告中注明依据。

**④ 隐含碳（硬件摊销）**

```
C_embodied = Σ [ 设备隐含碳 × (本次使用时长 ÷ 设计寿命) ]
           设计寿命_h = 寿命年数 × 365 × 24
```

**⑤ 每百万 Token 电耗（去掉因子影响，纯看能效）**

```
每百万Token电耗 = E_total_kWh ÷ (Token数 ÷ 1,000,000)
```

完整公式见[附录 B](#附录-b--核心公式与单位换算)。

### 8.3 四个测算口径

| 口径 | 回答 | 典型用途 |
|---|---|---|
| **按 Token** ⭐ | 每百万 Token 排多少碳 | **对外交代、横向对标** |
| 按时段 | 这个月/这一天排了多少 | 月度报告、趋势分析 |
| 按任务 | 这个任务/模型排了多少 | 对业务方交代 |
| 按机型 | 这批机器排了多少 | 设备选型、退役决策 |

### 8.4 口径标注（硬性要求）

每百万 Token 碳足迹这个数，**必须连同下列口径信息一起展示**，否则数字没有意义、无法比较：

| 必须标注 | 可选值 |
|---|---|
| Token 统计口径 | 输入+输出 / 仅输出 |
| 碳算法 | 地域法 / 市场法（**两个都给**） |
| 是否含隐含碳 | 含 / 不含 |
| 电耗边界 | 仅 IT / 含 PUE（默认含 PUE） |
| 因子版本 | 如 v1.2 |
| 统计周期 | 如 2026-07 |

> **例**：`42.3 gCO₂e/MTok（地域法，输入+输出，含PUE，含隐含碳，因子v1.2，2026年7月）`
> 没有这一串后缀的数字，不要拿出去用。

### 8.5 Scope 归集

| Scope | 算力园区包含什么 |
|---|---|
| Scope 1 | 柴油发电机、备用锅炉（通常很小） |
| Scope 2 | **外购电力**（绝对大头） |
| Scope 3 | 硬件隐含碳、上游燃料、外购蒸汽/冷 |

### 8.6 输出

- 🎯 **每百万 Token 碳足迹主卡片**（地域法 / 市场法双数值 + 完整口径标注）
- 📈 每百万 Token 碳足迹趋势图（看单位产品碳排是在降还是在升）
- 🥧 构成图：按 Scope / 按环节（IT vs 制冷）/ 运营碳 vs 隐含碳
- 📊 分机型的每百万 Token 碳足迹对比（关联差异性分析）
- 🔍 **明细可下钻**：每条结果能展开看「哪些活动数据 × 哪个因子（哪版）= 多少」
- 💾 导出 Excel + 图表 PNG

---

## 9. 模块五 · 能源成本与碳资产分析

### 9.1 能源成本

**电费构成**

```
总电费 = 电量电费 + 需量电费 + 其他费用
        ↑              ↑
  Σ(各时段电量 × 该时段电价)   按月度最高需量计收
```

**分时电价表**（可编辑，见[第 12 节](#12-基础表管理)）

| 时段类型 | 典型时间 | 特点 |
|---|---|---|
| 尖峰 | 夏季午后、晚高峰 | 最贵 |
| 高峰 | 白天 | 贵 |
| 平段 | 上午、傍晚 | 中 |
| 低谷 | 夜间 | 最便宜，**通常碳强度也更低** |

**成本-碳双维分析**

把同一时间轴上的「度电成本」和「度电碳强度」并排看，找出**又贵又脏**的时段：

| 时段 | 电价 | 碳强度 | 结论 |
|---|---|---|---|
| 尖峰 | 高 | 高 | ⚠️ 优先转移 |
| 低谷 | 低 | 低 | ✅ 优先安排 |
| 绿电高峰 | 中 | 极低 | ✅ 最优先安排 |

**折算到产品口径**：`每百万 Token 电费成本 = 总电费 ÷ (Token数 ÷ 1e6)`，和碳足迹用同一个功能单位，方便一起看。

### 9.2 碳资产台账

**只做台账和测算，不做交易**（见 [1.3](#13-它不解决什么问题重要先说清楚)）。

| 资产类型 | 记录什么 |
|---|---|
| 碳配额（CEA） | 免费分配量、有偿购买量、当前持仓、已履约量 |
| 绿电 | 采购电量、对应时段、合同编号 |
| 绿证（GEC） | 证书数量、对应电量、购买时间 |
| CCER | 持有量、可抵销比例上限 |

**履约缺口测算**

```
履约缺口 = 核算排放量 − 免费配额 − 已购配额 − 绿电抵扣 − CCER抵销
```

> - **> 0**：缺口，需补足 → 界面标红
> - **< 0**：盈余
> - CCER 抵销通常有比例上限（如不超过应履约量的 5%），平台按设置里填的上限自动截断，并提示截断了多少

### 9.3 输出

- 💰 电费总额卡片 + 分时段构成图
- ⚡ 需量曲线 + 月度最高需量标记（关联[不稳定性分析](#63-不稳定性分析--这个波动是正常的还是出事了)的尖峰事件）
- 🔀 成本-碳双维散点图（横轴成本、纵轴碳强度，按时段着色）
- 🎯 每百万 Token 成本 + 每百万 Token 碳足迹 对照卡片
- 📒 碳资产台账表
- 🚨 履约缺口卡片（缺口标红 + 来源拆解）
- 💾 导出 Excel + 图表 PNG

---

## 10. 模块六 · 低碳算力工作流

前面模块是能力，这一节把它们串成**每月照着跑的固定动作**。这是平台的最终产出物。

### 10.1 工作流闭环

```
      ┌─────────────────────────────────────────────┐
      ↓                                             │
  ① 采集   → 能耗 / Token / 绿电 / 任务与机型台账     │
      ↓                                             │
  ② 分析   → 三性分析（差异性·时序性·不稳定性）        │
      ↓                                             │
  ③ 测算   → 每百万Token碳足迹 + 成本 + 碳资产        │
      ↓                                             │
  ④ 识别   → 高能耗时段 + 弃电时段 + 低效组合         │
      ↓                                             │
  ⑤ 建议   → Agent 给减碳措施 + 预期收益              │
      ↓                                             │
  ⑥ 执行   → 你和运维去落地（平台不自动执行）          │
      ↓                                             │
  ⑦ 复核   → 下周期回测，措施到底有没有效 ─────────────┘
```

**⑦ 复核是闭环的关键**，也是最容易被略过的一步。平台把每条采纳的措施记录下来，下周期自动对比措施前后的**同口径**数据，给出「实际减碳量 vs 预期减碳量」。措施有没有用，用数字说话。

> **同口径**指：相同的 Token 统计口径、相同的因子版本、相同的电耗边界。口径不一致的对比没有意义，平台会在口径不一致时明确警告。

### 10.2 固化形式

- **模板**：内置「月度能碳复盘」「季度履约盘点」两个模板，规定每步做什么、看哪些图、填哪些结论
- **进度**：界面上以清单形式呈现，做完一步勾一步，中断了下次接着做
- **留痕**：每次跑完存成一次「工作流实例」，含数据快照、因子版本、Agent 结论、你的批注
- **复用**：下月点「新建实例」，自动带上上次的参数配置

### 10.3 措施台账

| 字段 | 说明 |
|---|---|
| 措施描述 | 如「将长文本推理任务从机型B 迁至机型A，并错峰至 10:00-15:00」 |
| 关联发现 | 来自哪次分析的哪条结论（可追溯） |
| 提出来源 | Agent 建议 / 人工提出 |
| 预期收益 | 预计减碳量、预计降本金额、预计每百万Token碳足迹改善 |
| 状态 | 待评估 / 已采纳 / 已执行 / 已复核 / 已否决 |
| 执行时间 | |
| 实际收益 | 复核时由平台按同口径回测填入 |

---

## 11. Agent 设计规格

**先读完 [1.5 核心设计原则](#15-核心设计原则)的数据边界部分再看本节。**

### 11.1 Agent 是什么、不是什么

| Agent **是** | Agent **不是** |
|---|---|
| 会主动调用平台功能查数、算数，再综合出结论的分析助手 | 聊天机器人 |
| 三性归因、减碳建议、报告初稿的产出者 | 碳足迹数字的计算者（数字由测算层算死） |
| 可以随时关掉的**增强功能** | 平台运行的必需品 |

**一句话**：**数字由代码算，话由 Agent 说。**

### 11.2 四项职责

| 职责 | 输入 | 输出示例 |
|---|---|---|
| **归因** | 三性分析的聚合结果 | 「机型B 跑长文本能效仅为机型A 的62%，且这类任务集中在夜间高碳时段」 |
| **建议** | 归因结论 + 成本碳双维 + 绿电错配 | 具体措施 + 预期减碳/降本量 + 可行性说明 |
| **报告** | 本期全部聚合结果 | 报告初稿（结论、图表说明、建议清单） |
| **答疑** | 你的自然语言提问 | 调工具查数后回答，并附依据 |

### 11.3 工具集（Agent 能调用的本地函数）

**所有工具在本地执行**，Agent 只收到返回值，且返回的一律是**聚合结果**，不含原始明细。

| 工具 | 作用 | 返回什么 |
|---|---|---|
| `query_energy_summary` | 查时段能耗汇总 | 总电耗、IT/制冷分项、PUE、功率区间统计 |
| `analyze_variability` | 跑差异性分析 | 任务×机型能效矩阵、离群组合、变异系数 |
| `analyze_temporality` | 跑时序性分析 | 周期成分、自相关、各曲线对相位差 |
| `analyze_instability` | 跑不稳定性分析 | 滚动波动率、突变点、尖峰事件、稳定性评分 |
| `detect_high_load` | 高能耗时段识别 | 时段清单（起止、时长、超出幅度、分级） |
| `query_green_power` | 绿电利用分析 | 分时绿电比例、消纳率、弃电/高碳时段清单 |
| `calc_token_footprint` | 每百万Token碳足迹测算 | 数值 + 完整口径标注 + 构成 |
| `query_cost` | 查成本 | 分时段电费、需量费、度电成本、每百万Token成本 |
| `query_carbon_asset` | 查碳资产 | 配额、绿电、绿证、履约缺口 |
| `list_jobs` | 查时段内任务 | 任务数、类型分布、卡时占比（**任务名默认脱敏**） |
| `get_factors` | 查基础表 | 因子/电价/机型台账的数值、版本、来源 |
| `compare_periods` | 同口径对比两个时段 | 各指标差异和变化率（**口径不一致时报错，不静默对比**） |

> **不提供的工具**：没有「读原始记录」「读设备序列号」「执行调度命令」「改数据库」这类工具。Agent **只读、只拿聚合值**。

### 11.4 数据边界的具体执行

**允许出网**：聚合统计量、时间范围、指标名、因子数值、机型的**脱敏代号**、你主动输入的问题。

**禁止出网**：逐条功耗明细、设备序列号 / IP / 主机名、机房物理位置、原始任务名和模型名（默认脱敏为 `job-001` / `机型A`，可在设置里关闭脱敏）、任何文件原文、Token 明细日志。

**执行机制**：
1. 每次调 API 前，界面弹出 **「本次将发送的内容」** 完整预览，你点确认才发
2. 可勾选「本会话内不再询问」，但**换一个会话就恢复询问**
3. 所有 API 交互写入 `data/agent_audit.log`（时间、发送内容摘要、返回摘要、token 消耗）
4. 设置页有全局开关：**纯本地模式**（关闭 Agent，其余功能全部保留）

### 11.5 技术实现要点

| 项 | 选择 | 说明 |
|---|---|---|
| SDK | `anthropic`（Python 官方 SDK） | 见[第 16 节](#16-技术栈清单) |
| 模型 | `claude-opus-5` | 除非你明确要求换 |
| 循环方式 | Tool Runner（`client.beta.messages.tool_runner`） | 不手写 agent 循环，工具用 `@beta_tool` 定义 |
| 思考模式 | `thinking={"type": "adaptive"}` | 三性归因属于需要推理的任务 |
| 流式 | 长回答用 `.stream()` + `.get_final_message()` | 避免请求超时 |
| 密钥 | 环境变量 `ANTHROPIC_API_KEY` | **绝不硬编码，绝不提交进 Git** |

### 11.6 Agent 出错时怎么办（不静默失败）

| 情况 | 平台行为 |
|---|---|
| 没配 API Key | 明确提示「未配置密钥，Agent 不可用，其余功能正常」，不崩溃 |
| 网络不通 / API 报错 | 显示具体错误和建议，**测算结果照常展示** |
| Agent 结论与测算数字矛盾 | 以**测算数字为准**，提示「AI 结论与计算结果存在出入，请以计算结果为准」 |
| 超出 token 预算 | 提示已用量，询问是否继续 |

### 11.7 界面视觉区分（硬性要求）

| 内容类型 | 呈现方式 |
|---|---|
| 平台计算结果 | 常规样式，直接展示 |
| Agent 输出 | **🤖 图标 + 浅色底框 + 「AI 分析建议，请自行核实」标注** |

---

## 12. 基础表管理

四张可编辑的基础表，是所有测算的依据。

| 表 | 内容 | 关键字段 |
|---|---|---|
| **排放因子表** | 电力、燃料等的碳换算系数 | 因子名、单位、数值、来源、适用年份 |
| **分时电价表** | 各时段电价和需量单价 | 时段类型、生效月份、时间区间、电价 |
| **服务器机型台账** | 异构机型的规格（差异性分析基准） | 机型、卡型、额定功率、隐含碳、寿命 |
| **园区结构表** | 园区 → 机房 → 机型的层级 | park_id、room_id、机型清单、PUE |

**四张表通用能力**：查看 / 编辑 / 新增 / 删除、导入 Excel 覆盖、导出备份。

**版本快照（硬性要求）**：每次修改自动存一版。历史测算结果记录当时用的版本，**基础表改动不影响已有结果**。

> 电价、PUE、隐含碳随地区和设备差异极大，**必须按你的实际情况填写**，内置值仅为示例。初始条目见[附录 A](#附录-a--排放因子初始条目)。

---

## 13. 数据模型

本地 SQLite（`data/app.db`）。十二张表。

### 13.1 活动数据表

**表 1 · `energy_records`（能耗记录）**

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` / `ts_end` | 时间 | 是 | 2026-07-03 14:00 | 统计区间 |
| `park_id` / `room_id` | 文本 | 否 | park-01 / room-A | 园区层级 |
| `segment` | 文本 | 是 | IT | IT / 制冷 / 供配电 / 其他 / 总计 |
| `server_model` | 文本 | 否 | H100-8卡 | 关联机型 |
| `device_id` | 文本 | 否 | node-12 | 关联设备 |
| `energy_kwh` | 小数 | 是 | 420.5 | 电耗 |
| `avg_power_kw` | 小数 | 否 | 420.5 | 平均功率 |
| `source_type` | 文本 | 是 | 文件 | 文件 / 采集 / 手工 |
| `source_file` | 文本 | 否 | 7月台账.xlsx | |
| `imported_at` | 时间 | 是 | | |

**表 2 · `token_records`（Token 计量）** ⭐ v3.0 新增

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` / `ts_end` | 时间 | 是 | | |
| `service_id` | 文本 | 否 | svc-chat-a | 服务/模型标识（可脱敏） |
| `job_type` | 文本 | 是 | 推理 | 推理 / 训练 / 微调 |
| `tokens_in` | 整数 | 否 | 1240000 | 输入 Token |
| `tokens_out` | 整数 | 否 | 380000 | 输出 Token |
| `tokens_train` | 整数 | 否 | | 训练消耗 Token（训练任务用） |
| `server_model` | 文本 | 否 | H100-8卡 | 跑在哪种机型 |
| `node_list` | 文本 | 否 | node-12~15 | |
| `source_file` / `imported_at` | | 是 | | |

**表 3 · `green_power_records`（绿电供给与消纳）** ⭐ v3.0 新增

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` / `ts_end` | 时间 | 是 | | |
| `green_type` | 文本 | 是 | 自建光伏 | 自建光伏 / 风电PPA / 其他 |
| `supply_kwh` | 小数 | 是 | 180.0 | 该时段可用绿电 |
| `consumed_kwh` | 小数 | 是 | 145.0 | 实际消纳 |
| `park_id` | 文本 | 否 | park-01 | |
| `contract_no` | 文本 | 否 | PPA-2026-007 | |
| `source_file` / `imported_at` | | 是 | | |

> 绿证（GEC）不带时间属性，记在 `carbon_assets` 表，**不进本表**。

**表 4 · `job_records`（任务记录）**

| 字段名 | 类型 | 必填 | 示例 |
|---|---|---|---|
| `id` / `job_id` | | 是 | job-20260703-001 |
| `job_name` | 文本 | 否 | 模型A训练 |
| `job_type` | 文本 | 是 | 训练 |
| `ts_start` / `ts_end` | 时间 | 是 | |
| `gpu_hours` | 小数 | 否 | 3840 |
| `server_model` | 文本 | 否 | A100-8卡 |
| `node_list` | 文本 | 否 | node-12~15 |
| `owner` | 文本 | 否 | 视觉组 |
| `source_file` / `imported_at` | | 是 | |

### 13.2 结果表

**表 5 · `carbon_records`（碳排放记录）**

| 字段名 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| `id` | 整数 | 是 | 1 | |
| `ts_start` / `ts_end` | 时间 | 是 | | |
| `scope` | 文本 | 是 | Scope 2 | |
| `category` | 文本 | 是 | 运营碳排 | 运营碳排 / 隐含碳 |
| `emission_gco2e` | 小数 | 是 | 68400000 | 统一存 gCO₂e |
| `method` | 文本 | 是 | location | location / market |
| `tokens_basis` | 整数 | 否 | 1620000 | 对应的 Token 数 |
| `gco2e_per_mtok` | 小数 | 否 | 42.3 | **每百万Token碳足迹** |
| `token_scope` | 文本 | 否 | in+out | in+out / out_only |
| `include_embodied` | 布尔 | 是 | 是 | 是否含隐含碳 |
| `energy_boundary` | 文本 | 是 | with_pue | it_only / with_pue |
| `factor_id` / `factor_version` | | 是 | 3 / v1.2 | 用了哪版因子 |
| `job_id` / `server_model` | 文本 | 否 | | 归属 |
| `calc_basis` | 文本 | 是 | | 计算依据（下钻用） |
| `calculated_at` | 时间 | 是 | | |

> `token_scope` / `include_embodied` / `energy_boundary` / `factor_version` 四个字段就是[第 8.4 节](#84-口径标注硬性要求)要求的口径标注，**必须随结果一起存**，否则数字无法比较。

**表 6 · `analysis_results`（三性分析结果）** ⭐ v3.0 新增

| 字段名 | 类型 | 说明 |
|---|---|---|
| `id` | 整数 | |
| `analysis_type` | 文本 | variability / temporality / instability |
| `ts_start` / `ts_end` | 时间 | 分析覆盖周期 |
| `dimension` | 文本 | 分析维度，如 `job_type×server_model` |
| `metric_name` | 文本 | 如 `tokens_per_kwh`、`cv`、`phase_shift_h` |
| `metric_value` | 小数 | 数值 |
| `params` | 文本 | 当时用的参数（JSON，如窗口大小、阈值） |
| `calculated_at` | 时间 | |

> 存参数是为了可复现——同一份数据换了窗口大小结果就不同，不记参数的结果没法解释。

### 13.3 基础表

**表 7 · `emission_factors`（排放因子表）**

| 字段名 | 类型 | 必填 | 示例 |
|---|---|---|---|
| `id` / `factor_name` | | 是 | 电力（华东电网） |
| `category` | 文本 | 是 | 电力 |
| `input_unit` / `output_unit` | 文本 | 是 | kWh / gCO2e |
| `factor_value` | 小数 | 是 | 570.3 |
| `data_source` | 文本 | 是 | 生态环境部公布值 |
| `applicable_year` | 文本 | 否 | 2024 |
| `version` | 文本 | 是 | v1.0 |
| `is_builtin` | 布尔 | 是 | 是 |
| `updated_at` | 时间 | 是 | |

**表 8 · `tariff_table`（分时电价表）**

| 字段名 | 必填 | 示例 |
|---|---|---|
| `id` / `period_type` | 是 | 尖峰 |
| `month_range` | 是 | 7-9 |
| `time_start` / `time_end` | 是 | 14:00 / 17:00 |
| `price_per_kwh` | 是 | 1.2038 |
| `demand_charge` | 否 | 42.0 |

**表 9 · `server_specs`（服务器机型台账）** ⭐ v3.0 新增

| 字段名 | 必填 | 示例 | 说明 |
|---|---|---|---|
| `id` / `server_model` | 是 | H100-8卡 | 机型标识 |
| `accelerator` / `card_count` | 否 | H100 / 8 | 加速卡 |
| `rated_power_w_per_card` | 否 | 700 | 单卡额定功率 |
| `rated_power_kw` | 否 | 10.2 | 整机额定功率 |
| `embodied_kgco2e` | 是 | 3000 | 隐含碳 |
| `lifetime_years` | 是 | 5 | 设计寿命 |
| `room_id` | 否 | room-A | 所属机房 |
| `version` / `updated_at` | 是 | v1.0 | |

**表 10 · `carbon_assets`（碳资产台账）**

| 字段名 | 必填 | 示例 |
|---|---|---|
| `id` / `asset_type` | 是 | 碳配额 |
| `year` | 是 | 2026 |
| `amount` / `unit` | 是 | 9800 / tCO2e |
| `direction` | 是 | 获得 / 使用 |
| `contract_no` | 否 | |
| `note` / `imported_at` | | |

### 13.4 流程表

**表 11 · `measures`（措施台账）** 字段见 [10.3](#103-措施台账)。

**表 12 · `workflow_runs`（工作流实例）**

| 字段名 | 说明 |
|---|---|
| `id` / `template` | 用了哪个模板 |
| `period` | 覆盖周期，如 2026-07 |
| `status` | 进行中 / 已完成 |
| `steps_done` | 已完成步骤（JSON） |
| `snapshot_params` | 当时的口径与参数快照（JSON） |
| `agent_conclusion` | Agent 结论存档 |
| `user_note` | 你的批注 |
| `created_at` | |

---

## 14. 界面结构

Streamlit 侧边栏导航 + 主区域，共八页。

```
┌──────────────────┬──────────────────────────────────┐
│  能碳监测平台      │        （主内容区）                │
│  算力工业园区      │                                  │
│                  │   当前页的图表、表格、操作按钮      │
│  🏠 总览          │                                  │
│  📥 数据接入      │                                  │
│  🔬 三性分析      │  ← 方法论内核                     │
│  🔥 高能耗与绿电  │                                  │
│  🌱 Token碳足迹   │  ← 顶层指标                       │
│  💰 成本与碳资产  │                                  │
│  🔄 工作流        │                                  │
│  ⚙️ 设置与基础表  │                                  │
│                  │                                  │
│  ─────────────   │                                  │
│  🤖 Agent  [开/关]│  ← 全局开关，随时可关             │
└──────────────────┴──────────────────────────────────┘
```

| 页面 | 内容 |
|---|---|
| 🏠 **总览** | 核心卡片（每百万Token碳足迹、绿电比例、PUE、电费、履约缺口）+ 最近导入 + 待办工作流 |
| 📥 **数据接入** | 四类数据的上传/采集/导入 → 待确认预览区 → 确认入库 |
| 🔬 **三性分析** | 三个 Tab：差异性（任务×机型矩阵）/ 时序性（周期+错配）/ 不稳定性（波动+突变） |
| 🔥 **高能耗与绿电** | 参数设置 → 高能耗时段清单 → 热力图 → 绿电双曲线图 → 弃电/高碳时段 → 四维归因 |
| 🌱 **Token碳足迹** | 口径选择 → 主卡片（双算法+完整标注）→ 构成图 → 分机型对比 → 明细下钻 → 导出 |
| 💰 **成本与碳资产** | 电费构成 → 需量曲线 → 成本-碳双维图 → 碳资产台账 → 履约缺口 |
| 🔄 **工作流** | 模板选择 → 七步清单（勾选推进）→ 措施台账 → 历史实例 |
| ⚙️ **设置与基础表** | 四张基础表（可编辑+版本历史）+ API 密钥 + 数据边界设置 + 口径默认值 |

**统一的页面顺序**：`输入 → 确认 → 参数 → 看图 → 看明细 → 导出`。八个页面结构一致，学会一个就会用全部。

---

# 第三部分 · 技术与工程

## 15. 技术方案决策记录

> 记录「为什么选这个、还考虑过什么」。以后谁问「为啥不用 XX」，看这里。

### 15.1 主方案：渐进式路线（v1.0 决策，沿用）

**阶段一~四** → Python + Streamlit
**阶段五** → PyInstaller 打包成 Windows / macOS 双击即用的启动包

**决策日期**：2026-08-21 ｜ **决策人**：项目所有者

**为什么选它**
1. **风险最低**：先小成本做出能跑的东西，发现需求不对可以马上改
2. **代码零浪费**：打包阶段只加一层配置，不需要重写
3. **匹配使用者水平**：Streamlit 代码量最少，零基础也看得懂、改得动
4. **匹配任务性质**：数据分析 + AI 工具，Python 在文件解析、统计分析、图表、LLM SDK 上生态最成熟

**备选方案对比（留档）**

| 维度 | A · Streamlit | B · PySide6 原生 | C · Tauri/Electron + React |
|---|---|---|---|
| 最终形态 | 浏览器打开的本地网页 | 原生桌面窗口 | 现代化桌面客户端 |
| 界面美观度 | 一般，工具感 | 中等 | 最好 |
| 代码复杂度 | 最低 ⭐ | 中高（约 A 的 3–5 倍） | 最高 |
| 零基础可维护性 | 最好 ⭐ | 一般 | 最差 |
| **统计分析能力** | 最强（pandas/numpy）⭐ | 最强 ⭐ | 弱，要造轮子 |
| LLM SDK 生态 | 最成熟 ⭐ | 最成熟 ⭐ | 中等 |
| GPU 功耗采集 | `pynvml` 现成 ⭐ | 现成 ⭐ | 需绕道调 Python |
| 跨平台成本 | 零，代码完全一致 ⭐ | 需分别打包 | 需分别打包 |
| 开发周期 | 最短 ⭐ | 中等偏长 | 最长 |

**为什么没选 B**：界面代码复杂度对零基础不友好，且 macOS 打包必须有 Mac 实机。
**为什么没选 C**：需同时掌握前后端两套栈，学习成本最高；而「界面漂亮」不是本项目的核心矛盾——核心矛盾是**把三性分析做对、把每百万 Token 碳足迹算准**，那是 Python 的主场。

### 15.2 v3.0 新增决策（2026-09-02）

| 决策 | 选择 | 理由 | 代价 |
|---|---|---|---|
| **分析框架** | 算力工业园区（Token 即产品） | 可直接复用工业碳核算成熟方法论，业务方听得懂 | 需要 Token 计量数据，拿不到就退化 |
| **核心指标** | 每百万 Token 碳足迹 | 单位产品碳足迹是通用语言，可横向对标 | 口径敏感，必须强制标注 |
| **方法论主线** | 差异性 / 时序性 / 不稳定性 | 这三性是能碳数据的固有特征，也是「不只是数据平台」的落点 | 分析代码量显著增加 |
| **能耗归因边界** | 机型对比用 IT 电耗，不含 PUE | PUE 是机房属性，摊进机型对比会污染结论 | 需要分项计量，粒度要求高 |
| **Token 口径** | 默认 输入+输出，可切换，**强制标注** | 行业无统一标准，只能显式化而非替用户决定 | 界面和数据模型要多带四个口径字段 |
| **绿电时间属性** | 绿电按时序曲线处理，绿证按总量处理 | 绿证本就不带时间属性，混在一起会算错分时比例 | 两套逻辑 |
| **统计库** | 先用 pandas + numpy 自实现 | 滚动统计、CV、自相关、简易分解都能做，不新增依赖 | 季节性分解和变点检测不如专业库严谨 |
| **是否引入 statsmodels** | **暂不引入，列为待定** | 依赖精简优先；若阶段二发现自实现不够用再评估 | 现阶段分解方法较朴素 |

### 15.3 沿用的决策

| 决策 | 选择 | 理由 |
|---|---|---|
| Agent 类型 | 大模型驱动的智能体 | 三性归因需处理非结构化情况、需解释原因 |
| 数据边界 | 原始明细留本地，只发聚合摘要 | 在「能用 AI」和「数据安全」之间取平衡 |
| 数字 vs 结论分离 | 碳足迹由代码算死，Agent 只解读 | 测算必须确定可复现，不能因模型随机性变化 |
| Agent 可关闭 | 纯本地模式全局开关 | 没网、没密钥、数据敏感时平台仍可用 |
| 碳资产 | 做台账和履约测算，不做交易 | 交易涉及资质和外部接口，超出单机工具范畴 |
| 双算法并行 | 地域法和市场法同时输出 | 口径不同、用途不同，只给一个会误导 |
| 数据存储 | 本地 SQLite | Python 自带，零安装，单文件好备份 |
| 解析结果确认 | 必须人工确认才入库 | 解析出错是常态 |
| 基础表版本快照 | 每次修改存版本 | 保证历史结果可追溯、不被静默改变 |

---

## 16. 技术栈清单

| 依赖 | 版本要求 | 它负责干什么（大白话） | 阶段 |
|---|---|---|---|
| **Python** | 3.11 或更高 | 编程语言本身，软件的地基 | 全程 |
| **streamlit** | ≥1.30 | 把 Python 代码变成网页界面 | 一 |
| **pandas** | ≥2.0 | 处理表格数据，相当于「代码版的 Excel」 | 一 |
| **numpy** | ≥1.26 | 数值计算，三性分析的统计指标靠它 | 一 |
| **openpyxl** | ≥3.1 | 读写 .xlsx 文件 | 一 |
| **xlrd** | ≥2.0 | 读老格式 .xls 文件 | 一 |
| **plotly** | ≥5.18 | 画交互式图表（热力图、箱线图、多曲线叠加都靠它） | 一 |
| **SQLite** | Python 自带 | 本地数据库，无需安装 | 一 |
| **pdfplumber** | ≥0.11 | 读 PDF 里的文字和表格 | 二 |
| **python-docx** | ≥1.1 | 读 Word 里的文字和表格 | 二 |
| **anthropic** | ≥1.0 | **Claude 官方 Python SDK，Agent 靠它调模型** | 三 |
| **pynvml** | ≥11.5 | 读 GPU 实时功耗（`nvidia-smi` 的底层库） | 四 |
| **PyInstaller** | ≥6.0 | 打包成双击就能开的启动包 | 五 |

**待定依赖**：`statsmodels`（严谨的季节性分解和变点检测）。**默认不装**，阶段二若发现 pandas 自实现不够用再评估并补记到 [15.2](#152-v30-新增决策2026-09-02)。

**为什么不用更多库**：依赖越少，安装越不容易出错。上面每个都是必需的。

> **`anthropic` 1.x 注意**：基于 `httpx2` 而非 `httpx`。若自己要用 HTTP 库，写 `import httpx2 as httpx`，用 `httpx` 包的对象会在请求时失败。

---

## 17. 目录结构

```
LBY97/
├── CLAUDE.md                      # 本文档（产品说明书 + 项目宪法）
├── README.md                      # 怎么装、怎么启动
├── requirements.txt               # 依赖清单
├── .env.example                   # 环境变量模板（API 密钥填这里）
├── .gitignore                     # 不提交的文件（数据库、密钥、缓存）
│
├── app.py                         # 程序入口，运行它启动平台
│
├── pages/                         # 功能页面（Streamlit 自动读这个文件夹）
│   ├── 1_数据接入.py
│   ├── 2_三性分析.py
│   ├── 3_高能耗与绿电.py
│   ├── 4_Token碳足迹.py
│   ├── 5_成本与碳资产.py
│   ├── 6_工作流.py
│   └── 7_设置与基础表.py
│
├── core/                          # 核心逻辑，界面代码调用它
│   ├── __init__.py
│   ├── db.py                      # 数据库读写
│   ├── models.py                  # 十二张表的结构定义
│   ├── parsers/                   # 文件解析
│   │   ├── excel_parser.py
│   │   ├── pdf_parser.py
│   │   └── docx_parser.py
│   ├── collectors/                # 实时采集
│   │   ├── gpu_collector.py       # pynvml
│   │   └── ipmi_collector.py
│   ├── analysis/                  # ⭐ 三性分析（方法论内核）
│   │   ├── variability.py         # 差异性：任务×机型矩阵、CV、离群
│   │   ├── temporality.py         # 时序性：周期分解、自相关、相位差
│   │   └── instability.py         # 不稳定性：滚动波动、变点、尖峰
│   ├── detector.py                # 高能耗时段识别
│   ├── green_power.py             # 绿电利用与错配分析
│   ├── carbon.py                  # 碳足迹测算（含每百万Token）
│   ├── cost.py                    # 电费与成本
│   ├── assets.py                  # 碳资产与履约缺口
│   ├── workflow.py                # 工作流引擎
│   ├── charts.py                  # 图表生成
│   └── exporter.py                # 导出 Excel / 图片 / 报告
│
├── agent/                         # Agent 层（可整体禁用）
│   ├── __init__.py
│   ├── client.py                  # Claude SDK 封装
│   ├── tools.py                   # 工具集定义（@beta_tool）
│   ├── boundary.py                # 数据边界过滤 + 脱敏
│   ├── prompts.py                 # 系统提示词
│   └── audit.py                   # 调用审计日志
│
├── data/
│   ├── factors.csv                # 内置排放因子表
│   ├── tariff.csv                 # 分时电价表
│   ├── server_specs.csv           # 服务器机型台账
│   ├── park.csv                   # 园区结构表
│   ├── app.db                     # 本地数据库（自动生成，不进 Git）
│   ├── agent_audit.log            # Agent 调用审计（不进 Git）
│   └── versions/                  # 基础表历史版本快照
│
├── samples/                       # 示例文件，用来测试
│   ├── 示例_机房用电台账.xlsx
│   ├── 示例_Token计量.csv
│   ├── 示例_绿电出力曲线.csv
│   └── 示例_任务日志.csv
│
└── tests/
    ├── test_carbon.py             # 碳测算公式（最重要）
    ├── test_boundary.py           # 数据边界过滤是否生效（最重要）
    ├── test_analysis.py           # 三性分析的统计指标
    ├── test_parsers.py
    └── test_detector.py
```

**当前进度**：只有 `CLAUDE.md` 和 `README.md`，其余是待建蓝图。

---

## 18. 开发路线图

每阶段标注「做完之后你能干什么」，方便验收。

### ✅ 阶段零 · 产品文档（已完成）

**产出**：`CLAUDE.md`、`README.md`
**你能干什么**：明确知道要做什么；以后跟 Claude Code 说「按 CLAUDE.md 做」即可

---

### 🔨 阶段一 · 地基 + 核心指标（下一步）

**做什么**
1. Streamlit 框架 + 八个页面骨架
2. SQLite 数据库 + 十二张表
3. **Excel / CSV 导入**（能耗 + Token + 机型台账，含列名映射和待确认预览区）
4. **每百万 Token 碳足迹测算**（地域法 + 市场法 + 隐含碳 + **完整口径标注**）
5. 基础表管理（四张表，含版本快照）
6. 基础图表 + 导出 Excel

**你能干什么**
> 把机房台账和 Token 统计导进去，得到 `每百万 Token 碳足迹 = XX gCO₂e（含完整口径标注）`，看到构成图和趋势图，下钻查每个数的计算依据，导出汇总表。**这时平台已能对外交代模型服务的碳足迹。**

**验收标准**：拿一份真实数据走完「导入 → 确认 → 测算 → 下钻 → 导出」，数字与手算一致，且口径标注完整。

---

### 🔨 阶段二 · 三性分析（方法论内核）

**做什么**
1. **差异性**：任务×机型能效矩阵、变异系数、离群组合识别
2. **时序性**：周期分解、自相关、多曲线相位差与错配矩阵
3. **不稳定性**：滚动波动率、变点检测、尖峰事件、稳定性评分
4. 绿电供给曲线导入 + 分时绿电利用比例 + 弃电/高碳时段识别
5. 高能耗时段识别 + 四维归因
6. PDF / Word 解析

**你能干什么**
> 平台能告诉你哪个「任务×机型」组合最低效、负载和绿电差了几个小时、这周的波动是不是异常。**这是平台从「数据工具」变成「分析工具」的关键一步。**

**验收标准**：三性各自能对一份真实数据产出可解释的结论，且分析参数可调、可复现（参数随结果存档）。

---

### 🔨 阶段三 · 成本碳资产 + Agent 接入

**做什么**
1. 分时电价 + 需量 + 成本-碳双维图
2. 碳资产台账 + 履约缺口测算
3. **数据边界过滤 + 脱敏**（先做这个，再接模型）
4. `anthropic` SDK 封装 + 密钥管理
5. 工具集（12 个工具，`@beta_tool` 定义）
6. Tool Runner 循环 + 三性归因 / 建议 / 答疑
7. 发送前预览确认 + 审计日志 + 纯本地模式开关
8. Agent 生成报告初稿

**你能干什么**
> 点一下，Agent 自己去查数、跑三性分析，告诉你「机型B 跑长文本能效仅为机型A 的 62%，且这类任务集中在夜间高碳时段」，并给出量化的减碳建议。报告初稿自动写好。

**验收标准**：① 关掉 Agent 后其余功能全部正常；② 审计日志里确认发出的内容**不含**任何原始明细和设备标识。

---

### 🔨 阶段四 · 实时采集 + 工作流闭环

**做什么**
1. GPU 功耗采集（`pynvml`）+ 采样调度 + 降采样归档
2. IPMI / Redfish 整机功耗
3. 工作流模板（月度复盘 / 季度盘点）+ 七步清单
4. 措施台账 + 下周期同口径自动回测

**你能干什么**
> 功耗数据自动流进来；每月照着工作流跑一遍；上月采纳的措施，这月平台自动告诉你实际减了多少碳、每百万 Token 碳足迹改善了多少。

---

### 🔨 阶段五 · 打包分发

**做什么**：PyInstaller 配置 → Windows 启动包 → macOS 启动包 → 使用手册

**你能干什么**
> 双击图标就打开，不用装 Python、不用敲命令；也能发给同事，解压双击就用。

---

### 💭 阶段六 · 可选增强（远期）

- 扫描件 PDF 的 OCR
- 多园区横向对标（各园区每百万 Token 碳足迹排名）
- 碳排预测（基于时序性分析的趋势外推）
- 更严谨的统计方法（评估引入 `statsmodels`）
- 自定义报告模板

---

## 19. 已知限制与不做清单

诚实列出来，避免误解。

| 项目 | 状态 | 原因 |
|---|---|---|
| **Token 数据拿不到** | ⚠️ 核心指标失效 | 每百万 Token 碳足迹**依赖 Token 计量**。拿不到就只能退回按时段/任务口径 |
| **绿电无时序数据** | ⚠️ 降级为总量 | 只有月度总量时，分时绿电比例和错配分析做不了 |
| **分项计量粒度不足** | ⚠️ 归因变粗 | 机型级能耗归因需要分项计量。只有园区总表时，差异性分析做不到机型级 |
| 扫描件 / 图片型 PDF | ❌ 阶段一到五不做 | 需 OCR 引擎，依赖重、准确率不稳定 |
| 碳交易 / 配额撮合 / 定价 | ❌ 不做 | 涉及资质和外部接口，超出单机工具范畴 |
| 自动执行调度或制冷控制 | ❌ 不做 | 平台只给建议，执行由人决定。自动控制涉及生产安全 |
| 带约束的成本-碳联合优化求解 | ❌ 不做 | 本平台做测算、识别、归因、建议，不做优化求解器 |
| 多人协作 / 共享数据库 | ❌ 不做 | 明确单机使用；做这个需服务器、账号、权限体系 |
| 认证级碳核算报告 | ❌ 不做 | 输出仅供内部分析。ISO 14064/14067 等需第三方核证机构出具 |
| 自动判断数据正确性 | ❌ 不做 | 平台只做测算和统计标记，数据质量由使用者把关 |
| **判断「统计异常」是否为「业务问题」** | ❌ 不做 | 计划内压测也会被标为突变点。**是不是问题由你判断** |
| **Agent 完全离线运行** | ❌ 做不到 | 大模型 API 必须联网。**代价已知并接受**，用数据边界和纯本地模式控制风险 |
| Agent 结论的准确性 | ⚠️ 需自行核实 | 大模型有不确定性。**结论仅供参考，数字以平台计算为准** |
| Token 口径的行业标准 | ⚠️ 尚无统一标准 | 输入+输出 vs 仅输出，各家算法不同。平台强制标注，但**跨组织对比仍需谨慎** |
| 排放因子的权威性 | ⚠️ 需自行校准 | 内置因子为**参考值**，使用前必须按地区、年份核对更新 |
| 电价表 / PUE | ⚠️ 必须自行填写 | 随地区、季节、电压等级差异极大，内置值仅为示例 |
| 隐含碳数据 | ⚠️ 精度有限 | 厂商 EPD 报告覆盖率低，多数需用行业均值估算 |
| 时序分解的严谨性 | ⚠️ 方法朴素 | 阶段二用 pandas 自实现，不如专业统计库严谨。见 [15.2](#152-v30-新增决策2026-09-02) |
| 复杂 Excel（多层合并表头） | ⚠️ 有限支持 | 尽力解析，解析不了在预览区提示，需人工调整源文件 |
| 实时采集的权限前提 | ⚠️ 依赖环境 | 需能访问目标机器。拿不到就退回文件导入，粒度变粗 |

---

# 第四部分 · 协作约定

## 20. 常用命令

> ⚠️ 阶段一代码还没写，这些命令**要等阶段一完成后才能用**。先放在这里备查。

### Windows（在 PowerShell 或 CMD 里执行）

```powershell
# 第一次使用：创建独立环境并安装依赖
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# 配置 API 密钥（只有要用 Agent 才需要）
copy .env.example .env
# 然后用记事本打开 .env，把密钥填进去

# 每次启动平台
.venv\Scripts\activate
streamlit run app.py

# 停止平台：在命令窗口按 Ctrl + C
```

### macOS（在「终端」里执行）

```bash
# 第一次使用：创建独立环境并安装依赖
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 配置 API 密钥（只有要用 Agent 才需要）
cp .env.example .env
# 然后用文本编辑器打开 .env，把密钥填进去

# 每次启动平台
source .venv/bin/activate
streamlit run app.py

# 停止平台：在终端窗口按 Control + C
```

启动成功后浏览器自动打开 `http://localhost:8501`。

### 常见问题速查

| 现象 | 怎么办 |
|---|---|
| `python 不是内部或外部命令` | Python 没装或没勾「Add to PATH」，重装时勾上 |
| `ModuleNotFoundError` | 依赖没装全，重跑 `pip install -r requirements.txt` |
| 浏览器没自动打开 | 手动输入 `http://localhost:8501` |
| 端口被占用 | 换端口：`streamlit run app.py --server.port 8502` |
| 提示「未配置 API 密钥」 | 只影响 Agent，其余功能正常 |
| `pip` 下载慢 | 换镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| Mac 提示「无法验证开发者」 | 阶段五打包版才会遇到。系统设置 → 隐私与安全性 → 「仍要打开」 |

---

## 21. 给 Claude Code 的工作约定

> 这一节是给 AI 助手看的规则，每次会话都会自动读到。

### 21.1 语言与命名

- **面向用户的文字一律中文**：界面文案、提示、错误信息、报告标题
- **代码注释用中文**，让零基础的项目所有者看得懂
- **变量名、函数名、文件名用英文**（`pages/` 下的页面文件名例外，用中文以便侧边栏显示中文）
- 术语统一用[第 4 节](#4-名词解释零基础必读)的表述，不要自创同义词

### 21.2 开发原则

1. **先跑通，再优化**：每个功能先做最简可用版本，验证没问题再改进。不要一上来写复杂抽象
2. **小步交付**：一次只做一个功能点，做完让项目所有者能立刻看到效果
3. **不静默失败**：解析出错、采集失败、API 报错，一律在界面明确提示，绝不悄悄跳过或填默认值
4. **保持依赖精简**：不为一个小功能引入新库。确需新增时先说明理由并补记到[第 15.2 节](#152-v30-新增决策2026-09-02)
5. **任何依赖变更必须同步更新 `requirements.txt`**

### 21.3 数据相关的硬性规则

- **解析和采集结果必须经用户确认才入库**（[5.6](#56-统一接入流程)），不允许直接写库
- **每条入库数据必须记录来源和时间**，保证可追溯
- **每条碳测算结果必须存完整口径标注**：`token_scope` / `include_embodied` / `energy_boundary` / `factor_version` 四个字段缺一不可（见 [8.4](#84-口径标注硬性要求)）
- **修改基础表必须保留版本快照**，历史结果不能因基础表改动被静默改变
- **不要删除或覆盖 `data/app.db`**，那是用户的真实数据
- **地域法和市场法必须同时计算并同时展示**，只给一个会误导
- **口径不一致的数据禁止直接对比**：`compare_periods` 之类的对比功能遇到口径不一致必须报错，不允许静默对比

### 21.4 三性分析的硬性规则

1. **参数必须可见可调**：窗口大小、阈值系数、权重等一律在界面上显示当前取值，**不允许隐藏的魔法数字**
2. **参数必须随结果存档**（`analysis_results.params`）：同一份数据换个窗口大小结果就不同，不记参数的结果没法解释、没法复现
3. **机型能效对比必须用 IT 电耗**，不含 PUE。PUE 是机房属性，摊进机型对比会污染结论
4. **只标记统计异常，不下业务结论**：平台说「这是统计上的突变点」，不说「这是故障」
5. **样本量不足要明确提示**：某个「任务×机型」组合样本太少时，界面必须标注「样本不足，结论不可靠」，不能给出看似确定的数字

### 21.5 Agent 相关的硬性规则

> 这几条是安全底线，**违反任何一条都是严重问题**。

1. **数据边界不可绕过**：所有出网内容必须经过 `agent/boundary.py` 过滤。禁止在 `agent/` 以外的地方直接调 API
2. **禁止发送原始明细**：逐条记录、设备序列号/IP/主机名、机房位置、文件原文、Token 明细日志，一律不得出网
3. **API 密钥只从环境变量读**（`ANTHROPIC_API_KEY`）。**绝不硬编码，绝不写进代码或提交进 Git**，`.env` 必须在 `.gitignore` 里
4. **每次调用必须写审计日志**（`data/agent_audit.log`）
5. **Agent 不得写数据库**：工具集只提供只读、只返回聚合值的函数
6. **测算层不得依赖 Agent**：关掉 Agent 后，`core/` 的功能必须全部正常。**写代码时保证 `core/` 不 import `agent/`**
7. **Agent 输出必须标注**：界面上加 🤖 图标和「AI 分析建议，请自行核实」

### 21.6 调用 Claude API 的编码约定

- **SDK**：用官方 `anthropic` Python SDK，不要用 `requests` 手写 HTTP
- **模型**：默认 `claude-opus-5`。不要为省钱擅自降级——那是项目所有者的决定
- **客户端**：用零参构造 `anthropic.Anthropic()`，让 SDK 自己从环境变量解析凭据
- **思考模式**：用 `thinking={"type": "adaptive"}`。**不要用 `budget_tokens`**，它在 `claude-opus-5` 上会返回 400
- **Agent 循环**：用 Tool Runner（`client.beta.messages.tool_runner`），工具用 `@beta_tool` 装饰器定义，不要手写 while 循环
- **长输出**：用 `.stream()` + `.get_final_message()`，避免请求超时
- **`max_tokens`**：非流式默认 `16000`，流式默认 `64000`。不要给太小导致输出被截断
- **拒绝回退**：写 `claude-opus-5` 请求时默认带上服务端 fallback（`betas=["server-side-fallback-2026-07-01"]` + `fallbacks="default"`），并检查 `stop_reason` 是否为 `"refusal"` 再读 `content`
- **错误处理**：按具体到宽泛的顺序捕获（`NotFoundError` → `RateLimitError` → `APIStatusError` → `APIConnectionError`），不要一个 `except` 兜底
- **不要重复造轮子**：用 SDK 自带的类型和辅助方法

### 21.7 跨平台注意事项

- 文件路径一律用 `pathlib.Path`，不要手写 `\` 或 `/`
- 读 CSV 要处理 UTF-8 和 GBK 两种编码（中文 Windows 常见 GBK）
- 不使用仅 Windows 或仅 macOS 可用的库
- `pynvml` 在没有 NVIDIA 卡的机器上会导入失败，必须 try/except 并优雅降级

### 21.8 测试要求

三个模块**必须有测试**，其余尽量：

1. **`core/carbon.py`**（碳测算）——公式算错，整个平台就没意义了。**重点测每百万 Token 碳足迹在各种口径组合下的正确性**
2. **`agent/boundary.py`**（数据边界）——过滤失效，敏感数据就出网了
3. **`core/analysis/`**（三性分析）——统计指标算错会导致错误结论。用构造的已知数据验证 CV、自相关、相位差、变点检测

### 21.9 沟通方式

项目所有者是**编程零基础**的初学者：

- 解释技术问题用大白话和类比，避免堆术语
- 给出的命令要能**直接复制粘贴执行**，并说明在哪里执行
- 报错时先说「这是什么意思」，再说「怎么解决」
- 涉及技术选型的决策，列出选项和优劣，让项目所有者拍板，并把结论补记到[第 15 节](#15-技术方案决策记录)

---

# 附录 A · 排放因子初始条目

> 🔴 **重要声明**：下表数值为**参考值**，用于让平台开箱即用，**不构成任何权威依据**。
> 正式使用前，请务必按你所在**地区、年份、行业**的官方发布数据核对更新。
> 可在「⚙️ 设置与基础表」页随时编辑。

### A.1 电力因子（最关键，务必替换）

> 单位统一用 **gCO₂e/kWh**，与每百万 Token 碳足迹的单位口径一致。

| 因子名称 | 单位 | 参考数值 | 结果单位 | 数据来源类型 | 备注 |
|---|---|---|---|---|---|
| 电力（全国电网平均） | kWh | 570.3 | gCO2e | 生态环境部公布值 | 需按最新年度值更新 |
| 电力（华北区域电网） | kWh | 按实际填 | gCO2e | 区域电网因子 | 各区域差异大，**必须自行填入** |
| 电力（华东区域电网） | kWh | 按实际填 | gCO2e | 区域电网因子 | 同上 |
| 电力（南方区域电网） | kWh | 按实际填 | gCO2e | 区域电网因子 | 同上 |
| 绿电 / 绿证抵扣 | kWh | 0.0 | gCO2e | market-based 口径 | 仅市场法下生效 |

### A.2 其他能源因子

| 因子名称 | 单位 | 参考数值 | 结果单位 | 备注 |
|---|---|---|---|---|
| 柴油（应急发电） | L | 2630.0 | gCO2e | IPCC 缺省值折算 |
| 天然气 | m³ | 2162.2 | gCO2e | IPCC 缺省值折算 |
| 外购蒸汽 | GJ | 110000.0 | gCO2e | 随供热源差异极大，务必替换 |
| 自来水 | t | 300.0 | gCO2e | 行业经验值 |

### A.3 硬件隐含碳参考值

> 🔴 隐含碳数据公开度低，下列为**粗略行业估算**，精度有限。有厂商 EPD 报告的务必替换。

| 设备类型 | 隐含碳（kgCO₂e/台） | 设计寿命（年） | 备注 |
|---|---|---|---|
| GPU 服务器（8 卡，高端） | 3000 | 5 | 随卡型差异大 |
| GPU 服务器（8 卡，中端） | 2200 | 5 | |
| 通用 CPU 服务器（2U） | 1200 | 5 | |
| 存储服务器 | 1500 | 6 | |
| 交换机（数据中心级） | 400 | 7 | |
| UPS（模块化） | 2000 | 10 | |
| 精密空调 | 2500 | 10 | |

### A.4 PUE 参考区间

| 机房类型 | 典型 PUE | 说明 |
|---|---|---|
| 老旧风冷机房 | 1.6 – 2.0 | 改造空间大 |
| 常规风冷数据中心 | 1.3 – 1.5 | 主流水平 |
| 优化风冷 / 间接蒸发 | 1.2 – 1.3 | |
| 液冷智算中心 | 1.1 – 1.2 | 先进水平 |

> 平台不内置 PUE，**必须用你机房的实测值**（总电耗 ÷ IT 电耗）。

### A.5 三性分析的默认参数

> 全部可在界面调整，此处为初始值。

| 参数 | 默认值 | 用在哪 |
|---|---|---|
| 滚动窗口 | 24 个采样点 | 滚动标准差、滚动均值 |
| 高能耗阈值系数 | 1.2 | 高能耗时段识别 |
| 最短持续时长 | 30 分钟 | 高能耗时段识别 |
| 区间合并间隔 | 10 分钟 | 高能耗时段识别 |
| 离群判定 | 均值 ± 2×标准差 | 差异性离群标记 |
| 尖峰分位数 | P95 | 尖峰事件识别 |
| 变点检测窗口 | 前后各 12 个点 | 突变点识别 |
| 变点显著性阈值 | 前后段均值差 > 20% | 突变点识别 |
| 最小样本量 | 10 | 低于此值标注「样本不足」 |

---

# 附录 B · 核心公式与单位换算

### B.1 能耗与 PUE

```
机房总电耗  E_total = E_IT × PUE
PUE        = E_total ÷ E_IT
IT 电耗     E_IT = Σ(设备功率_kW × 时长_h)
卡时估算    E_IT ≈ 卡时 × 单卡平均功率_kW
```

### B.2 碳足迹

```
【运营碳排 · 地域法】
  C_location = E_total × EF_grid

【运营碳排 · 市场法】
  C_market   = (E_total − E_green − E_gec) × EF_grid + E_green × EF_green
             （EF_green 通常为 0；E_gec 为绿证抵扣电量）

【隐含碳摊销】
  C_embodied = Σ [ EC_device × (使用时长_h ÷ 设计寿命_h) ]
             设计寿命_h = 寿命年数 × 365 × 24

【总碳足迹】
  C_total    = C_operational + C_embodied
```

> ⚠️ **单位一致性**：排放因子表用 `gCO₂e/kWh`，而设备隐含碳表用 `kgCO₂e/台`（`server_specs.embodied_kgco2e`）。
> 计算 `C_embodied` 时**必须先 ×1000 换算为 gCO₂e** 再与运营碳排相加，否则结果会差三个数量级。
> 数据库 `carbon_records.emission_gco2e` 统一以 **gCO₂e** 存储。

### B.3 每百万 Token 指标（核心）

```
【Token 总量】
  Tokens = tokens_in + tokens_out        （默认口径）
         = tokens_out                     （可切换口径）
         = tokens_train                   （训练任务口径，单独统计）

【每百万 Token 碳足迹】⭐
  gCO2e/MTok = C_total_gCO2e ÷ (Tokens ÷ 1,000,000)

【每百万 Token 电耗】
  kWh/MTok   = E_total_kWh ÷ (Tokens ÷ 1,000,000)

【能效】
  Tokens/kWh = Tokens ÷ E_IT_kWh          （注意：用 IT 电耗，不含 PUE）

【每百万 Token 成本】
  元/MTok    = 总电费_元 ÷ (Tokens ÷ 1,000,000)
```

> ⚠️ 以上四个指标**必须连同口径标注一起展示**，见 [8.4](#84-口径标注硬性要求)。

### B.4 绿电指标

```
【总量绿电比例】   绿电消纳量 ÷ 总用电量 × 100%
【分时绿电比例】   绿电消纳量(t) ÷ 总用电量(t) × 100%
【绿电消纳率】     绿电消纳量 ÷ 绿电供给量 × 100%
【弃用绿电量】     绿电供给量 − 绿电消纳量
【绿电间歇指数】   供给为零的时段数 ÷ 总时段数 × 100%
```

### B.5 三性分析

```
【差异性】
  能效           = Tokens ÷ E_IT_kWh
  单位Token电耗  = E_IT_kWh ÷ (Tokens ÷ 1e6)
  机型能效比     = 机型A能效 ÷ 机型B能效
  变异系数 CV    = 标准差 ÷ 均值
  离群判定       = |x − 均值| > 2 × 标准差

【时序性】
  时序分解       原始 = 趋势 + 周期 + 残差
                 趋势 ≈ 滚动均值（窗口 = 一个完整周期）
                 周期 ≈ 按小时/星期分组的均值 − 总均值
                 残差 = 原始 − 趋势 − 周期
  自相关 ACF(k)  = corr( x[t], x[t−k] )
                   k=24 显著 → 有日周期；k=168 显著 → 有周周期
  相位差         = argmax_k { 互相关(曲线A, 曲线B, 滞后k) }

【不稳定性】
  滚动标准差     窗口内的标准差（默认窗口 24 点）
  极差比         (max − min) ÷ 均值
  尖峰频次       超过 P95 分位数的次数
  变点判定       |前段均值 − 后段均值| ÷ 前段均值 > 20%
```

### B.6 高能耗时段识别

```
【基线】     所选窗口内功率中位数（或手工指定）
【判定条件】 功率 > 基线 × 阈值系数(默认1.2)
             且 持续时长 > 最短时长(默认30分钟)
【区间合并】 间隔 < 10 分钟的相邻区间合并
【超出幅度】 (区间平均功率 − 基线) ÷ 基线 × 100%
```

### B.7 成本

```
【电量电费】  Σ(各时段电量 × 该时段电价)
【需量电费】  当月最高需量_kW × 需量单价
【总电费】    电量电费 + 需量电费 + 其他费用
【度电成本】  总电费 ÷ 总电量
【度电碳强度】总碳排 ÷ 总电量
```

### B.8 碳资产

```
【履约缺口】核算排放 − 免费配额 − 已购配额 − 绿电抵扣 − CCER抵销
           （CCER 抵销受比例上限约束，超出部分自动截断并提示）
```

### B.9 单位换算

| 换算关系 | 系数 |
|---|---|
| 1 tCO₂e | = 1000 kgCO₂e = 1,000,000 gCO₂e |
| 1 kWh | = 3.6 MJ |
| 1 MWh | = 1000 kWh |
| 1 GJ | = 1000 MJ = 277.78 kWh |
| 1 tce（吨标准煤） | = 29.3076 GJ |
| 1 kW 持续 1 小时 | = 1 kWh |
| 1 卡时（单卡 400W） | ≈ 0.4 kWh |
| 1 MTok | = 1,000,000 Token |

---

**文档结束** ｜ 有任何需求变更，请直接修改本文件，它是这个项目的唯一事实来源。
