# ============================================================
#  这里就是填 Claude API 密钥的地方
# ============================================================
#
# 用法（只需一次）：
#   Mac/Linux:  cp .env.example .env
#   Windows:    copy .env.example .env
#   然后编辑 .env，把下面第 15 行的占位文字换成你自己的密钥。
#
# 密钥在 https://console.anthropic.com/settings/keys 生成，形如 sk-ant-...
# 本文件（.env.example）是模板，会进仓库；.env 不会，放心填。
# 填好后运行 python check_setup.py 自检。
#
ANTHROPIC_API_KEY=sk-ant-在这里粘贴你的key

# 可选：换模型只改这一行（LangChain 版生效），留空则用 claude-opus-5
# LC_MODEL=claude-sonnet-5
